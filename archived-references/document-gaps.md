<!-- GENERATED by tools/references/refs.py index. Do not edit by hand. -->

# References with no document

**One thing puts a reference here: the archive could not get the document.**
Nothing under `md/` and `pdf/` carries this research, so there is something to
FETCH. Its two sibling reports are about references the archive DOES have:

| Report | The document | What is missing |
|---|---|---|
| document-gaps.md | not archived | the document itself |
| [review-gaps.md](review-gaps.md) | archived | a judgement that it came out right |
| [store-gaps.md](store-gaps.md) | archived | the source bytes it was made from |

Three shapes of the same problem, at different stages:

- **stub** - a file exists but carries a record rather than a document (its grade
  is `records`): a stub, a metadata-only page, a talk with no transcript.
- **unresolved** - no file at all, because conversion or the fetch failed.
- **broken capture** - a file was written from something that is not the
  document at all, so it was withdrawn.

A reference that HAS its archived Markdown and PDF is not listed here, even when
the content store no longer holds the bytes those files were made from. That is
a store-integrity fault, not an acquisition one: the document is archived and
published, and what is missing is the evidence behind it. Those are listed on
[store-gaps.md](store-gaps.md) so a reader of this file is never sent to fetch
something the archive already has.

A record that is a record BY NATURE - a CVE database row, a vendor advisory,
release notes, a package page - is not listed here either. Nothing is missing
from those; see [excluded.md](excluded.md) for the pages kept with no document.

Everything here is still CITED and still recorded in `manifest.json`.

Nothing here is lost work: the raw bytes of anything that was fetched are in the
content store, so a better converter or a different route is an offline re-run
(`refs.py acquire --force`), not a second crawl.

**This is the file to read if you want to fetch something a different way.**
Hand a file to the tool with `refs.py acquire --only <url>` after dropping it in,
or fix the route and re-run.


489 reference(s) unresolved. 442 of them already have their raw bytes stored.

## http://archives.neohapsis.com/archives/fulldisclosure/2007-01/0062.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE MESSAGE HEADER BLOCK WAS DROPPED, so the archived mailing-list post no longer states who sent it or when - the source carries a From line naming the researcher with his address and a Date line, and neither reaches the body. published is empty as a result, although the source states the date twice. The four numbered vulnerability sections each start a fresh list, so the rendered document numbers them 1, 1, 1, 1. An escaped tag pair in the author's own text was decoded into live markup. Head furniture is retained - the publisher's masthead, two logos and a recruitment advertisement - and 396 characters of navigation furniture remain at the end; the mailing list's own charter and sponsor lines above that are part of the archived message and should be kept (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:6`

## http://blog.48bits.com/2010/09/28/iis6-asp-file-upload-for-fun-and-profit/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the extractor read a UTF-8 page as Latin-1, producing 187 corrupt characters across 160 damaged tokens - extension, caracter, codigo, despues, podriamos and 35 more all publish with mojibake. The stored raw declares UTF-8 and decodes cleanly. The diagnostic is conclusive: the ONE Latin-1-encoded region in the raw, an IRC-quote widget, came out CORRECT while everything UTF-8 came out wrong. Literal UTF-8 smart quotes were also truncated to a bare byte, while entity-escaped quotes survived. The recorded title carries the blog masthead and breadcrumb in both frontmatter and body H1 (reported 2026-08-15) ITS TRANSLATION ALSO LEAVES ONE PARAGRAPH IN SPANISH: the entire 'The response' section body publishes untranslated in the English copy, and four blockquotes are empty - a bare marker followed by a blank line - so the material they should quote sits outside the quote. (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand. ENCODING REPAIRED 2026-08-17: the page is utf-8 with a few stray bytes and is now decoded as utf-8 rather than latin-1; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:60`

## http://blog.andlabs.org/2010/08/xssing-client-side-dynamic-html.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THREE BLOCKQUOTES LOST THEIR QUOTING - the published file has orphaned markers on their own lines with the quoted material following unquoted, so a passage quoted from Matt Austin now reads as the archived author's own words. Only the third quote survived. The closing JavaScript example is published as bare prose with no fence, and an img tag sits inline as live raw HTML. The byline was dropped although the source carries it twice, as a post-author vcard and a rel=author link, naming lava (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:54`

## http://blog.andlabs.org/2010/09/re-visiting-java-de-serialization-it.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: escaped markup was decoded into live tags, and it costs the sentence its meaning. The stored source has the element names escaped and bolded; the published file has real string-array and map tags, so the prose renders as "We can clearly see the and the elements" and map becomes a live HTML element. The byline was also dropped although the source carries a rel=author block naming Manish S., corroborated in-body by the Black Hat EU anchor and the demo data. All the DSer shell listings are unfenced and split across blank lines (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:38`

## http://blog.checkpoint.com/2015/08/04/wordpress-vulnerabilities-1/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every code excerpt the article argues from is absent - the prose introduces listings that are simply not there (Let us observe an excerpt from the map_meta_cap() code, which uses the following code, Note the highlighted code) and the file contains zero code fences and zero images, so the original screenshots are lost; the article then stops on an empty POC section with Admin token retrieval and Race Condition headings and nothing under either (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:28`

## http://blog.guya.net/2008/09/10/bug-in-internet-explorer-security-model-when-embedding-flash/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE'S ONLY EVIDENCE WAS REJECTED AS FURNITURE. The images step recorded 0 of 1 preserved, refusing error-access-denied-thumb.png on the ground that "435x121 is furniture rather than a figure" - but that is the "Access is denied" screenshot the prose points straight at, and the full-size version behind it was never enumerated at all. The same thumbnail is also published twice, once as a hero image pulled in from og:image and once in place. A stray pair of backticks terminates the last body line and reaches the PDF. 678 characters of furniture remain at the end, a related-posts block that includes a 2015 post and a one-line reader comment carrying no technique. Note the sole code listing is flattened onto one line so its trailing comment swallows the rest - that is SOURCE-INHERENT, the publisher's modern rebuild ships it that way, and a re-render cannot restore the line breaks (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:23`

## http://blog.kotowicz.net/2010/11/xss-track-how-to-quietly-track-whole.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a live iframe tag swallows most of the article. The stored source has it escaped; the published file has it unescaped as inline HTML, and because no closing iframe tag exists anywhere in the document the parser consumes 9,585 characters - lines 87 to 250, all five code listings, all three figures and the whole Demonstration section - as unrendered fallback text. The iframe mentions inside the fenced blocks are safe; this one is not (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:52`

## http://blog.kotowicz.net/2011/03/exploiting-unexploitable-xss-with.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the demonstration was dropped, leaving a dangling lead-in. The body ends a sentence with "See for yourself:" and nothing follows; the stored source carries a YouTube iframe at that point. The one artefact showing the drag-and-drop attack actually working is gone. Everything else is sound - the single escaped payload survived intact inside its fence, and the published file has no deleted event handlers (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:34`

## http://blog.mindedsecurity.com/2010/04/fooling-b64encodepayload-on-wafs-and.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The body is 165 lines of which ZERO are prose paragraphs - it is the blog tag-cloud sidebar, 164 label links, plus the masthead. Identical signature to the 2011 sibling on this blog, where the article was confirmed present in the stored source and the extraction chooser preferred the raw candidate for having the most words, every one a label name. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:39`

## http://blog.mindedsecurity.com/2010/04/mysql-stacked-queries-with-sql.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The body is 165 lines of which ZERO are prose paragraphs - it is the blog tag-cloud sidebar, 164 label links, plus the masthead. Identical signature to the 2011 sibling on this blog, where the article was confirmed present in the stored source and the extraction chooser preferred the raw candidate for having the most words, every one a label name. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:40`

## http://blog.mindedsecurity.com/2010/10/dns-rebinding-on-java-applets.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The body is 165 lines of which ZERO are prose paragraphs - it is the blog tag-cloud sidebar, 164 label links, plus the masthead. Identical signature to the 2011 sibling on this blog, where the article was confirmed present in the stored source and the extraction chooser preferred the raw candidate for having the most words, every one a label name. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:35`

## http://blog.mindedsecurity.com/2010/10/get-internal-network-information-with.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The body is 165 lines of which ZERO are prose paragraphs - it is the blog tag-cloud sidebar, 164 label links, plus the masthead. Identical signature to the 2011 sibling on this blog, where the article was confirmed present in the stored source and the extraction chooser preferred the raw candidate for having the most words, every one a label name. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:42`

## http://blog.mindedsecurity.com/2010/10/java-applet-same-ip-host-access.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The body is 165 lines of which ZERO are prose paragraphs - it is the blog tag-cloud sidebar, 164 label links, plus the masthead. Identical signature to the 2011 sibling on this blog, where the article was confirmed present in the stored source and the extraction chooser preferred the raw candidate for having the most words, every one a label name. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:43`

## http://blog.mindedsecurity.com/2011/10/autocompleteagain.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The published body is one masthead line plus 164 tag-cloud sidebar links - 14,057 characters, 164 list items, ZERO headings and ZERO prose paragraphs. The article is in the store and was dropped: the post-body element holds about 2,031 characters of prose beginning "This is a short post about autocomplete feature in browsers", two figures and the link to the greasemonkey checker. Note HOW it happened - steps.acquire.extraction chose the raw candidate with the reason "raw kept the most code blocks and text", scoring 164 links and 0 code blocks highest, so the chooser measured link soup and preferred it. The rel=author byline naming Stefano Di Paola was dropped with the article. Remedy: re-extract from the stored raw object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:38`

## http://blog.php-security.org/archives/54-JavaScriptHTML-Portscanning-and-HTTP-Auth.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 8,917 CHARACTERS WERE CUT FROM THE END - the byline, the categories, the time, and ALL FIVE COMMENTS - while the chrome that INTRODUCES the comments was kept, so the document now ends with a lead-in to comments that are not there. TWO OF THE FIVE CARRY TECHNIQUE: one sets out a full request-forgery chain against home routers riding remembered credentials, which the article never states; and THE AUTHOR'S OWN REPLY IS THE PAYOFF OF THE ARTICLE'S CLOSING "to be continued", naming password brute-forcing as the next step and pointing at a SIBLING REFERENCE IN THIS SAME COLLECTION. The byline is missing although the source states it in the posted-by line and again in the entry's own class name, and published is empty although the date and time appear twice in the body. An empty emphasis element also became a dangling pair of asterisks that renders literally (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:27`

## http://blog.php-security.org/archives/56-Bruteforcing-HTTP-Auth-in-Firefox-with-JavaScript.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PROOF OF CONCEPT CANNOT BE COPIED OUT. The publisher's syntax highlighter wraps every angle bracket in a bold element, and the converter turned each one into a pair of asterisks INSIDE the code fences - so the listing publishes as "****<**html**>****" and "onLoad=\"okPW()\" onError=\"wrongPW()\"**>****" where it should read plain markup. Because the asterisks are inside fences they are shown literally, so a reader gets a listing that will not run. The handlers themselves survived correctly. The byline is missing although the source states "Posted by Stefan Esser", and published is empty although the source states the date TWICE and both copies survive in the published body. 208 characters of comment-display furniture remain at the end; the four comments there were read and none carries technique, so the cut is safe, and the date is above it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:28`

## http://blog.securitee.org/?p=37

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all nine image references point at a host that now redirects to its site root, and the manifest records UnidentifiedImageError for all eight distinct images - nothing was preserved. That kills the four screenshots the argument is built on. Publisher furniture also remains at the end: the category and permalink line, three pingback teasers, a Leave a Reply block, a CAPTCHA image and the allowed-tags list, 1,145 characters. The 14 real reader comments were correctly dropped; only the pingbacks leaked (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:13`

## http://blog.washingtonpost.com/securityfix/2006/01/account_hijackings_force_livej.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: SIXTY-SEVEN COMMENT BODIES WERE DROPPED WHILE ALL SEVENTY BYLINES WERE KEPT, so the file ends with seventy orphan attribution lines and roughly 19,354 characters of discussion gone. THE FIRST DROPPED REPLY IS THE ARTICLE'S OWN AUTHOR adding what the article omits - he quotes the attackers directly to correct the record, that the vendor's fix targeted a browser-specific bug which their own cross-browser attacks never used. The attackers' stated motive is in another dropped comment. The recorded title is the BLOG MASTHEAD, carried on every post of that blog; the document's own heading names the incident and matches the citation exactly. About 7,050 characters of trackbacks, orphan bylines and a comment form remain at the end, and published is empty although the byline states the date. One surviving commenter handle is mojibake, UTF-8 read as Latin-1. CLEARED for the record: this is NOT a section index - the article itself is fully archived with its outbound links intact (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:65`

## http://blog.watchfire.com/wfblog/2007/10/favorites-gone.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline and the date were dropped together, because the precision candidate cut the entry footer where both live. The stored source states "Posted by Yair Amit on October 23, 2007" and repeats the name in its syndication metadata; authors is empty and published is empty. Worth recording for future runs: THIS PUBLISHER'S TEMPLATE DOES NOT USE rel=author AT ALL - a search returns zero hits - so a byline sweep keyed on that marker will miss every post on this blog. The recorded title prefixes the blog masthead onto the document's own title, which is "Favorites Gone Wild". The post's only figure resolves against the dead host rather than the Wayback replay and recorded an empty response. A DROPPED COMMENT DIRECTLY ANSWERS THE POST'S CLOSING QUESTION: the article ends by asking how an attacker could inject a Favorite, and a commenter proposes the route (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:38`

## http://blog.watchfire.com/wfblog/2008/06/cross-environ-1.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE SAME-ORIGIN COMPARISON TABLE IS DESTROYED AND THE ATTACK DIAGRAM IS LOST. The table's header cells are exploded across thirteen lines as lone pipes and lone bold words, so no separator row can match and the PDF prints the whole thing as one run-on paragraph - the reader cannot map method to same-origin-policy to content access, which is the point of the table. The one figure, the attack-flow diagram, recorded an empty response and is referenced against the DEAD host rather than the Wayback replay the text came from, leaving the sentence "hopping from one port (environment) to another on the computer, as illustrated below" with nothing after it. A blockquote is broken - a bare marker, a blank line, then the quoted definition at column 0. The 74 scripts publish as 74 separate ONE-LINE fenced blocks. THE DROPPED COMMENT THREAD CARRIES TECHNIQUE: kuza55 challenges the attack against the Java reverse-DNS fix and the Flash socket-policy requirement and posts the exact exception, the author answers with the tested Firefox and JRE versions and explains why DNS-rebinding defences do not apply, and Dan Kaminsky posts dig output plus a workaround. published is empty although the source states June 23, 2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:49`

## http://blog.watchfire.com/wfblog/2008/10/flash-parameter.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: EIGHTY-TWO PERCENT OF THE PUBLISHED BODY IS THE BLOG SIDEBAR. The chooser was offered precision at 1,016 characters with 3 links - the article - and took raw at 6,005 characters with 59 links, on the tie-break "raw kept the most code blocks and text" when every candidate had ZERO code blocks, so the decision fell entirely to character count. The result publishes a Twitter badge, an AppScan advert, ten recent posts, ten monthly archives, twelve category links, eight resource links and a nine-link blogroll; 4,920 characters of it are a removable tail. authors is empty although THE ARCHIVED PDF'S OWN COVER PAGE names Yuval Baror, Ayal Yogev and Adi Sharabani, and the blog body separately names Sharabani and Yogev; the blog's rel=author is the publisher's own account and correctly does not count on its own. published is empty although the source states October 02, 2008 and the paper is dated September 24, 2008. Both images are furniture - an avatar and an advert banner - and both were KEPT as failed placeholders while the document has no figure of its own (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:14`

## http://blog.watchfire.com/wfblog/2008/12/breaking-google-gears-cross-origin-communication-model.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the source's entry footer reads "Posted by <a rel="author">Yair Amit</a> on December 08, 2008" - the SAME marker the archive already used to credit four other references on this blog, so this is an inconsistency within the archive rather than a hard case - and published is empty despite the same line. A blockquote is broken: a bare marker, a blank line, then the quoted Gears documentation at column 0, so the vendor quotation is indistinguishable from the author's own prose. An emphasis run is broken too, six trailing asterisks and an orphan marker on the following line. Note the Gears excerpt appearing twice was checked and is SOURCE-INHERENT, the original HTML repeats it (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:6`

## http://blog.watchfire.com/wfblog/2009/02/active-man-in-the-middle-attacks.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the article is 1,283 characters inside a 6,523-character body; the remaining 4,216 characters are TypePad trackback, archive, category, recent-post and blogroll lists - and the line "Listed below are links to weblogs that reference..." is followed by the MONTHLY ARCHIVE INDEX rather than any trackback. Same chooser fault: raw at 5,579 characters and 58 links beat precision at 1,278 characters and 3 links. The recorded title also carries the blog masthead in frontmatter and body H1. The byline was dropped: the source reads "Posted by Ory Segal on February 27, 2009"; Roi Saltzman is a commenter and Adi Sharabani the keynote subject, neither taken. This is additionally a LANDING PAGE - the research is the linked AMitM.ppt and AMitM.pdf, also_at is empty, and the host is now DNS-dead, so neither artefact is archived (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:76`

## http://blog.watchfire.com/wfblog/2011/10/dns-poisoning-via-port-exhaustion.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the PUBLISHED BODY ITSELF signs off "-Roee Hay and Yair Amit", and the stored source carries a rel=author byline naming Roee Hay with the date. The recorded title is the blog title tag, masthead plus post title, reaching both the frontmatter and the body H1, where the source own post heading is just "DNS poisoning via Port Exhaustion" and cited_title already says so. Half the file is navigation: 4,398 characters of trackback listing, monthly archives, categories, recent posts and blogroll against 1,867 characters of article, and the extraction chooser picked the raw candidate for keeping the most text. The cited post is an ANNOUNCEMENT - the actual whitepaper sits behind a bit.ly link in the body and was never resolved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:7`

## http://blueinfy.com/wp/blindsql.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: WORDS ARE SPLIT MID-TOKEN THROUGHOUT AND pdftotext READS THEM CLEANLY. The body publishes XP_CMDSH/ELL, b/lind, SQ/L injection, penetr/ation, loophol/e, mast/er..xp_cmdshell and IIs/WebVirtualDir with the break inside the word; pdftotext -layout on the same stored bytes returns every one intact, so the damage is extractor-introduced and repairable offline. Three code listings - the VBS script, the xp_cmdshell URL sequence and the Metasploit block - are unfenced at column 0 and merge with the surrounding prose, and the running footers are glued into the text. The byline was dropped although the cover page names the author twice, "by Shreeraj Shah" and an author block giving his role, company and email. Provenance is also mislabelled: the header prints "Current location: https://blueinfy.com" and "Preserved from: https://blueinfy.com (stored)" when the document came from a Wayback replay of the paper's own URL, and the five images listed on the entry are the HOMEPAGE's leftovers from the bad capture. One apparent truncation was checked and is source-inherent (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:56`

## http://events.ccc.de/congress/2011/Fahrplan/events/4680.en.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the 28C3 programme entry stands in for the talk. The pointer-page grading is sound on an intact extract - the page description element really is empty - but the SLIDES are linked inside the captured page as a 6 MB PDF (2007_28C3_Effective_DoS_on_web_application_platforms.pdf) and were never resolved; also_at is empty. The year list cites this for HashDoS and the technique lives in that deck, not in the 2,380-character abstract. authors also names only Alexander Klink where the page names a SECOND speaker, Julian Waelde. A literal pipe inside his name as the publisher renders it splits the Markdown table row and breaks the link (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:58`

## http://foregroundsecurity.com/MyBlog/flash-origin-policy-issues.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 10,837 of 22,728 characters - 48 percent of the file - are tail furniture. It begins at a Subscribe to this comment's feed line and runs through 92 gravatar placeholder image lines whose comment TEXT was stripped, leaving bare avatars, then the whole services sidebar, ending on a marketing thumbnail link. Head chrome is retained too - logo, Press Room, Quick Contact, a Your Message form and empty pipe rows. TWO VIDEO EMBEDS WERE DROPPED leaving dangling lead-ins: the body reads "but it worked beautifully. Here's video:" and "Mike Murray put together a quick video on the use and installation of these two products:" with nothing after either, where the stored source carries both YouTube embeds. All nine images recorded an empty response, including the two real figures, cpanel_flash_upload.png and squirrelmail_flash_upload.png, which still point at the dead original host (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:32`

## http://grutztopia.jingojango.net/2008/01/another-free-macworld-platinum-pass-yes.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE JOHN THE RIPPER FILTER - THE REPRODUCIBLE CORE OF THE ARTICLE - PUBLISHES AS PROSE. The john.conf external filter sits unfenced at one-space indent, so the renderer flows it into paragraphs and its C comment swallows the statement that follows on the same line: the PDF prints "while (c = word[i]) { // If character is lower case, convert to upper if (c >= 'a' && c <= 'z') word[i] &= 0xDF; i++; }". A shell prompt line beginning # renders as an H1 HEADING inside a blockquote, another line lost its quote continuation so half the keyspace table falls out, and all five of the article's bold-italic section headings publish unemphasised, so the structure is gone. The demo video embed was dropped, leaving "Download the High Quality version" dangling. The byline was dropped although the blog's profile widget names Kurt Grutzmacher and the same profile replies in the thread as the post's author; published is empty although the source states 2008-01-14 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:25`

## http://ha.ckers.org/blog/20060615/a-story-that-diggs-itself/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS PURE SITE CHROME WITH NO TOPIC AT ALL - "ha.ckers.org web application security lab" - in the frontmatter, the body H1, the sources title AND the PDF metadata. The document's own title is "A story that diggs itself", from its bookmark heading and from the breadcrumb still preserved in the body, and it matches the year list's citation. The raw title element is structurally IDENTICAL to siblings that came out with their topic intact, so the topic was available and was discarded. THIS SLUG HEADS A TWELVE-DOCUMENT TOPICLESS FAMILY spanning three collections, five of them in this one. The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. Three comments dropped, one adding the prior-art paper and the mitigation the post says is hard. 3,930 characters follow; the posted-on line is the ONLY place the date appears: Thursday, June 15th, 2006 at 10:45 am (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:31`

## http://ha.ckers.org/blog/20060728/cross-domain-leakage-with-image-size/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWELVE MOJIBAKE CHARACTERS from a charset mismatch - the source declares ISO-8859-1 while its bytes are UTF-8, and the converter honoured the declaration and double-decoded, so a stray capital A-circumflex now sits in twelve sentence gaps. This is the only document in its group with the fault, which makes it a decode slip rather than a source artefact. The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "Cross Domain Leakage With Image Size". The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. Two comments dropped, 3,469 characters. The posted-on line is the ONLY place the date appears: Friday, July 28th, 2006 at 1:40 pm (reported 2026-08-15) ENCODING REPAIRED 2026-08-17: the page is utf-8 with a few stray bytes and is now decoded as utf-8 rather than latin-1; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:33`

## http://ha.ckers.org/blog/20060731/expect-header-injection-via-flash/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "Expect Header Injection Via Flash". The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. Three comments dropped, 4,343 characters; none carries technique for this post. The posted-on line is the ONLY place the date appears: Monday, July 31st, 2006 at 8:58 am. The two names in the body are the vulnerability's discloser and the person who pointed the author at it - prior-art credits rather than this post's byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:32`

## http://ha.ckers.org/blog/20060815/circumventing-dns-pinning-for-xss/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title concatenates the site masthead onto the document's own title with no separator, where the post's own heading is "Circumventing DNS Pinning for XSS". 362 characters of furniture remain at the end and that block is the ONLY place the date appears: Tuesday, August 15th, 2006 at 10:22 am. WORTH RECORDING FOR THE YEAR LIST: the research this post describes is credited in its own body to another researcher, whose disclosure it links - the year list cites this as a post by the blog owner while the technique is not his. There is no byline evidence in this capture, so the empty authors field is correct. Note the wayback step records a failed lookup while the health record carries a snapshot and the bytes are present and verify - the step is stale, not a gap (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:48`

## http://ha.ckers.org/blog/20060817/variable-width-encoding/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DOCUMENT'S ONLY CONCRETE MARKUP LIVES IN A DROPPED COMMENT. The post itself carries no code listing at all; the sole event handler anywhere in the source is in the first reader comment, which demonstrates the DEFENDED rendering - what the markup looks like once quotes are escaped both inside and outside attributes - and the published file has none of it. The following comment is the blog owner conceding the mitigation, a defence the post never states, and it also records that the payload was mangled by the publisher at source. The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. Six comments dropped, 6,178 characters - more than the published article. The recorded title concatenates the masthead onto the document's own title with no separator, where the post's own heading is "Variable Width Encoding". The posted-on line is the ONLY place the date appears: Thursday, August 17th, 2006 at 11:50 am. The name in the body is the finder of the underlying evasion, a prior-art credit (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:53`

## http://ha.ckers.org/blog/20060821/stealing-user-information-via-automatic-form-filling/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. TWELVE COMMENTS WERE DROPPED - 11,690 characters, more than four times the published article - AND THE THREAD IS WHERE THIS TECHNIQUE WAS ACTUALLY BUILT. The post is 327 words; the thread carries a complete step-by-step proof of concept for the password-manager attack with a live demo URL, the automation the post only gestures at with its polling interval, the cross-subdomain limitation, and an extension of the attack to router credentials with a confirmed working proof. The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "Stealing User Information Via Automatic Form Filling". The posted-on line is the ONLY place the date appears: Monday, August 21st, 2006 at 6:40 pm (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:47`

## http://ha.ckers.org/blog/20060823/css-history-stealing-acts-as-cookie/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A BROKEN BLOCKQUOTE SILENTLY REASSIGNS A CONTRIBUTOR'S TECHNIQUE TO THE BLOG OWNER. The source wraps roughly 1,500 characters of another researcher's proposal in a blockquote; the published file emits a bare marker, a blank line, then his text at column 0, so it renders as an empty quote box followed by ordinary prose and THE READER CANNOT TELL WHOSE TECHNIQUE THE HIERARCHICAL VIRTUAL-FOLDER SCHEME IS. The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "CSS History Stealing Acts As Cookie". The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. Four comments were dropped; one adds the prior-art paper on cache cookies that the post lacks, and another raises the browser history-expiry limit that materially bounds the attack. 5,101 characters follow; the posted-on line is the ONLY place the date appears: Wednesday, August 23rd, 2006 at 8:29 pm (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:45`

## http://ha.ckers.org/blog/20060823/detecting-firefox-extentions/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - in the frontmatter, the body H1 AND the PDF metadata title. The document's own title, from its own bookmark heading, its permanent-link attribute and the page breadcrumb, is "Detecting FireFox Extentions", keeping the author's own spelling. This is the fifth document in its batch of thirteen published under that one meaningless string. 643 characters of furniture remain at the end and that block is the ONLY place the date appears: Wednesday, August 23rd, 2006 at 9:15 am. TWENTY-TWO COMMENTS WERE DROPPED - the richest thread in its group - and they carry technique the post does not: a reader sets out and NAMES a technique in six numbered steps; another supplies the reference sources that make the extension mapping reproducible; the detection is extended to an anonymity network and then to operating-system fingerprinting; another gives a CROSS-BROWSER BYPASS of the whole approach; and the thread establishes the platform scope the post gets wrong, proving it fires on two Linux distributions where the post implies Windows. Two trackbacks in the same block independently credit the blog owner with the post (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:46`

## http://ha.ckers.org/blog/20060827/response-splitting-filter-evasion/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title concatenates the masthead onto the document's own title with no separator, where the post's own heading is "Response Splitting Filter Evasion". 765 characters of footer furniture remain at the end and the posted-on line is the ONLY place the date appears: Sunday, August 27th, 2006 at 7:38 pm. Worth recording as a POSITIVE: this is the only document in its group that carries its payload in the published file, and the handler inside it survives byte-for-byte; it is also the only one with no comments at source, so nothing was dropped, and its PDF is the cleanest of the group. The payload is unfenced but contains no active characters, so nothing is eaten - worth fencing for clarity rather than as a repair (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:44`

## http://ha.ckers.org/blog/20060908/dns-pinning-just-got-worse/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 624 characters of furniture remain at the end and that block is the ONLY place the date appears: Friday, September 8th, 2006 at 8:44 am. TWELVE COMMENTS WERE DROPPED and the thread extends the technique from READ to WRITE: a ten-turn exchange on taking internal servers down from inside, intranet phishing, and abusing intranet form-to-email handlers to send authenticated spam that passes sender authentication - none of it in the article. The snapshot field is EMPTY for this entry, so the published file carries no capture timestamp and no capture source line, unlike its siblings, although the bytes are present and verify. Note the title here was already corrected by a maintainer decision and the correction verifiably reached the frontmatter, body H1 and PDF metadata. The three names in the body are prior-art credits (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:7`

## http://ha.ckers.org/blog/20060911/detecting-privoxy-users-and-circumventing-it/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DROPPED THREAD CONTAINS A WORKING BYPASS OF THE DETECTOR AND A BETTER DETECTOR. Thirteen comments were removed; in them a reader publishes an independent and better test using a pseudo-top-level domain reachable only through the anonymity network - which the blog owner concedes he had not seen - then supplies its false-positive and false-negative profile, warning that hostname-keyword matching misfires on two ordinary domains. Another reader gives the DEFENSIVE bypass, blocking the detector by the same means the proxy blocks advertisements. A third contests the premise, and a trackback announces a sequel using a different mechanism. 306 characters of furniture remain at the end - the shortest tail in its group - and that block is the ONLY place the date appears: Monday, September 11th, 2006 at 3:01 pm. The recorded title concatenates the masthead onto the document's own title with no separator (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:42`

## http://ha.ckers.org/blog/20060911/using-css-to-de-anonymize/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS PURE SITE CHROME WITH NO TOPIC AT ALL, in the frontmatter, the body H1, the sources title and the PDF metadata - even though this slug does carry the topic. The document's own title is "Using CSS to De-Anonymize", from its bookmark heading and from the breadcrumb preserved in the body. The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. One comment dropped. 2,546 characters follow; the posted-on line is the ONLY place the date appears: Monday, September 11th, 2006 at 9:44 am. The name in the body is the author of the underlying CSS trick, a prior-art credit (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:43`

## http://ha.ckers.org/blog/20060928/google-indexes-xss/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - in the frontmatter, the body H1 AND the PDF metadata title. The document's own title, from its own bookmark heading, its permanent-link attribute and the page breadcrumb, is "Google Indexes XSS". THE POST'S ONLY EVIDENCE IS ALSO LOST: the screenshot the article exists to present - its claim is that this is the first hundred-percent proof that the search engine indexes cross-site scripting attacks - recorded an empty response because it was resolved against the DEAD host rather than the Wayback replay this capture came from, and the replay path for it is known. 658 characters of furniture remain at the end and that block is the ONLY place the date appears: Thursday, September 28th, 2006 at 8:18 am. Six comments were dropped, one carrying a year of logged data on relaying attacks through search-engine crawlers. The name in the body is the finder, a prior-art credit rather than the byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:39`

## http://ha.ckers.org/blog/20061005/google-dorks-strike-again/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - in the frontmatter, the body H1 AND the PDF metadata title. The document's own title, from its own bookmark heading, its permanent-link attribute and the page breadcrumb, is "Google Dorks Strike Again". A BLOCKQUOTE IS BROKEN so a contributor's QUOTED EMAIL is reattributed to the blog owner - a bare marker, a blank line, then his words at column 0, running on as flat prose with no separation from the owner's own paragraph. THE DORKS THEMSELVES ARE UNUSABLE AS PRINTED: the source's hard line breaks became soft newlines, so each search string rejoins with an inserted space when rendered, and the PDF shows one broken across two lines mid-parameter. 622 characters of furniture remain at the end and that block is the ONLY place the date appears: Thursday, October 5th, 2006 at 8:28 am. Note the contributor named in the body is the dorks' author, a prior-art credit, and there is no byline evidence anywhere in this capture, so the empty authors field is correct here (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:73`

## http://ha.ckers.org/blog/20061103/selecting-encoding-methods-for-xss-filter-evasion/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. ELEVEN COMMENTS WERE DROPPED - 9,623 characters, more than three times the published article - and they carry technique the post does not: a WORKING PAYLOAD AGAINST A DIFFERENT INJECTION POINT, injecting into the charset parameter itself rather than the data parameter; the discovery of a third unused parameter; and the blog owner stating the PRECONDITION the post never gives, that the technique needs angle brackets and quotes filtered inside a parameter but quotes unfiltered outside it. The pointer-page grade is defensible for the post, but the technique is in the thread. The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "Selecting Encoding Methods For XSS Filter Evasion". PROVENANCE GAP: the snapshot field is empty and the wayback step records a failed lookup, so this is the one document in its group with no capture timestamp and no capture source recorded, although the bytes are present and verify. The posted-on line is the ONLY place the date appears: Friday, November 3rd, 2006 at 8:26 pm (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:12`

## http://ha.ckers.org/blog/20061108/detecting-states-of-authentication-with-protected-images/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE POST'S ONLY CODE IS ITS ENTIRE TECHNIQUE, AND BOTH CONVERTER FAULTS HIT THE SAME LINE. The author escaped his image tag so a reader could see it; the published file DECODED it into live markup, and then DELETED THE HANDLER OUT OF THE MIDDLE OF IT - exactly twenty characters cut mid-value, leaving the orphan fragment "authenticated')\"&gt;" hanging after the src attribute. The onerror handler IS the detection; what remains describes nothing, and in any renderer that passes raw HTML the line becomes a live broken-image element and the sample disappears entirely. A blockquote is also broken - a bare marker, a blank line, then the code at column 0 - so the sample loses its quoted status. The recorded title concatenates the masthead onto the document's own title, "Detecting States of Authentication With Protected Images", with no separator. 307 characters of furniture remain at the end and that block is the ONLY place the date appears: Wednesday, November 8th, 2006 at 3:30 pm. A DROPPED COMMENT ADDS A TIMING VARIANT of the same oracle that the post does not carry. Note the name in the body is the collaborator who discussed the idea, a prior-art credit rather than this post's byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:36`

## http://ha.ckers.org/blog/20061118/enumerating-through-user-accounts/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title concatenates the site masthead onto the document's own title with no separator, where the post's own heading is "Enumerating Through User Accounts". 308 characters of furniture remain at the end and that block is the ONLY place the date appears: Saturday, November 18th, 2006 at 11:24 pm. Three comments were dropped, one contributing a working search string that the blog owner confirms finds injection flaws at scale - modest, but technique the post does not carry (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:34`

## http://ha.ckers.org/blog/20061123/google-hacks-on-your-behalf/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - in the frontmatter, the body H1 AND the PDF metadata title. The document's own title, from its own bookmark heading, its permanent-link attribute and the page breadcrumb, is "Google Hacks On Your Behalf". 572 characters of furniture remain at the end and that block is the ONLY place the date appears: Thursday, November 23rd, 2006 at 11:32 am. Six comments were dropped, two carrying corroborating measurement - one reporting years of study of which engines could be abused best, another a working case of the technique in the wild. The publisher named in the body is the finding's source, a prior-art credit; there is no byline evidence in this capture, so the empty authors field is correct here (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:72`

## http://ha.ckers.org/blog/20061206/malformed-url-in-image-tag-fingerprints-internet-explorer/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE POST'S ENTIRE FINDING IS INVISIBLE. The author escaped his image tag so a reader could see the malformed percent-escape that fingerprints the browser; the published file DECODED it into live markup at column 0 inside a paragraph, so any renderer that passes inline HTML emits a real image element against a dead host - the reader sees a broken-image icon and the payload text, which IS the finding, is gone. The escape sequence itself was double-decoded on top of that. The PDF inherits the same defect. The recorded title is the site masthead plus an Archive breadcrumb in the frontmatter, the body H1 AND the PDF metadata title; the document's own title, from its bookmark heading and its permanent-link attribute, is "Malformed URL in Image Tag Fingerprints Internet Explorer". The comment thread was dropped OUT OF THE MIDDLE of the document while the EMPTY REPLY FORM after it was kept, so the file publishes bare field labels and no comments. SIX COMMENTS WERE DROPPED AND THEY CORRECT THE POST'S SCOPE: a reader reports the tag rendering fine in an earlier browser version and the author CONCEDES the fingerprint is specific to one version only - the published article states it unqualified, and the correction exists nowhere in the archive. 4,915 characters of comments and furniture follow; the posted-on line is the ONLY place the date appears: Wednesday, December 6th, 2006 at 12:20 pm (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:26`

## http://ha.ckers.org/blog/20061211/exponential-xss-attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DROPPED THREAD CARRIES THE PROOF OF CONCEPT THE POST SAYS IT LACKS. Nine comments were removed. In them the author supplies the THREE CONCRETE COMPONENTS the article only gestures at, naming a worm demonstration, an Expect-header vulnerability demo and a cross-domain leak, and closes "I haven't written a PoC, but you can see all of the puzzle pieces are there". A reader then raises the binding constraint - the browser's concurrent-request limit - and a third REBUTS it, explains that the limit is per-subdomain, notes a further limitation of the leak and adds a link-rewriting variant. The author also records in the thread that some comments had already been lost at source. Note the title here was ALREADY CORRECTED by a maintainer decision and the correction verifiably reached the frontmatter, the body H1 and the PDF metadata - no repeat of the recorded-but-unpublished trap. 619 characters of furniture remain at the end and that block is the ONLY place the date appears: Monday, December 11th, 2006 at 10:15 am. The two names in the body are the idea's originator and a prior technique's author - prior-art credits, and this post explicitly frames itself as developing the first (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:11`

## http://ha.ckers.org/blog/20061214/adultspace-xss-worm/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE WORM SOURCE IS THE ONLY PRIMARY ARTEFACT OF THIS CITATION AND IT IS UNPRESERVED. The post links a zip archive containing the worm; it was never fetched, is not in the images inventory, and is recorded nowhere in the manifest - the Wayback replay for this capture's snapshot is the recovery path. The recorded title concatenates the site masthead onto the document's own title with no separator, where the post's own heading is "Adultspace XSS Worm". 364 characters of furniture remain at the end and that block is the ONLY place the date appears: Thursday, December 14th, 2006 at 3:04 pm. The name in the body is the worm's finder and the source of the sample, a reporter credit rather than this post's byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:70`

## http://ha.ckers.org/blog/20061215/csrf-with-word-part-ii/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - in the frontmatter, the body H1 AND the PDF metadata title. The document's own title, from its own bookmark heading, its permanent-link attribute and the page breadcrumb, is "CSRF with Word Part II" - note the year list's citation adds a word the document itself does not use, so prefer the document's own string. 631 characters of furniture remain at the end and that block is the ONLY place the date appears: Friday, December 15th, 2006 at 12:40 pm. TWO COMMENTS WERE DROPPED AND ONE IS THE STRONGEST ATTRIBUTION EVIDENCE IN ITS GROUP: an independent third-party trackback names the blog owner as the post's author AND restates the mechanism, and a reader adds a necessary step the post omits. The name in the body is the author of Part I, a prior-art credit rather than this post's byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:77`

## http://ha.ckers.org/blog/20061228/hacking-intranets-via-brute-force/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE 162-ROW EVIDENCE TABLE IS PUBLISHED AS ONE RUN-ON PARAGRAPH. The source wraps the scan results in a blockquote with each IP and hostname on its own line; the published file emits a bare quote marker, then 171 lines at one-space indent, and the renderer joins them all - the PDF reads "10.0.1.10 intranet.godaddy.com 10.1.119.43 intranet.dyndns.org 10.210.136.22 internal.iask.com ..." with the tab column separator gone. The finding IS the table. The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Hacking Intranets Via Brute Force"; the recorded string is the masthead plus an Archive breadcrumb. 583 characters of furniture remain at the end, and that block is the ONLY place the date appears: Thursday, December 28th, 2006 at 3:53 pm. A DROPPED READER COMMENT BY THE BLOG'S OWNER carries the verification of the post's central claim - he scripted the 162 hosts and lists seven that returned 401 Authorization Required, naming them (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:55`

## http://ha.ckers.org/blog/20070116/stealing-mouse-clicks-for-banner-fraud/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title carries the masthead and an Archive breadcrumb ahead of the document's own title, "Stealing Mouse Clicks for Banner Fraud". 149 characters of furniture remain at the end, and published is empty although the body states Tuesday, January 16th, 2007. THE DROPPED COMMENTS CARRY THE REFINEMENT THAT TURNS THIS INTO CLICKJACKING PROPER - hiding the frame and revealing it on mouse-down, and repainting the cursor so nothing appears beneath it - on a post that is one of the earliest statements of the technique (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:68`

## http://ha.ckers.org/blog/20070119/iframe-http-ping/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE TITLE IS THE MASTHEAD ALONE WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab", shared identically with two siblings; this document's own title is "Iframe HTTP Ping". The body also carries MOJIBAKE: a non-breaking space in the source was decoded as cp1252 and re-encoded, so the text reads "for this purpose.A Yet another way" with a stray capital A-circumflex - the only such occurrence in its group, which makes it a decoder slip rather than a source artefact. 604 characters of furniture remain at the end, and that block is the ONLY place the date appears: Friday, January 19th, 2007 at 11:47 am (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:66`

## http://ha.ckers.org/blog/20070209/image-names-gone-bad/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A DOUBLE DECODE DESTROYED THE THIRD VECTOR, AND TWO LIVE SCRIPT TAGS SWALLOW THE REST OF THE FILE. The source's third payload ends ...var%20a=(&#038;quot.jpg, where &#038;quot is the LITERAL TEXT &quot - an entity deliberately written without its semicolon, which is the whole point of the vector. The published file decodes it twice and prints a bare double quote, so the vector demonstrates nothing; the sibling payload on the next line survived only because &apos is absent from the legacy no-semicolon entity table, which is what proves this a decoder fault rather than a source artefact. Separately the first payload's escaped markup was decoded into TWO LIVE, UNCLOSED script elements at column 0, so in any Markdown-to-HTML render everything after them - the other two vectors, the explanation and the tail - is eaten as script text. All three vectors are published unfenced as bare paragraphs. The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Image Names Gone Bad"; here the masthead is concatenated on with no separator. 361 characters of furniture remain at the end, and that block is the ONLY place the date appears: Friday, February 9th, 2007 at 5:06 pm. A DROPPED READER COMMENT BY THE BLOG'S OWNER adds a fourth vector that exists nowhere else in the archive, a filename using high-bit characters to exploit the US-ASCII encoding issue (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:74`

## http://ha.ckers.org/blog/20070209/non-alpha-non-digit-3/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE XSS VECTOR - THE ENTIRE FINDING - WAS DECODED INTO A LIVE SCRIPT ELEMENT. The author escaped it so readers could see it; the published file emits it as real markup inside a blockquote, so a conforming reader sees nothing at all. The irony is recorded in the source's own comment thread, where the blog owner tells a commenter they must escape their angle brackets to make them visible. THE RECORDED TITLE IS THE MASTHEAD ALONE with the post title amputated; the document's own title is "Non-Alpha-Non-Digit 3". 149 characters of furniture remain at the end, and published is empty although the body states Friday, February 9th, 2007. Dropped comments carry a prior-art argument and a cross-browser variant of the vector (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:23`

## http://ha.ckers.org/blog/20070210/ie70-detector/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE TITLE IS THE MASTHEAD ALONE WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab", shared identically with two siblings in this collection; this document's own title, from its bookmark heading and the breadcrumb still in its body, is "IE7.0 Detector". 543 characters of furniture remain at the end, and that block is the ONLY place the date appears: Saturday, February 10th, 2007 at 8:42 pm. Six reader comments were dropped, including one correcting the post's own broken tool link. The body itself is sound (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:77`

## http://ha.ckers.org/blog/20070210/iso-8895-1-vulnerable-in-firefox-to-null-injection/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THIS DOCUMENT NOW PRESERVES NO TECHNIQUE AT ALL. The post's vector was only ever published as a SCREENSHOT, and that image recorded an empty response and is referenced against the dead original host rather than the Wayback replay this very capture came from, so the line "The code for this is very simple:" is followed by a broken image and nothing else. Note the image is 294x78, so a re-fetch alone may not be enough: the size heuristic refused a 400x62 image as furniture on a sibling in this same collection. The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "ISO-8895-1 Vulnerable in Firefox to Null Injection"; the recorded string is the masthead plus an Archive breadcrumb. 674 characters of furniture remain at the end, the largest share in its group, and that block is the ONLY place the date appears: Saturday, February 10th, 2007 at 9:21 pm. THE DROPPED COMMENTS CARRY THE MECHANISM: two readers establish that Firefox reads the page as UTF-16BE despite the ISO-8859-1 header, which is arguably the real explanation and appears nowhere in the archived text (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:20`

## http://ha.ckers.org/blog/20070216/embeding-svg-that-contains-xss-using-base64-encoding-in-firefox/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title carries the site masthead and an Archive breadcrumb ahead of the document's own title, "Embeding SVG That Contains XSS Using Base64 Encoding in Firefox" - the misspelling is the source's own and is correctly preserved. COVERAGE NOTE: the post never inlines its vector, linking instead to a cheat-sheet anchor that is now dead, so the payload was never in this document and the citation rests on prose describing it. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Friday, February 16th, 2007. The raw object is absent from the store and correctly recorded on store-gaps (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:79`

## http://ha.ckers.org/blog/20070309/firefox-header-redirection-javascript-execution/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title carries the site masthead and an Archive breadcrumb ahead of the document's own title, "Firefox Header Redirection JavaScript Execution", in frontmatter, body H1 and PDF metadata. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Friday, March 9th, 2007. A dropped reader comment supplies the prior art. The body itself is sound - note the post never writes a literal javascript: URI, so there was nothing here for the URI-rewriting fault to damage (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:80`

## http://ha.ckers.org/blog/20070319/detecting-default-browser-in-ie/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title carries the site masthead and an Archive breadcrumb ahead of the document's own title, "Detecting Default Browser in IE", in frontmatter, body H1 and PDF metadata. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Monday, March 19th, 2007. A DROPPED READER COMMENT EXPLAINS THE ACTUAL MECHANISM - that the browser is surfacing a registry class default mislabelled as a MIME type - which the post itself does not state (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:71`

## http://ha.ckers.org/blog/20070323/internet-archiver-port-scanner/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE QUOTED EMAIL LOST ITS QUOTING AND THE FTP TRANSCRIPT WAS JOINED INTO ONE PARAGRAPH. The source wraps the contributor's whole email in a blockquote; the published file emits a bare marker and puts everything after it at column 0, so a guest's email now reads as the blog owner's own words. The fourteen-line FTP log that is the evidence of HTTP being spoken at an FTP service is flattened - the PDF reads "(000033) 23/03/2007 12:40:33 - (not logged in) (208.70.29.90)> Connected, sending welcome message... (000033) 23/03/2007 12:40:33 ..." as continuous prose. The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Internet Archiver Port Scanner"; here the masthead is concatenated on with no separator. 302 characters of furniture remain at the end, and that block is the ONLY place the date appears: Friday, March 23rd, 2007 at 9:15 am (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:56`

## http://ha.ckers.org/blog/20070325/ie-sends-local-addresses-in-referer-header/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ONE LINE THAT IS THE TECHNIQUE RENDERS AS NOTHING. The source escapes the XML data island so a reader can see it; the published file decodes it into live markup, and because an xml element is unknown and has no text content, a Markdown-to-HTML render emits it and the browser shows an empty space where the payload should be. The archive's own PDF path escapes instead, so the fault is invisible to a PDF check and bites only the Markdown the website serves. The Apache log sample is also unfenced. The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "IE Sends Local Addresses in Referer Header"; the recorded string is the masthead plus an Archive breadcrumb. 596 characters of furniture remain at the end, and that block is the ONLY place the date appears: Sunday, March 25th, 2007 at 9:09 pm. Note the only occurrence of the site owner's handle inside the article is a Windows PATH FRAGMENT in a log sample, not a byline (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:75`

## http://ha.ckers.org/blog/20070403/localrodeo-detection/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title concatenates the site masthead onto the document's own title with no separator, where the post's own heading is "LocalRodeo Detection". 126 characters of furniture remain at the end, and published is empty although the body states Tuesday, April 3rd, 2007. THE DROPPED COMMENTS CARRY TECHNIQUE THE POST DOES NOT: fourteen responses in which the blog owner posts two working BYPASSES of the tool this post is about, prompted by another researcher, and the tool's own author replies (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:73`

## http://ha.ckers.org/blog/20070411/intra-protocol-exploitation/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - and the same string is carried by the frontmatter, the body H1 and the PDF metadata title. Four other documents in this collection are published under that identical meaningless string. The archive proved this is ITS fault, not the source's: the stored title element has the same shape as siblings whose topic survived, so the same input produced both outcomes. This document's own title, from its bookmark heading and its breadcrumb, is "Inter Protocol Exploitation" - and here the proof is exact, because the stored raw's title element reads "ha.ckers.org web application security lab - Archive > Inter Protocol Exploitation", structurally identical to a sibling that KEPT its topic. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Wednesday, April 11th, 2007. A DROPPED READER COMMENT CARRIES TECHNIQUE: it gives the precise gopher three-part form that opens a connection to a chosen host and port and sends a chosen string, which the post itself does not state. Note the publisher's own URL misspells the topic word, which is not a capture fault (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:70`

## http://ha.ckers.org/blog/20070421/noisy-decloaking-methods/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - and the same string is carried by the frontmatter, the body H1 and the PDF metadata title. Four other documents in this collection are published under that identical meaningless string. The archive proved this is ITS fault, not the source's: the stored title element has the same shape as siblings whose topic survived, so the same input produced both outcomes. This document's own title, from its bookmark heading and its breadcrumb, is "Noisy Decloaking Methods". 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Saturday, April 21st, 2007. Dropped reader comments generalise the technique to any installed custom protocol handler and propose a further scheme (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:57`

## http://ha.ckers.org/blog/20070516/read-firefox-settings-poc/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PROOF OF CONCEPT IS THE DOCUMENT AND IT WAS DECODED INTO LIVE MARKUP. This post is graded records at 201 words; the listing is what it carries. The source escapes the whole script so a reader can see it, and the published file decodes it - three script elements now live - and the bold tags the proof of concept exists to PRINT became live formatting instead of visible text. The blockquote was emptied at the same time: a bare marker on its own line with the content pushed outside it. With no fence and only one-to-three-space indent, the seven-line listing then renders as ONE run-on line. THE RECORDED TITLE IS THE MASTHEAD ALONE with the post title amputated; the document's own title is "Read Firefox Settings (PoC)". 149 characters of furniture remain at the end. THE DROPPED COMMENT THREAD REFUTES THE FINDING: 32 responses in which two Mozilla developers establish that it reads only shipped defaults rather than user settings, and the blog owner concedes and withdraws it - so the archived document alone materially OVERSTATES what this reference supports. The same thread adds three working extensions. published is empty although the body states Wednesday, May 16th, 2007 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:67`

## http://ha.ckers.org/blog/20070518/enumerate-windows-users-in-js/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE MASTHEAD ALONE, WITH NO POST TITLE IN IT - "ha.ckers.org web application security lab" - and the same string is carried by the frontmatter, the body H1 and the PDF metadata title. Four other documents in this collection are published under that identical meaningless string. The archive proved this is ITS fault, not the source's: the stored title element has the same shape as siblings whose topic survived, so the same input produced both outcomes. This document's own title, from its bookmark heading and its breadcrumb, is "Enumerate Windows Users In JS". THE DROPPED COMMENT THREAD IS THE COSTLIEST LOSS OF ITS GROUP: the technique's own author returns with the extension that makes it useful - checking one profile file rather than another to tell whether the account is CURRENTLY LOGGED IN, and a UNC form that tests for local administrator - and none of it appears in the archived text. A reader's comment also carried a payload with an event handler, which vanished with the block rather than by handler-stripping. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Friday, May 18th, 2007. Note the slug encodes the 2008 concatenated title while the title field encodes the 2007 breadcrumb, so the two were written from different captures (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:64`

## http://ha.ckers.org/blog/20070604/passing-malicious-php-through-getimagesize/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE TWO SCREENSHOTS THAT ARE THE EVIDENCE ARE PUBLISHED AS PLAIN TEXT LINKS TO A DEAD HOST. The hex-editor images showing the crafted GIF are not rendered as images at all, were never resolved against the Wayback replay, and are not even listed in the images inventory - the only two entries there are the site's advertisement and masthead, so the furniture was attempted and the evidence was not. A blockquote is also broken: only its first line is quoted, so five paragraphs of a contributor's email read as the blog owner's own words. The recorded title carries the masthead and an Archive breadcrumb ahead of the document's own title, "Passing Malicious PHP Through getimagesize()". 149 characters of furniture remain at the end, and published is empty although the body states Monday, June 4th, 2007. The raw object is absent from the store, correctly recorded on store-gaps (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:62`

## http://ha.ckers.org/blog/20070606/additional-image-bypass-on-windows/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A QUOTED CONTRIBUTOR'S RESEARCH IS REATTRIBUTED TO THE BLOG OWNER. The source wraps the contributor's whole six-paragraph translation in one blockquote; the published file marks only the first paragraph, so paragraphs two to six sit at column 0 and read as the blog owner's own words - and the line that genuinely IS his is indistinguishable from them. The recorded title also concatenates the masthead onto the document's own title with NO separator, in frontmatter, body H1 and PDF metadata, where the post's own title is "Additional Image Bypass on Windows". A literal underscore pair in the source was read as emphasis, so the underscores the author typed are gone. 126 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Wednesday, June 6th, 2007. Dropped comments carry the source-disclosure precedent this builds on and the mitigation (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:60`

## http://ha.ckers.org/blog/20070608/cross-domain-basic-auth-phishing-tactics/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE POST'S ONLY SUPPORTING SCREENSHOT IS UNCAPTURED AND UNLISTED. The visual proof of the IDN claim - one of the two bugs the citation exists for - lives in a reader comment on the contributor's own server, and the comment block was dropped, so the image is absent from the archive and absent from the images inventory. The quoted five-paragraph email also lost its quoting after the first paragraph, so a reader cannot tell where the contributor's report ends and the blog owner's commentary begins, and the IDN finding falls on the wrong side of that line. A parenthetical in the prose was read as emphasis and lost its asterisks. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Friday, June 8th, 2007. Note this is the one document in its group where the blog owner does not appear even in the comments, so no byline exists anywhere in the source (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:59`

## http://ha.ckers.org/blog/20070620/code-execution-through-filenames-in-uploads/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE HTML AND PDF RENDERINGS NOW DISAGREE ABOUT WHAT THE SOURCE CODE SAYS. The Perl listing's filehandle read is escaped in the source; the published file decodes it into live markup, and because it is a well-formed inline tag CommonMark passes it through, so a browser swallows it as an unknown element and the reader sees "print while ();" - the filehandle read that is the whole point of the listing is GONE from the HTML while the PDF path escapes it and keeps it. Separately both blockquotes enclose whole listings in the source but only their first line is quoted in the published file, and 26 lines sit at one-to-three-space indent with zero fences in the file, so the renderer joins the entire Perl script and the entire ls -al output into running prose, destroying the column alignment that IS the evidence. 149 characters of furniture remain at the end; the posted-on line above it is the ONLY place the date appears: Wednesday, June 20th, 2007. THE DROPPED COMMENTS ARE UNUSUALLY LOAD-BEARING - a reader supplies a working variant, and the thread then documents the PUBLISHER'S OWN mangling of angle brackets, which is direct evidence that some bracket loss here is source-inherent while the decode above is ours. Note the en-dashes in the directory listing are WordPress texturisation and source-inherent (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:58`

## http://ha.ckers.org/blog/20070623/hiding-js-in-valid-images/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE COMMENT MARKERS THAT ARE THE TRICK ARE DELETED FROM THE PDF, AND A LIVE UNCLOSED SCRIPT TAG SWALLOWS THE REST. This is the number-five technique of 2007. The Markdown correctly holds the JavaScript long-comment delimiters, but the renderer consumes their asterisks as emphasis, so the published PDF reads "long comment / and /" - the two markers that make a GIF parse as JavaScript are gone from what a reader sees. Separately two escaped payloads were decoded into live markup: a PHP processing instruction at column 0, which a browser swallows as a bogus comment so the payload is INVISIBLE, and an inline script element with no closing tag, which consumes the closing paragraph and the tail. A blockquote is also broken, so the quoted email from the contributor now reads as the blog owner's own words. The title is correct here - the one correct title in its group. 564 characters of furniture remain at the end, and that block is the ONLY place the date appears: Saturday, June 23rd, 2007 at 12:36 am. Thirty-three reader comments were dropped, several technical (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:9`

## http://ha.ckers.org/blog/20070827/paper-on-hacking-intranets-using-websites-not-web-browsers/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Paper on Hacking Intranets Using Websites (Not Web Browsers)"; the recorded string is the masthead plus an Archive breadcrumb. COVERAGE GAP: this is a genuine pointer page and the paper it points at is not archived anywhere in the manifest, so the archive holds the announcement and not the research. 629 characters of furniture remain at the end, and that block is the ONLY place the date appears: Monday, August 27th, 2007 at 2:48 pm. The raw object is absent from the store and correctly recorded on store-gaps (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:48`

## http://ha.ckers.org/blog/20071209/initiatin-probes-against-servers-via-other-servers/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Initiating Probes Against Servers Via Other Servers"; the recorded string is the masthead plus an Archive breadcrumb. 614 characters of furniture remain at the end, and that block is the ONLY place the date appears: Sunday, December 9th, 2007 at 3:41 pm. The raw object is absent from the store and correctly recorded on store-gaps. Note the publisher's own URL misspells "initiating", and the year list cites it with the same misspelling, so that is not a capture fault (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:46`

## http://ha.ckers.org/blog/20080227/res-timing-file-enumeration-without-javascript-in-ie70/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title takes the WHOLE breadcrumb - "ha.ckers.org web application security lab - Archive > Res Timing File Enumeration Without JavaScript in IE7.0" - where the document's own title is the last part alone. 149 characters of comment-form furniture remain at the end, and published is empty although the body states Wednesday, February 27th, 2008. Both published images are dead site chrome. Note the raw object is absent from the store, and that the names in the body, David Byrne for the res:// timing trick and Jeremiah Grossman for META-refresh blocking, are PRIOR-ART credits and must not be taken as this post's byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:28`

## http://ha.ckers.org/blog/20080520/http-proxies-bypass-firewalls/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE POST'S ONE FIGURE IS LOST AND THE PROSE POINTS AT IT. The body ends a paragraph with "Here's a diagram of what I'm talking about:" and the diagram, the inward-pointing-proxy illustration, recorded an empty response; it and the two chrome images resolve against the DEAD host rather than the Wayback replay the text came from, which was the recovery route not taken. The recorded title is the site masthead alone, with no topic in it, in frontmatter, body H1 and the PDF title, where the post's own heading is "HTTP Proxies Bypass Firewalls". 149 characters of comment-form furniture remain at the end, and published is empty although the body states Tuesday, May 20th, 2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:35`

## http://ha.ckers.org/blog/20090120/persistent-cookies-and-dns-rebinding-redux/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE PAYLOAD PUBLISHES AS A LIVE SCRIPT ELEMENT. The Set-Cookie example carrying an XSS payload was unescaped and left unfenced inside a blockquote, so on a pass-through renderer the element renders as nothing and the reader loses the payload. The recorded title also runs the site masthead onto the document own title, and 362 characters of publisher furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:14`

## http://ha.ckers.org/blog/20090608/rfc1918-blues/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title carries the site masthead in both frontmatter and body H1, where the document's own heading is "RFC1918 Blues". 3,032 characters of tail - 43 percent of the file - are furniture, and they include ONE reader comment of about 2,000 characters retained out of the seven the source carries, so a single commenter's words are published as if they were part of the article. Both published figures are the page's advertising banners (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:19`

## http://ha.ckers.org/blog/20090617/slowloris-http-dos/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title in both frontmatter and body H1, where the post heading is just "Slowloris HTTP DoS". Publisher furniture remains at the end, 304 characters of posted-on line, a discuss link and comment-form field labels. Both images are dead ha.ckers.org chrome - an advertising banner and the site logo - 0 of 2 preserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:9`

## http://ha.ckers.org/blog/20090630/csrf-and-ignoring-basicdigest-auth/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ONE LINE THAT IS THE ENTIRE FINDING RENDERS AS A LIVE ELEMENT. The stored source escapes the div whose CSS background-image points at the router URL; the published file decodes it, unfenced, at the start of a blockquote line - which CommonMark treats as an HTML block - so the reader sees only the word blah, and the rendered page itself fires a request to the router path. The recorded title also runs the site masthead onto the document's own title in both frontmatter and body H1, where the post heading is just "CSRF And Ignoring Basic/Digest Auth", and 303 characters of publisher furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:20`

## http://ha.ckers.org/blog/20090713/hash-information-disclosure-via-collisions-the-hard-way/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title glues the site masthead onto the document's own title in the frontmatter, the body H1 and the SLUG, where the post's own heading - present verbatim further down the same file - is "Hash Information Disclosure Via Collisions - The Hard Way". 301 characters of publisher furniture remain at the end, a posted-on line, a discuss link and comment-form field labels, and that discarded block is the ONLY place the publication date appears while published is empty. Head chrome publishes an advertising banner, the literal words Paid Advertising and the site logo before the article; both images are that dead chrome, 0 of 2 preserved. Note the raw object is genuinely gone from the store and correctly recorded on store-gaps, so the source-versus-published comparison cannot be run for this entry (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:21`

## http://ha.ckers.org/blog/20090720/xmlhttpreqest-ping-sweeping-in-firefox-35/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the source has NO title element at all, so the recorded title was assembled by gluing the site masthead - taken from the logo image's alt text - onto the document's own heading, in both frontmatter and body H1. The document's own title is the single h2. Retained head chrome publishes an advertising banner, the literal words "Paid Advertising" and the site logo before the article, and 358 characters of publisher furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:23`

## http://ha.ckers.org/blog/20090727/quicky-firefox-dos/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document own title in the frontmatter, the body H1 and the slug, where the post heading is just "Quicky Firefox DoS". The listing is published as a blockquote whose continuation lines are indented one space, below the four Markdown needs, so the four lines collapse into one wrapped paragraph and the author line breaks are gone. Two retained chrome images sit at the TOP of the body - an advertising banner followed by the literal words "Paid Advertising", and the site logo - both dead and unpreserved. Publisher furniture remains at the end, 301 characters ending in comment-form field labels. The pointer-page grade is also inflated by those retained chrome links: the page carries the technique in full (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:25`

## http://ha.ckers.org/blog/20090809/smbenum/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title glues the site masthead onto the document's own title in both frontmatter and body H1 - the page title element reads "SMBEnum ha.ckers.org web application security lab" with no separator, where the post's own bookmark heading is just "SMBEnum", which is also what cited_title says. 303 characters of publisher furniture remain at the end, a posted-on line, a discuss link and bare comment-form labels, and that block is the only place the publication date appears while published is empty. Head chrome publishes an advertising banner, the literal words Paid Advertising and the site logo before the article; both images are that dead chrome, 0 of 2 preserved. The manifest raw_sha256 is absent from the store, though an equivalent capture was located there by content search (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:27`

## http://ha.ckers.org/blog/20090810/de-cloaking-in-ie70-via-windows-variables/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title in both frontmatter and body H1, where the post heading is "De-cloaking in IE7.0 Via Windows Variables". A blockquote is broken - a bare marker, a blank line, then the proof-of-concept URL at column 0 - and both published figures are the page's advertising banners, recorded as empty responses. 303 characters of publisher furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:30`

## http://ha.ckers.org/blog/20090811/smb-decloaking/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAYLOAD PUBLISHES AS A LIVE ELEMENT - the stored source escapes the iframe pointing at a file UNC path so a reader can see it, and the published file decodes it, unfenced, inside a blockquote, so it renders as nothing. This entry is the one whose source survives and therefore PROVES the class for its siblings: the title tag really does concatenate the masthead with no separator, which is how every ha.ckers title in this archive acquired it, and the document own heading is just "SMB Decloaking". 305 characters of publisher furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:29`

## http://ha.ckers.org/blog/20090819/itms-decloaking/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE POST'S SINGLE TECHNICAL ARTEFACT IS INVISIBLE. The stored source escapes the iframe pointing at itms:www.apple.com:80@ha.ckers.org so a reader can see it; the published file decodes it into live markup, so it renders as an empty iframe and the technique disappears from the page. The closing tag is present, so nothing downstream is swallowed. The recorded title also runs the site masthead onto the document's own title in both frontmatter and body H1, where the post heading is just "itms Decloaking", and 306 characters of publisher furniture remain at the end. Both images are dead ha.ckers.org chrome, 0 of 2 preserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:31`

## http://ha.ckers.org/blog/20091116/session-fixation-via-dns-rebinding/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title in both frontmatter and body H1, where the post heading is "Session Fixation Via DNS Rebinding". Retained head chrome publishes the advertising banner and site logo before the article, and 305 characters of publisher furniture remain at the end. Note a metadata inconsistency: the manifest records two cited_by lines while the published frontmatter lists only one (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:14`, `2009.md:24`

## http://ha.ckers.org/blog/20091117/dns-rebinding-for-credential-brute-force/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document own title in frontmatter, body H1 and slug, where the post heading is "DNS Rebinding for Credential Brute Force". 306 characters of publisher furniture remain at the end, head chrome publishes the advertising banner and site logo, and both images are dead and unpreserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:26`

## http://ha.ckers.org/blog/20091118/dns-rebinding-for-scraping-and-spamming/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document own title in the frontmatter, the body H1 and the slug, where the post heading is "DNS Rebinding for Scraping and Spamming" - which is what the year list cites. 477 characters of publisher furniture remain at the end, and head chrome publishes an advertising banner and the site logo before the article. Both images are dead and unpreserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:14`, `2009.md:28`

## http://ha.ckers.org/blog/20091228/popup-focus-url-hijacking/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH REAL FIGURES ARE BROKEN and they are load-bearing - the sentence ends "resulting in the following popup:", the image follows, then "Click to Enlarge". Both the Firefox and Internet Explorer download-dialog screenshots recorded an empty response because they were resolved against the dead live host. The other two images in the file are the page's advertising banners. The recorded title also runs the site masthead onto the document's own title, and 306 characters of furniture remain at the end. The pointer-page downgrade counted links from those retained ad banners and the comment form, on a post that carries a demo and two original screenshots (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:80`, `2010.md:31`

## http://ha.ckers.org/blog/20100125/css-history-hack-in-firefox-without-javascript-for-intranet-portscanning/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the document's own heading with the FOOTER MASTHEAD spliced on - the source has no title element at all, so the site's copyright line became part of the title, in both frontmatter and body H1. Publisher furniture remains at the end (305 characters of post-meta, a discuss link and comment-form labels) and ad chrome at the head. Both images are unpreserved and point at a dead host (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:13`

## http://ha.ckers.org/blog/20100614/turning-xss-into-clickjacking/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the blog masthead appended to the document's own title, in both frontmatter and body H1, where the permalink heading gives just "Turning XSS into Clickjacking". An escaped script tag was unescaped into live markup twice, leaving four live script tags in the body with nothing fenced. Both published figures are site chrome - an advertising banner and the site logo - and the manifest records both as empty response with no local copy, so both links are dead. Publisher furniture remains at the end: post-meta, a discuss link and comment-form labels, 302 characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:30`

## http://ha.ckers.org/blog/20100621/side-channel-attacks-in-ssl/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title with no separator, in both frontmatter and body H1, where the post's own heading is just 'Side Channel Attacks in SSL'. Head chrome is retained - masthead line, an advertising banner, the literal words 'Paid Advertising' and the site logo - and publisher furniture remains at the end, 301 characters of post-meta, a discuss link and comment-form labels. Both images are site chrome, unpreserved and dead. The manifest raw_sha256 is absent from the store, so do not re-render (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:29`

## http://ha.ckers.org/blog/20100622/improving-https-side-channel-attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title with no separator, in both frontmatter and body H1, where the post's own heading is 'Improving HTTPS Side Channel Attacks' - which is also what cited_title says. Publisher furniture remains at the end, 302 characters of post-meta plus a comment form, and that discarded block is the ONLY place the publication date appears while published is empty. Both images are dead-host site chrome, 0 of 2 preserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:28`

## http://ha.ckers.org/blog/20100718/flash-camera-and-mic-remember-funtion-and-xss/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title runs the site masthead onto the document's own title in both frontmatter and body H1, where the post's own bookmark heading gives 'Flash Camera and Mic Remember Function and XSS'. Publisher furniture remains at the end, 358 characters; the source's seven reader comments were correctly dropped but the comment-form labels were not. Both images are dead-host site chrome, 0 of 2 preserved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:27`

## http://ha.ckers.org/blog/20100820/quick-proxy-detection/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the recorded title concatenates the blog masthead onto the document's own title with no separator, in the frontmatter, the body H1 and the slug. Head chrome remains in the body - a paid advertising image, the literal words "Paid Advertising", the site logo and a posts-remaining widget - and the extraction note records that the raw candidate was chosen for keeping the most text, which is that chrome. Publisher furniture remains at the end including a comment form, 304 characters, and both images are dead and unarchived. Note the body cannot be fully verified: the raw bytes are gone from the store (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:26`

## http://ha.ckers.org/blog/20100901/xshm-mark-2/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the escaped proof of concept was decoded and published UNFENCED, so a real inline script element now sits in the Markdown. The recorded title also runs the site masthead onto the document's own title, in both frontmatter and body H1, where the post's heading is just 'XSHM Mark 2'. Worth noting on the grading: the rule:pointer-page decision counts 9 links, but the ARTICLE has only 2 - the rest come from the retained head and tail chrome, the advertising banner, logo, category tag, forums link and comment form. Trimming the furniture would likely flip this entry back from records to research, since it carries an original technique with a working proof of concept (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:23`

## http://i8jesus.com/?p=10

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWO LISTINGS COLLAPSE AND THE RECORDED REASON MISDESCRIBES THE EVIDENCE. The CSS listing and the eBay base-href listing are each wrapped in a MULTI-LINE SINGLE-BACKTICK span rather than a fence, so each renders as one continuous line and the author's line structure is gone; the second also carries a stray backtick artefact that runs two examples together. The decision.reason states "the 2008-era captures declare no author, so the byline is stated here" - the stored capture DOES declare it, with a rel=author element naming arshan dabirsiaghi - and the same reason says "A 2008 Wayback capture recovered the cited article" while the recorded snapshot is from 2011. The credit itself is right. published is empty although the source states January 5, 2008. THIRTEEN READER COMMENTS CARRYING TECHNIQUE WERE DROPPED, including the author explaining where the vectors came from and naming the technique - "I'd always thought of clobbering as a JS thing, but what I'm doing is the same thing... I'll call it clobbering in the future" - and an exchange settling the prior art, none of which the article itself states (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:38`

## http://i8jesus.com/?p=13

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE NESTED LIST WAS FLATTENED, so the loop body cannot be told from the outer steps. The source nests four steps inside "Client then starts a loop that iterates 16/2 times"; the published file puts all four at the same bullet level as the outer steps, and in the PDF every step is indented identically - the loop IS the mechanism. The decision.reason carries the same two misstatements as its sibling: it claims the captures declare no author when the stored capture carries a rel=author element naming arshan dabirsiaghi, and calls it a 2008 Wayback capture when the recorded snapshot is from 2012. published is empty although the source states January 15, 2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:34`

## http://i8jesus.com/?p=48

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ELEVEN ESCAPED TAGS WERE DECODED INTO LIVE MARKUP AND ONE OF THEM SWALLOWS THE END OF THE FILE. The source escapes 21 angle brackets and the published file has none; eight of the resulting tags sit safely inside code spans but eleven go live in bare prose, including the form, b and i self-closing tags that ARE the finding - so the reader sees nothing where the vector should be. The last of them is a script element on the closing line, and because HTML parsers do not honour a self-closing script, it opens an element that consumes the article's final words. Separately the eight-line nested-form listing is wrapped in a single-backtick INLINE code span rather than a fence, so it renders as one long run-on inside a paragraph. The byline and date were dropped at extraction although the stored capture carries a rel=author element naming arshan dabirsiaghi and a postDate of May 6, 2009 - the credit was later stated by hand and is right, but the recorded reason claims the 2009-era captures declare no author, which the stored capture disproves (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:59`

## http://i8jesus.com/?p=75

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAYLOAD URIS WERE REWRITTEN TO # IN THE THREE LINES THAT ARE THE FINDING. The stored source carries javascript: seven times and the published file four; the three lost are exactly the quoted-attribute forms the article's escalation turns on, so RCPT TO: img src='javascript:alert(1)' publishes as src='#' three times over and the quoted-to-unquoted argument is unreadable. Every one of the eleven console transcripts is a MULTI-LINE SINGLE-BACKTICK code span rather than a fence, and a code span collapses its line endings to spaces, so the 35-line Cyrus and Postfix session renders as one unwrapped line, as do the FTP and netcat sessions. The byline and date were dropped at extraction although the stored capture carries a rel=author element naming arshan dabirsiaghi and a postDate of August 30, 2009 - note the decision reason claims the 2009-era captures declare no author, which the stored capture disproves. The one figure points at the now-parked host (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:58`

## http://ilia.ws/archives/145-Network-Scanning-with-HTTP-without-JavaScript.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE TWO TAGS THAT ARE THE TECHNIQUE SHOW THE READER NOTHING, AND OPENING THE DOCUMENT FIRES A REQUEST TO THE ATTACKER HOST IN THE EXAMPLE. The scanning stylesheet link and the beacon image were decoded into live markup outside any fence, so they emit as real elements: visible text drops from 5,016 characters to 3,107, thirty-eight percent invisible, and the prose reference to the tag disappears twice as well. The byline and date cannot be recovered from this capture - the migrated page states neither, which makes the empty date CORRECT here rather than a miss; a Wayback capture of the original URL would carry the platform's date line. CLEARED as source-inherent: one mangled function name is the publisher's own markup, faithfully reproduced (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:54`

## http://jeremiahgrossman.blogspot.com/2006/01/advanced-web-attack-techniques-using.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: AN ENTITY WAS DECODED TWICE INSIDE THE RESEARCHER'S OWN CODE. The publishing platform had already re-escaped the logical-and operator in the proof of concept, and the extractor decoded it one level further, so the published listing shows a different number of ampersand entities than a single correct decode would give - and because the listing is UNFENCED, the website reader decodes it once more, so the operator the reader needs is never visible. The whole JavaScript proof of concept and the array constant are unfenced, so the paragraph rule joins them into run-on prose. The byline and the date were both dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a machine-readable datePublished timestamp. DIAGNOSTIC WORTH KEEPING: a sibling in this same collection used the RAW extraction candidate rather than precision and STILL lost the byline, so the loss is the converter dropping this platform's post-author block, not only the precision candidate cutting the footer - which is why all six entries from this blog lost their dates. Cleared for the record: the asterisked pseudo-tags in this post are the AUTHOR'S OWN de-fanging convention, verbatim in the source, not dropped tags (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:67`

## http://kyleosborn.org/2011/10/09/the-hidden-xss-attacking-the-desktop-mobile-platforms-slides-video/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH EMBEDS WERE LOST and the document is now an announcement stub. The body promises "Slides are below, and video is after the break" and the stored source holds a SlideShare embed and a YouTube embed at that point; neither reached the document, and the entire technique lives in them. The byline was also dropped although it is PUBLISHED IN THE BODY as "Posted by Kos" and the source marks it rel=author. The recorded title carries a leading guillemet from the WordPress breadcrumb and reaches the body H1 too; the document's own heading ends "- Slides & Video" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:29`

## http://lab.gnucitizen.org/projects/cups-0day

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE CARRIES THE SITE NAME AND TWO INVISIBLE CHARACTERS. It reads "CUPS 0day (Information Security Laboratory)" with a U+200E LEFT-TO-RIGHT MARK before each parenthesis, repeated in the H1 and the byline line, where the document's own title is "CUPS 0day". Four emphasis runs are broken and reader-visible in the PDF - "does *not *need to be logged in", "even if you are *not *logged into", "is a* separate issue*" and a stray trailing pair of asterisks after a link. The proof of concept the page links, cups_dos_poc.html.txt, was never archived; the attachment widget that names it is the only pointer to it and also carries the byline and date, so it should be kept rather than cut as furniture (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:53`

## http://lists.webappsec.org/pipermail/websecurity_lists.webappsec.org/2011-February/007533.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the ENTIRE PROSE ARTICLE is wrapped in a code fence, and the extractor emitted seven Markdown links inside it, so they will display raw as bracket-and-parenthesis text. The byline was dropped although the body names the writer, Phillip Purviance, and the source marks it in bold. Mailman navigation is also duplicated at the end, 991 characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:14`

## http://m-austin.com/blog/?p=19

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWO OF THE FOUR CODE BLOCKS ARE GONE and a third is an empty fence. The stored source has four pre blocks; the published file has two fences, one containing nothing. Block 1 - the PHP CORS headers plus the div whose onreplace fires and the img whose onerror alerts - is missing entirely. Block 2 consisted only of an onerror handler appending a remote script, so deleting the handler left an EMPTY fence, and the preceding line still promises "Now we can load a remote script to do the work for us:". Block 4, the 1,066-character document.domain and iframe POST sequence, is missing entirely. The source article region has 5 event handlers and the published file has ZERO. An escaped script tag was also unescaped into live markup, and the only figure is unarchived on a host the manifest records as dead (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:32`

## http://netifera.com/research

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THIS IS THE DOCUMENT FOR THE NUMBER 3 TECHNIQUE OF 2009 AND IT IS THE WRONG PAGE. The year list cites Flickr's API Signature Forgery Vulnerability, an MD5 extension attack; the capture is the publisher's research INDEX page taken from a 2012 snapshot, three years later, leading with CRIME and the ASP.NET padding oracle and reducing the cited work to a final section. The words md5 and extension occur ZERO times in the stored source - the page never uses the citation's own vocabulary. The actual advisory, flickr_api_signature_forgery.pdf, is linked from inside the capture but is not archived and linked_document_url is unset. The recorded title is that index page's title tag, and all 17 images resolve against the dead original host rather than the working Wayback replay path, so every one recorded an empty response (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:7`, `2010.md:5`

## http://news.netcraft.com/archives/2006/06/16/paypal_security_flaw_allows_identity_theft.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE'S TWO REAL FIGURES ARE UNPRESERVED AND THE IMAGES MAP HOLDS SOMETHING ELSE ENTIRELY. The captions point at an SSL-certificate screenshot and an injected-content screenshot - the visual evidence of the whole story - and neither is in the images map; the map instead holds twelve assets from the publisher's PRESENT-DAY marketing site, left over from a superseded capture. The images step predates the recapture by four days and was never pruned, unlike a sibling in this same collection which did get pruned. Both figure targets point at a host that now redirects to a marketing page rather than the Wayback replay, so neither will load. 4,725 characters - 45 percent of the file - are sitewide furniture at the end. The byline and date are both missing although the published body itself carries a posted-by line naming Paul Mutton with the date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:68`

## http://news.netcraft.com/archives/2006/09/23/hostgator_cpanel_security_hole_exploited_in_mass_hack.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE IMAGES MAP IS STALE AND WRONG - it holds twelve assets from the publisher's present-day marketing site, left over from a superseded capture, because the images step predates the recapture and was never pruned. The article has no figure of its own, so nothing essential is lost, but the record is false and the two remaining image targets point at a dead host rather than the Wayback replay. 4,251 characters - 45 percent of the file - are sitewide furniture at the end. The byline and date are both missing although the published body itself carries a posted-by line naming Rich Miller with the date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:62`

## http://paranoia.dubfire.net/2007/05/remote-vulnerability-in-firefox.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: NINE SECTION HEADINGS WERE FLATTENED INTO PLAIN PARAGRAPHS, so a 2,460-word advisory - executive summary, FAQ, description, when users are vulnerable, how to fix it, why vendor extensions are affected, code signing, vendor notification and the conflict-of-interest statement - has no navigable structure at all. The source marked them with inline styling rather than heading tags. The byline is empty although THE DOCUMENT NAMES ITS OWN AUTHOR IN ITS OWN WORDS in the self-disclosure section, twice; the platform's byline element is empty, which is why a marker-based sweep missed it. published is empty although the source states a 2007-05-30 timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:21`

## http://pauldotcom.com/2011/05/stealth-cookie-stealing-new-xs.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is pure SITE CHROME - "PaulDotCom: Archives", the Movable Type archive-template title - in both the frontmatter and the body H1, while the captured page carries the document own title explicitly: "Stealth Cookie Stealing (new XSS technique)". The SLUG is built from a failed live probe that hit a browser error page (chrome-error://chromewebdata/), so it will change when the title is corrected. The byline was dropped although the source carries a full vcard, "By John Strand", and the body first line credits the technique to LanMaSteR53; the publication date was dropped from the same block. The single figure is unretrievable, 0 of 1 preserved, 0 bytes, and the Markdown still points at it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:45`

## http://polyboy.net/docs/2011_DIMVA_Flash_crossdomain_proxies.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: all five code listings are mangled by the extraction path. The publish used pdfminer, which injects spaces into every token, so a script tag publishes as "< script src = \" http :// b . net / somescript . js \" / >" and a PHP header call as "header ( \" Content - type : application /x - shockwave - flash \" );". pdftotext on the SAME stored bytes renders all five correctly, so this is repairable offline by re-extracting through the other path. The recorded title is also the citation's wording, which occurs ZERO times in the paper - the document's own title is "Biting the hand that serves you: A closer look at client-side Flash proxies for cross-domain requests". Six figures additionally publish as garbage tokens, but that is source-side: the figure fonts carry no usable ToUnicode (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:9`

## http://powerofcommunity.net/poc2008/hasegawa.pptx

- Outcome: `broken-capture`
- Kind: slides
- Reason: faulty capture: THE RENDERER DELETES FOUR CLOSING ANGLE BRACKETS, ONE OF THEM INSIDE THE PAYLOAD. Four body lines consist of a bare > character, which the renderer treats as an empty blockquote, so the PDF loses the > that closes <script[0x0B]> and the > that closes the <input ... onmouseover=alert(1)> on slide 40, plus the > rows of the escaping table on slide 48 and the bit-stripping table on slide 64. The published MARKDOWN is correct and every UTF-7 payload survives byte for byte - this is a renderer fault, fixable offline without re-acquiring. Separately the deck's 21 embedded images across 12 slides were never extracted and there is no images step at all, so slide 65 publishes as the caption "Interpreting 7-bit encoding" with nothing under it - the two screenshots WERE the slide - and slides 22, 76 and 80 are captions without their evidence. The recorded title names a part as the whole: it is the citation's title, and JSON hijacking is slides 57 to 62 of 86, where the deck's own title slide reads "Attacking with Character Encoding for Profit and Fun". publisher and published are empty although slide 2 states NetAgent Co., Ltd and slide 1 states POC2008, and language is empty on a bilingual Japanese deck. All 86 "Speaker notes" lines are the master's page-number placeholder rather than authored notes, and a reader will read them as notes. ITS TRANSLATION ALSO LOSES CONTENT: slide 30 drops about 17 lines including the item "Conclusion" and the author's entire English agenda block, and slide 55 is the one place the author's OWN English was silently rewritten rather than copied (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:74`

## http://riosec.com/how-to-create-a-gifar

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: AN RSS AGGREGATOR SIDEBAR IS PUBLISHED AHEAD OF THE ARTICLE. Only two candidates were produced and NO precision candidate at all, so the chooser had nothing correct to pick; the result is that the first fifty lines of the body are the site's "Interesting Reading", SANS ISC, Linux Security and SecurityFocus feeds - unrelated 2008 news links - and the article itself is the nine lines after them, one paragraph. The article's only demonstration was dropped: "The folowing video demonstrates this technique." is followed by nothing, where the source carries a YouTube embed, and the video id 5TgfKKX7uSQ is recorded nowhere in the file. A dropped comment also carries a reproduction attempt with server logs showing the applet 404ing on a .gif extension across Firefox, IE and Opera. The byline was dropped although THE PUBLISHED BODY ITSELF reads "Posted 2008-08-12 17:05 by Christopher", and published is empty despite the same stamp; licence records unknown although the source footer states Creative Commons Attribution 2.5. Note the slug is derived from a failed Chrome probe and names a browser error page, not the document (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:5`

## http://samy.pl/evercookie/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL THREE DEMO BUTTONS DELETED WITH THEIR HANDLERS. The stored article region has three input buttons whose onClick calls create and rediscover evercookies; the published file has zero handlers and no buttons, so the example section reads "Click to create an evercookie ... Now, try deleting this uid cookie anywhere possible, then" followed by a dangling "or" - prose instructing the reader to click controls that no longer exist. Separately all nine fences wrap PROSE rather than code, with 30 Markdown links and dozens of bold markers publishing as literal text inside them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:6`

## http://samy.pl/mapxss/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE XSS PAYLOAD ITSELF WAS TRUNCATED. The stored href ends with a script tag loading fiospwn.js; the published URL stops at the ssid value, and fiospwn appears NOWHERE in the file - so the reader is handed a URL that does nothing, in a document whose entire demonstration is that URL. Both genuine handlers were deleted too: the proof of concept link whose onclick calls locateMeOnMap survives only as bold text with no link, and the MAC-lookup form with its input and submit button was dropped, leaving the sentence "Or, you can simply test the Location by entering a router MAC address:" ending on a colon with nothing after it. The recorded title is the site masthead in both frontmatter and body H1, where the document's own title names router XSS and Google Location Services - which is what the year list cites (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:22`

## http://samy.pl/natpin/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH RESEARCH HANDLERS DELETED AND THE PROOF OF CONCEPT IS GONE. The stored source has two event handlers in the article region, neither of them site chrome. The demo button - an input whose onClick calls natpin() with the chosen port - has vanished entirely, while the prose still reads 'After you click the button below, try telnet 31.94.38.26 [port]' and the next line is a bare 'Port:' with the input stripped. The second, an anchor whose onClick alerts getNetInfo(), survives only as inert words. Beyond the handlers the whole script block defining natpin() and getNetInfo() - about 45 lines, the substance of the reference - is absent: getNetInfo occurs zero times in the published file. The page's one genuine chrome script, Google Analytics, was correctly dropped. The title is also the site masthead in both frontmatter and body H1, where the document's own title is the bold line naming NAT Pinning and port forwarding (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:21`

## http://scan.flashsec.org/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE SCANNER'S DEFAULT PORT LIST WAS DROPPED, and it is research content - the demonstration form's two input elements carried the target host and a curated list of 35 ports, and the published body keeps only the field labels. The source also uses backticks as apostrophes, and they were carried through unescaped, so two of them pair into a code span and the reader sees a fragment of prose set as code with a stray delimiter before it. The typography is the publisher's; failing to escape it for Markdown is not. published is defensible as empty - the page states no publication date, only a disclosure timeline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:49`

## http://seclists.org/bugtraq/2008/Jan/0270.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE EXPLOIT URL IS CLIPPED OFF THE PAGE IN THE PDF. seclists auto-linkified the CUPS proof of concept and the converter turned it into a Markdown link INSIDE a code fence, so it prints literally at about 230 characters and overflows the page: the archived PDF ends the line at "...job_printer_uri=javascript:alert](http://loca" and the payload tail (document.cookie) is not on the page at all. The Markdown itself is intact, so this is a rendering loss rather than a deletion. Three more lines carry the same literal-Markdown-in-a-fence noise. published is empty although the message states its Date header, Fri 18 Jan 2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:55`

## http://secniche.org/gmd_hijack/advisory_gmail_google_docs_pdf_repurposing_attack.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE PDF FILE STEM, in the frontmatter and in BOTH body H1s - "advisory gmail google docs pdf repurposing attack" is the filename with underscores swapped for spaces, where the document's own cover page reads "Advisory Gmail - Google Docs Cookie Hijacking through PDF Repurposing Attack". cited_title is only "PDF", so the correct title has to come from page 1 of the body. Thirteen words are also broken across lines mid-token - wi/thout, pr/otocol, doma/in, ve/ctor, different/ial, G/oogle, proces/s, Gm/ail - and one of them corrupts the paper's own URL, splitting SNS_09_03_PDF_Silent_Form_Re_Purp_Attack.pdf across two lines; pdftotext -layout reads all thirteen cleanly on the same archived PDF, so this is extractor-introduced and repairable offline. The advisory's four raster figures are absent from the Markdown with images 0, leaving orphaned captions - the Google Docs usage chart's caption survives with no chart, and page 6 is reduced to the bare labels "Gmail/Google Doc - Domain Check" and "Gmail/Google Doc - Cookie Extracted". The archived PDF still holds them, so the loss is Markdown-only. publisher is empty although the cover states (C) SecNiche Security (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:71`

## http://secniche.org/ie_spoof_myth/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DOMAIN CHANGED HANDS AND THE NEW OWNER'S BRANDING IS IN THE ARCHIVE. The first body line is a Thai sports-betting site's title, present in the stored object itself. All the evidence is absent: the stored object contains ZERO anchors and ZERO images, so the script link is bare text, the line "Let's see what is interpreted by Microsoft Internet Explorer:" is a dangling colon with nothing after it, and the five Target Check and Inference pairs have no screenshots. The prose is also fragmented - 41 of 77 paragraphs do not end in terminal punctuation, because each hard-wrapped source line became its own paragraph and sentences are split across them (reported 2026-08-15) REPAIRED 2026-08-17: the squatted-domain import was replaced with a 2009 Wayback capture (20090328161750) of the real page. The Thai betting site's branding is gone from the first line, the document carries 13 links and 6 images where the import had none, so the script link and the Target Check and Inference screenshots are present again, and the author is credited from the page's own heading. NOT fully settled: the hard-wrapped prose the report also named is better but not fixed - paragraphs with no terminal punctuation fall from 41 of 77 to 15 of 55, some of which are headings and listings rather than split sentences - and published is still empty.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:72`

## http://securethoughts.com/2009/05/exploiting-ie8-utf-7-xss-vulnerability-using-local-redirection/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 4,689 characters of publisher furniture - 44 percent of the file - remain at the end: ten social-bookmark buttons, a tags list, a posted-on line, and a Leave a Reply form with its CAPTCHA images. The images step ran over that comment-form furniture, recording two CAPTCHA graphics and a captcha-show script that failed. The article's one real figure also points at the LIVE publisher URL rather than a local archived path even though the step stored its bytes, and one image is published with an empty link target (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:65`

## http://securethoughts.com/2009/08/pwning-opera-unite-with-infernos-eleven/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 4,621 characters - 23 percent of the file - are publisher furniture at the end, beginning at a Share: line and running through ten bookmarking buttons, a Tags list, a posted-on line, a Leave a Reply heading and the CAPTCHA form. The author's own References block above it is correctly retained. The byline was dropped although the stored source names the writer three times outside the post body - the blog description reads "Inferno's Blog on Application Security", the start link reads "Welcome to Inferno's Blog", and the blog owner replies in the comments cited as Inferno - and the article's own title is "Inferno's Eleven". A sibling in this same collection already carries that credit (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:66`

## http://securethoughts.com/2009/10/hijacking-operas-native-page-using-malicious-rss-payloads/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH RESEARCH PAYLOADS GUTTED. The article region of the stored source holds four on-handlers - two are Google Analytics tracking on the figure anchors and were correctly dropped, two are the research. The escaped img whose onerror runs script publishes as <img src="x:x" javascript"/> and the escaped anchor whose onmouseover runs script publishes as <a javascript">link</a>, so the sentence "If you had tried the simple xss attacks like ..." now shows no attack, and what remains renders as real img and anchor elements rather than visible code. Separately the proof of concept is missing at CAPTURE time, not extraction: the body ends on "here is an example PoC exploit code which executes the opera.feeds.subscribeNative function..." and the stored source already has an empty paragraph after it, so the citation's core artefact is absent either way. Note the live site is now a re-publication by new owners, who state they did not produce the content (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:63`

## http://securethoughts.com/2009/11/millions-of-pdf-invisibly-embedded-with-your-internal-disk-paths/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A BLOCKQUOTE WAS BROKEN INTO AN EMPTY BOX. The source quotes the search query filetype:pdf file c (htm OR html OR mhtml) inside a blockquote; the published file emits a bare marker, a blank line, then the query at column 0, so the reader sees an empty quote followed by the article's central query as ordinary prose. Publisher furniture also remains at the end, an 84-character Category line and a Tagged line whose unmatched bold markers render literally (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:64`

## http://securethoughts.com/2009/11/using-blended-browser-threats-involving-chrome-to-steal-files-on-your-computer/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 4,801 characters - 28 percent of the file - are publisher furniture at the end, beginning at a Share: line and running through the bookmarking buttons, a posted-on line, a Leave a Reply heading and the CAPTCHA form. The advisory's own DISCLOSURE TIMELINE and CREDITS sections above it are correctly retained. Cosmetic side effect worth noting: sections III and IV are underlined with plain hyphens in the source where the others use em dashes, so those two headings alone render as setext H2s while the rest do not - the inconsistency is the publisher's, faithfully carried (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:67`

## http://shampoo.antville.org/stories/1451301/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PUBLISHED FILE WAS BUILT FROM A THIN 2026 LIVE FETCH, NOT THE CAPTURE THE MANIFEST NAMES. Three raw hashes disagree - the manifest names a 2007 Wayback capture which is present, steps.acquire names an object that is ABSENT from the store, and the frontmatter names the live fetch that was actually used - and the content hashes disagree too. The live page is materially thinner: 5,435 characters of visible text against 8,583. WHAT WAS LOST MATTERS: the 2007 capture carries a reader CORRECTING THE ATTRIBUTION of one of the article's own references, naming the person the author had mis-credited, and the author accepting the correction. The live page also serves UTF-8 under a Latin-1 declaration and the extractor believed it, so nine lines carry mojibake - INCLUDING TWO LINK TARGETS, which are now malformed. The byline and date are missing although the document states both in its own posted-by line, and names the author again further down. About 2,290 characters of blog furniture remain at the end, including a calendar dated to the fetch year (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:7`

## http://skeptikal.org/2009/11/cross-subdomain-cookie-attacks.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title prefixes the blog name onto the document's own heading in both frontmatter and body H1, where the post's own heading and cited_title are both "Cross-subdomain Cookie Attacks". 181 characters of publisher furniture remain at the end, a Labels line of three category links and a rule. The byline was dropped although the stored source carries the post footer "posted by mckt at 9:32 AM" and the same page's profile widget names Mike Bailey - and that dropped footer is why published is empty too. COVERAGE GAP: this is a genuine pointer page, and the paper it points at, one_in_every_family.pdf on the now-dead host, is not archived - also_at is empty, linked_document_url is unset and there is no second manifest entry, so the archive holds the announcement and not the research (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:33`

## http://sla.ckers.org/forum/read.php?13,2033

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: SEVEN PAYLOADS LOST THEIR HANDLERS AND AN UNCLOSED STYLE TAG SWALLOWS THE REST OF THE FILE. The escaped vectors inside the thread's code blocks were decoded into live markup, an unclosed body tag then absorbed following text as attributes, and a sanitiser deleted every handler - so the MySpace zero-day publishes as "<body >", the cookie-stealer loses its handler mid-string, and THE SENTENCE THAT DEFINES THE TECHNIQUE becomes the fragment "(e.g.  placed in a dangerous position." Two more deletions land in prose: a list of handler names loses one of its own examples, and - most tellingly - a joke demonstrating that the platform's filter eats the Hebrew word for "you" HAD EXACTLY THAT WORD EATEN, so the converter reproduced the bug being described and destroyed both the joke and the demonstration. Entities were double-decoded too, turning an ampersand-escaped parameter into a section sign and corrupting three exploit URLs. SEPARATELY, an unclosed style element at line 193 - in the sentence recommending that unclosed tags be disallowed - makes a browser treat 411 lines and 13,779 characters to end of file as stylesheet text, so two whole sections and ALL 22 REMAINING REPLIES become invisible; a fence also swallows 43 lines of core prose, and six blockquotes are broken. All 30 posts are present - the damage is entirely inside them. published is empty although the first post states October 22, 2006 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:37`

## http://sla.ckers.org/forum/read.php?2,3153,3153

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: published is empty although every post carries a date and the first states November 19, 2006. Recorded chiefly as a POSITIVE for comparison: this is the best-preserved of the four forum threads in this collection - all eight posts present, every fence balanced, and every high-bit and UTF-7 vector byte-correct, including the alternative quotation and semicolon characters that ARE the technique. One unclosed tag sits inside a fence and stays inert and visible (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:52`

## http://sla.ckers.org/forum/read.php?3,3109

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the opening sentence's script payload was decoded into live markup outside any fence, so the vector the thread is about vanishes from its own first sentence at render. A BLOCKQUOTE IS ALSO BROKEN - an empty quote marker followed by the quoted text as an ordinary paragraph - so a reader cannot tell one participant's quotation of another from his own words; the DUPLICATE capture of this same thread renders that quote correctly, which localises the fault to this snapshot. All 30 posts are present and every exploit URL survives intact. See the duplicate entry for the dedup decision. published is empty although every post states a date (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:35`

## http://sla.ckers.org/forum/read.php?3,3109,3124

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THIS IS A DUPLICATE OF ANOTHER ENTRY IN THE SAME COLLECTION, NOT A SIBLING. The forum software renders the WHOLE thread from either URL, so this entry and its neighbour archive the same 30 posts of the same thread from different snapshots, and both carry the same cited title from two different lines of the year list. THE FORMAT-WORD CONVENTION DOES NOT APPLY - these are not two formats of one work but two captures of one thread, so the remedy is a dedup decision rather than a disambiguating suffix. The recorded title is the forum's own chrome, site name plus board name plus subject joined by colons; the thread's own subject is the last part alone. The opening sentence's script payload was also decoded into live markup outside any fence, so the vector the thread is about vanishes from its own first sentence at render. All 30 posts are present. published is empty although every post states a date. Note this capture renders its quote box CORRECTLY where the duplicate does not (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:51`

## http://stephensclafani.com/2009/05/04/clickjacking-oauth/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL THREE FIGURES ARE DEAD, and they carry the argument - the prose names them to explain which party is the Service Provider and which the Consumer. Every one failed with UnidentifiedImageError, 0 of 3 preserved, because the document was recovered from a Wayback capture but its images were resolved against the DEAD ORIGINAL host rather than the replay path the text itself came from. The same fault was filed against this author's other 2009 entry. Attribution is unresolved rather than wrong: the post carries no byline element at all, and the only evidence is the site masthead and About widget naming Stephen Sclafani on what is plainly his single-author blog (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:74`

## http://stephensclafani.com/2009/05/26/exploiting-unexploitable-xss/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all four figures are dead. The Google Sites, Blogger, YouTube and Twitter screenshots are the article's only evidence, and every one targets a host the manifest records as HTTP 404, failing with UnidentifiedImageError; each is additionally wrapped in a link to the full-size image on the same dead host, so eight targets resolve nowhere. The document was recovered from a Wayback capture but the images were resolved against the dead ORIGINAL host rather than the replay path (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:11`

## http://theharmonyguy.com/2009/10/09/the-month-of-facebook-bugs-report/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: AN UNCLOSED SCRIPT TAG SWALLOWS THE REST OF THE FILE. Nine escaped tags were decoded into live markup, and one of them is a script element whose closing tag appears ZERO times in the document, so everything after it renders inside a script. A blockquote is also broken - the quoted Facebook email now sits at column 0 and reads as the author's own words - and the byline was dropped although the source carries a rel=author element naming theharmonyguy (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:89`

## http://trivero.secdiscover.com/html5whitepaper.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE EXPLOIT SCRIPT COLLAPSES BECAUSE TWO REGEXES CONTAIN BACKTICKS AND NOTHING IS FENCED. Appendix A's HTML5CSdump listing is published with no fence, and it contains two JavaScript regexes whose literal backticks pair into an inline code span, folding six lines of the exploit into one. 101 of 555 body lines carry four or more leading spaces, so the table of contents, running headers and the whole appendix render as indented code. The paper's one screenshot is absent from the Markdown, leaving a dangling lead-in - "An example of the script's output is this:" followed immediately by the next paragraph - although the 89KB image is preserved in the archived PDF; there is no images step at all. published and publisher are both empty although the cover states first release 20 July 2008, latest revision 17 October 2008, and www.secdiscover.com. The entry also has a NULL decision - it was never adjudicated (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:10`

## http://ui-redressing.mniemietz.de/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 214 of the 228 listing lines are indented one or two spaces, below the four Markdown needs, so in a 45-page paper with 45 numbered listings EVERY listing renders as prose and its escaped markup goes live - html, head, body, an h1, a style block, a script block and an iframe all become real elements. The file contains zero backticks of any kind. The content itself is sound and complete (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:76`

## http://utf-8.jp/public/jjencode.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL SIX HANDLERS DELETED, and every one was the demo's own machinery rather than site chrome - the body onload calling init(), two keyup handlers, the jjencode button and the eval button. Both buttons therefore vanish, the variable field's value and the palindrome checkbox collapse into a bare label line, and a stale '798 letters' counter remains for an output that is no longer produced. Beyond the handlers, the roughly 110-line jjencode() implementation in the head script - the actual research - is absent: 'function jjencode' occurs zero times. The page's real chrome, the hatena and twitter buttons, uses data attributes and has no handlers, so nothing here was a correct drop (reported 2026-08-15) ALSO, ITS TRANSLATION BREAKS THE FILE'S OWN GUARANTEE: the source page contains ZERO Japanese characters - it is entirely English and only its xml:lang and Content-Language meta claim otherwise - so a machine translation pair was manufactured English-to-English, and it then corrupted what it promised not to touch. The original reads '798 letters' and the translation reads '0 letters', and three URL targets were emptied including the Permalink that encodes the demo payload. The banner asserts URLs were masked before translating and restored after so they are byte-identical - false here. A corpus-wide check found 4 of 24 translations lost URL targets, and the site serves the translation by default. (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:49`

## http://web.archive.org/web/20160507023636/http://ben-stock.de/wp-content/uploads/dns-rebinding.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the LaTeX angle brackets were emitted as the letters h and i and fused onto the domain names in the eSOP examples of section 6.3 - hexample.org, www.example.orgi and hsub.example.orgi for <example.org, www.example.org> and <sub.example.org> - 8 tokens in all, and the not-an-element operator split into two characters so the condition reads "domain in / server-origin". Scope is bounded: prose is clean, with application x39, first x13, specific x10 and difficult all correctly spelled (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:38`

## http://web.archive.org/web/20160507023636/http://www.agarri.fr/blog/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Markdown and the PDF are TWO DIFFERENT DOCUMENTS. The Markdown is the nanoblogger blog index carrying 10 posts, within which the cited work (Compromising an unreachable Solr server with CVE-2013-6397) is complete; the paired PDF is 16,111,809 bytes and 105 pages of ZeroNights_2012-Nicolas_Gregoire-XML_Hacking, a 2012 slide deck, copied in as steps.pdf.source linked-paper. The recorded title is also the site masthead rather than the cited post's own title. Separately, 12 of the 22 image links are recorded as empty response and are dead in Wayback (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:33`

## http://web.archive.org/web/20160507023636/http://www.isg.rhul.ac.uk/tls/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every one of the 9 superscript elements was flattened into its base, silently turning exponents into ordinary integers - 2^44 published as 244, 2^30 as 230, 2^24 as 224, 2^41 as 241, 2^45 as 245. So the sentence "around 230, but already with only 224 connections" should read 2^30 and 2^24, and "around 10 x 230 ... 6 x 230" should read 10 x 2^30 and 6 x 2^30. The numbers a cryptanalysis paper turns on are wrong by many orders of magnitude, and nothing about the prose looks damaged. Verified one-for-one against the stored source object (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:9`

## http://web.archive.org/web/20160507023636/http://www.isg.rhul.ac.uk/tls/Lucky13.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWO faults. The body is truncated - the file ends on the dangling heading "## For more information" with nothing under it, while the source carries that section's paragraph pointing at the research paper. And all four superscript elements were flattened into their base, so 2^23 publishes as 223, 2^19 as 219 and 2^13 as 213 - misstating the attack's headline complexity by orders of magnitude, with nothing about the prose looking damaged. Same class as the sibling RC4 entry (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:8`

## http://web.archive.org/web/20160507023636/http://www.slideshare.net/jeremiahgrossman/million-browser-botnet

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: THE TRANSCRIPT IS TRUNCATED. The published body stops at slide 9 and ends on a bare list bullet, while the stored source object carries transcript entries for slides 1 to 32 and thumbnails up to slide 50. About 23 slides are missing - Intranet Hacking, Auto-XSS, drive-by, distributed hash cracking, application-level DDoS, the connection-limit bypass, the ad-network buy and the live demo. Separately, 49 of the 55 image targets point at the HTML page rather than an image file (the manifest records UnidentifiedImageError for exactly that URL), and 26 alt texts contain raw newlines so those constructs will not parse as images at all. Proven against the store, so recoverable offline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:20`

## http://web.archive.org/web/20160507023636/https://media.blackhat.com/eu-13/briefings/Osipov/bh-eu-13-XML-data-osipov-slides.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: a broken ToUnicode CMap gives 103 wrong-codepoint substitutions across only 1,171 words, so roughly 9 percent of the deck is damaged and the prose is NOT clean - 8 stands for ti (Applica8on, valida8on, Injec8on, 53 times), F and J and B all stand for tt (AFack, aJacks, aBributes, 28 times), _ stands for ft (o_en, cra_), q stands for tf (encoding="uq-8"). Control words confirm the scope: zero correct occurrences of application, validation, function, question or option. Also 36 soft hyphens standing for ASCII hyphens, 33 undecomposed ligature codepoints, text from overlapping boxes interleaved so SYSTEM is split across three lines and root is doubled as rSoot, no slide delimiters for a 47-page deck, and every word on its own tab-terminated line. Title is also the citation label with the authors prepended, where the deck's own title slide reads XML Out-Of-Band Data Retrieval (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:10`

## http://web.archive.org/web/20160507023636/https://www.whitehatsec.com/blog/the-case-of-an-unconventional-csrf-attack-in-firefox/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the proof of concept was UNESCAPED into live markup. The source holds escaped text meant to be read - blockquote paragraphs containing &lt;script&gt; and the XHR lines - and the archived Markdown has a live script element at column 0 with the JavaScript as loose paragraphs and a closing tag further down, so in CommonMark the whole PoC becomes markup rather than displayed code. Two stray lone > lines also remain where the converter emitted each blockquote opener but dropped the > prefix from its contents, so each renders as an empty quote box followed by unquoted body (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:23`

## http://web.archive.org/web/20160507023636/https://www.whitehatsec.com/blog/tor-hidden-service-passive-de-cloaking/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: both linked figures are written across blank lines, so the link syntax cannot parse - the opening bracket, the caption and the closing target sit in separate paragraphs, and the published PDF confirms it, showing a bare [ on its own line, then the caption, then the raw URL. The image targets themselves are sound and both JPEGs are embedded in the PDF; only the markup is broken (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:13`

## http://websecurity.com.ua/2047/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title carries the publisher's "- Websecurity" suffix in frontmatter and body H1 where the post's own heading is "Recursive File Include - new face of DoS attacks". 378 characters of furniture remain at the end, and published is empty although the discarded tail and the body both state 22:55 24.04.2008. Note this document correctly has NO translation pair - it is the author's own English version of his Ukrainian post, and says so in its first line (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:68`

## http://websecurity.com.ua/2115/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE SLUG NAMES NOTHING BECAUSE THE TITLE TRANSLITERATED TO NOTHING. The page is MustLive's space-hack filter-bypass write-up, cited from the year list, whose own title is entirely Cyrillic; every Cyrillic character dropped out of the slug and left only the publisher suffix, giving "websecurity-com-ua-websecurity". It is the cited research, not a front page. The recorded title also carries the "- Websecurity" suffix in frontmatter and body H1 where the post's own heading is the Cyrillic title alone. 378 characters of furniture remain at the end, and published is empty although the date is stated twice. The payload survived correctly, including the space hack itself - the source writes the gap as a non-breaking space and the published file normalises it to a plain space, which is faithful because the article's own prose writes it with a plain space too (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:69`

## http://websecurity.com.ua/2233/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded English title STUTTERS - title_english and the body H1 both read "Session Extending - Session Extension - Websecurity", because the Ukrainian half of the publisher's title is the author's own GLOSS of the English term and translating it produces the same phrase twice; the document's own title is "Session Extending". The "- Websecurity" suffix is publisher chrome in both halves. 378 characters of furniture remain at the end of the original and 380 at the end of the translation, and published is empty although the date is stated twice. The byline here is correct and worth keeping: it rests on the article body naming the method after its author, not on sidebar chrome (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:70`

## http://websecurity.com.ua/2670/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: HALF THE DOCUMENT IS SITE CHROME, AND THE CHROME CAUSED THE GRADE. 3,834 of 7,662 characters are furniture at the end - a posted-on line, an empty comment form, language flags, then the whole site sidebar: a 23-link menu, 11 categories, ten recent-post links NUMBERED IN THE 9800s AND DATED 2026 on a 2008 article, an archive and a meta block. rule:pointer-page then measured the contaminated body and recorded "only 384 words of prose and 61 links in 5398 characters, so it points at research rather than carrying it", grading it records - but the ARTICLE alone is 1,585 characters with THREE links and 186 words of first-person prose describing a technique the author dates to 18.09.2008. All 58 extra links are the sidebar. The chooser never had a clean option: no precision candidate was generated, and both candidates already contained the sidebar. The recorded title carries the publisher's "- Websecurity" suffix in frontmatter, title_english and body H1. published is empty although the date is stated twice. ITS TRANSLATION ALSO DIVERGED: the English half was generated from an OLDER content object than the original now carries, and it turned a literal asterisk and plus into EMPTY MARKDOWN LINKS, so the two halves are no longer built from the same bytes (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:72`

## http://websecurity.com.ua/2854/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 440 characters of publisher furniture remain at the end of BOTH halves of this translation pair - a posted-on line, two category links and an RSS pitch, followed by a Leave a Reply heading, a login prompt and a rule - and that discarded block is the only place the publication date appears while published is empty, the body itself stating 22:48 05.02.2009 twice. The recorded title and title_english both carry the site masthead suffix, in frontmatter and in the body H1, where the document's own title is "Hellfire for Redirectors". The masthead is retained at the head as well. The grading is also wrong: rule:pointer-page counted 8 links, but 4 of them are the RETAINED CHROME - two category links, the feed and a wp-login link - and one is the post's own self-link, leaving three in-body citations on a post that carries the technique with two working proof-of-concept URLs. Translation fidelity itself was checked paragraph by paragraph against the Ukrainian original and is sound (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:85`

## http://websecurity.com.ua/2981/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 380 characters of publisher furniture remain at the end of BOTH halves of this translation pair - a posted-on line, a category link and an RSS pitch, then a Leave a Reply heading and a login prompt - and that discarded block is the only place the publication date appears while published is empty, the line itself reading 22:44 20.03.2009. The recorded title appends the site name to the document's own heading. The byline is recoverable and was not taken: the source names the site owner only in chrome, but a SIBLING IN THIS SAME COLLECTION settles it in the author's own words - a mailing-list post signed "MustLive, Administrator of Websecurity web site" - and that entry is credited MustLive. Translation fidelity itself was checked paragraph by paragraph and is sound, with no lost placeholders (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:86`

## http://websecurity.com.ua/3383/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the page title tag with the site masthead kept, in the frontmatter, the body H1 and the slug, where the document own heading is "URL Hiding - new method of URL Spoofing attacks". The full title string including its Cyrillic half is also published again as body text. Publisher furniture remains at the end, 378 characters of posted-on line, a Leave a Reply heading and a login prompt. Note for a maintainer: authors is empty and the capture carries NO post byline, but the site footer and the article first person together point at the handle MustLive - site-footer evidence, not a byline, so it is left as a judgement call (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:88`

## http://www.azarask.in/blog/post/a-new-type-of-phishing-attack/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAGE IS NOT THE CITED RESEARCH. The year list cites Aza Raskin's tabnabbing post; the domain was rebuilt and this URL now serves a generic 2022 SEO listicle enumerating spear phishing, whaling, smishing and vishing. Evidence from the stored object: tabnab, Raskin, favicon, 'browser tab', Gmail and 'inactive' all occur ZERO times, the generator meta says WordPress and the site name is AzarAsk. The slug itself records the substitution with its 2022 prefix, and published is 2022-06-04 with retrieved_kind live, so a 2026 refetch took the replacement page. Remedy: recapture from a 2010 Wayback snapshot (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:75`

## http://www.cgisecurity.com/2006/10/02

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: NINETY-EIGHT ANCHORS WERE DELETED TEXT AND ALL, because every one of them carried a navigation class the extractor's rule matched - including the anchors INSIDE THE ARTICLE. THE SENTENCE CITING THE PRIOR ART THIS ARTICLE BUILDS ON LOSES ITS SUBJECT ENTIRELY, reading "There has been a big fuss that with . I started to think about expanding this" - and with it goes the only pointer to the researcher whose technique is being extended, WHOSE OWN DOCUMENT IS ARCHIVED IN THIS SAME COLLECTION. The same rule blanked the self-link labels and emptied two link lists into about 35 bare bullets. THE CAUSE IS PRECISELY IDENTIFIED AND REPAIRABLE OFFLINE: do not apply the navigation-class rule inside the story container. The recorded title is the masthead plus a section label plus the headline; the document's own title is the headline alone. The article survives inside an exploded 54-row table, and 5,315 characters of furniture remain at the end. The empty authors field is CORRECT - the source names nobody (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:20`

## http://www.cgisecurity.com/papers/HackingFeeds.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PAPER'S CENTRAL CONTRAST IS INVERTED FOR THE READER. The paper distinguishes readers that treat angle brackets literally from readers that entity-decode them - and the literal example publishes as LIVE HTML, so it is invisible, while the entity example publishes visibly. The reader therefore sees the entity case and NOTHING for the literal case, the exact opposite of the argument. Roughly 35 listings are published unfenced as live markup, so the ActiveX file-steal proof, the XMLHttpRequest port-scan proof and the full Atom listing are all invisible, and the CSRF example renders as a broken image. The mitigation sentence is worse: its bold and font tags are UNCLOSED, so the recommendation to whitelist certain HTML tags renders as ", , and " with the rest of the block forced bold. TWENTY-THREE MID-WORD BREAKS are also present - descr/iption, litera/lly, privatel/y, at/tacker, se/curity - and BOTH pdftotext and pypdf read every one of them whole, so this is extractor-introduced and repairable offline. THE ARCHIVED PDF IS BYTE-IDENTICAL TO THE STORED RAW, so it is the pristine original: repair the Markdown only and never re-render this one. The recorded title is the PDF's FILE STEM and the citation's link text; the paper's own cover and its running header on all twenty pages read "Feed Injection in Web 2.0 - Hacking RSS and Atom Feed Implementations". publisher is empty although every page footer names the company (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:14`

## http://www.comsecglobal.com/FrameWork/Upload/SQL_Smuggling.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: publisher is empty although the cover states "Comsec Consulting Research" and the back page names Comsec Consulting, and published is empty although the cover states November 2007. The paper's one stored-procedure listing is unfenced. Recorded also because this entry is the rare case where the archive's extraction is BETTER than the reference tool: pdftotext drops the U+0100 character the paper's central example turns on, printing "the Unicode character (U+0100)", while the archived Markdown keeps it - so this document must NOT be re-extracted via pdftotext (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:75`

## http://www.darkreading.com/document.asp?doc_id=104313&f_src=darkreading_section_296

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS 28 LINES INSIDE A 62,000-CHARACTER FILE. The publisher's table layout produced 394 pipe rows with no separator row and 287 image references, almost all spacer images, and five image targets are BACKSLASH-ESCAPED out of a script string into malformed targets. THE IMAGES MAP HOLDS 22 ASSETS FROM THE PUBLISHER'S PRESENT-DAY HOMEPAGE - captured from the redirect target rather than the Wayback replay - with ZERO OVERLAP with the 50 images actually in the document, and the step reports "15 of 22 preserved" for a different page entirely. The byline is missing although the published body itself names the author and her masthead, and published is empty although the dateline states the date. The recorded title is the masthead plus a section breadcrumb plus the headline; the document's own title is the headline alone (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:57`

## http://www.darkreading.com/vulnerability-management/167901026/security/attacks-breaches/228000532/index.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 8,193 characters of publisher furniture at the end, 48 percent of the file - an RSS subscribe link, Write To Editor and Reprint controls, three whitepaper promos, four related items, six newsfeed links and a tech-centre nav list. Head chrome is retained too: an ad tracking pixel, a guest-login bar, search-box images and share buttons. All 14 images are unpreserved and every target is on a dead host; none is an article figure (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:11`

## http://www.dhanjani.com/blog/2008/05/safari-carpet-b.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH LISTINGS COLLAPSE TO ONE LINE AND THE EVIDENCE SCREENSHOT IS DEAD. The file has zero fences; the exploit HTML and the CGI are each wrapped in a MULTI-LINE SINGLE-BACKTICK span, and the span regex matches across newlines, so the PDF prints each as one continuous run with every line break in the exploit gone. The essential screenshot is referenced at the original dead typepad host rather than the Wayback replay the surrounding text came from, and the sentence immediately above it reads "this is what will happen to your desktop once you visit http://malicious.example.com/ :" - the picture IS the carpet bomb and it is absent; an images-prune step removed the figure rows and no re-fetch followed the recapture. Bookkeeping: health.final_url STILL points at the Network Solutions Typepad-shutdown notice with its title, even though the recapture succeeded and the published text is the real post, so the next reviewer will be misled. The byline is recoverable - the source title and masthead both name Nitesh Dhanjani on a single-author blog written in the first person - and published is empty although the source states May 15, 2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:7`

## http://www.gdssecurity.com/l/b/2010/03/17/penetrating-intranets-through-adobe-flex-applications/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the article's ONLY figure is lost, and the cause is visible in the manifest. The images step resolved the source's root-relative paths against the LIVE REDIRECT TARGET - the publisher's domain now 301s to a marketing page - instead of against the original host or the Wayback replay the text itself came from, so both fetches failed with UnidentifiedImageError and 0 of 2 were preserved. The published Markdown then points at the old host, which redirects, so the screenshot will not render either. Publisher furniture also remains at the end, 193 characters of comments-RSS and comment form (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:48`

## http://www.gdssecurity.com/l/b/2010/09/14/automated-padding-oracle-attacks-with-padbuster/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL 17 FIGURES ARE DEAD. Every image is absolute to a host that no longer serves them and the manifest records 0 of 17 preserved, so the 12 padding diagrams and the 4 PadBuster screenshots - the whole visual argument of a step-by-step tutorial - resolve nowhere. A comment-form tail also remains, 187 characters, and the live URL now redirects to a marketing page, which the archive correctly avoided by using a Wayback capture (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:5`

## http://www.hardened-php.net/library/poking_new_holes_with_flash_crossdomain_policy_files.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED DATE IS THE CMS TEMPLATE'S BOILERPLATE, NOT THE DOCUMENT'S. The stored page carries TWO date meta tags: the publishing platform's own template default early in the head, and the page's real date later, matching the footer. The archive took the FIRST, so published records a date more than a year before the document was written - and that wrong year is what produced this entry's slug prefix, filing a 2006 paper under 2005. The document states its own date and its own byline together in one line, and the byline is missing too: authors is empty although that line names Stefan Esser. The recorded title glues the site masthead onto the document's own title, which is "Poking new holes with Flash Crossdomain Policy Files", matching the citation. Two bullet lists are also EXPLODED - each marker renders as a lone paragraph with its item text as a separate paragraph - and the second of those is content, not chrome. 283 characters of badge furniture remain at the end, but the line ABOVE the cut is the byline and the only date the document states, so it must not be cut with it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:38`

## http://www.informationweek.com/news/showArticle.jhtml?articleID=192500500&subSection=Breaking+News

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS TEN PERCENT OF THE FILE. It occupies 29 of 544 lines, with 184 lines of masthead and navigation before it and 267 lines of sidebar and footer after; 11,495 characters - 42 percent - are removable furniture at the end. The page's table layout produced 282 pipe rows with no separator row and 270 image references, almost all spacer images. THE IMAGES MAP IS ALSO STALE: it holds 35 assets from the publisher's present-day site - layoff tracker and profile artwork - left over from a superseded capture and never pruned. The byline and date are both missing although the published body itself carries them: a by-line naming Larry Greenemeier with his address, and a timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:55`

## http://www.jumperz.net/index.php?i=2&a=3&b=3

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 1,327 characters - 15 percent of the file - are sidebar and footer furniture at the end, and THE BYLINE IS INSIDE THAT BLOCK, so it must be recorded before the cut. An emphasis run is unclosed on the line pointing at the demonstration, so it prints with literal asterisks or bolds through to the next heading. The page's table layout emits 35 pipe rows with no separator row, and the two quoted documentation blocks and the links list are unfenced, so 18 paragraphs swallow 56 lines. Thirteen images are all site chrome resolved against the dead host, so the body carries 37 broken image references. The recorded title prefixes the letter-spaced site name onto the document's own title (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:65`

## http://www.mbsd.jp/Whitepaper/smtpi.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the apostrophe glyph is dropped inside words throughout the prose - Vicente Aguilera Diazs work, Now lets see how the attack works, delivered to the victims mailbox, Rubys Mail, In the JavaMails case - with exactly one apostrophe surviving in the whole file; and pages 20 to 22 are decoded PDF font and timestamp-signature streams published as prose, holding both of the files U+FFFD characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:44`

## http://www.mikeonads.com/2008/07/13/using-your-browser-url-history-estimate-gender/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE INTERACTIVE DEMO IS AN EMPTY CODE SPAN. The paragraph reads "Click the button to give it a shot" and is followed by a bare pair of backticks enclosing nothing, where the demo widget was, so the article's whole point renders as blank. The recorded title is a WordPress breadcrumb in frontmatter and body H1 AND is repeated a third time as the first body line, where the post's own title is "Using your browser URL history to estimate gender". 884 characters of furniture remain at the end - related posts, a Digg link, the byline line and an empty comment form - and THE BYLINE IS INSIDE THAT BLOCK, reading "Posted by Mike", so it must be lifted before the cut; published is empty although the document's own date heading reads July 13th, 2008. The only published figure is an ad-network tracking pixel, kept as a figure. Note the raw object is absent from the store and correctly recorded on store-gaps (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:32`

## http://www.networkworld.com/news/2008/080708-black-hat-ssl-vpn-security.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 6,330 CHARACTERS - 54 PERCENT OF THE FILE - ARE FURNITURE AT THE END, and it is anachronistic furniture: the capture is a 2012 re-render of a 2008 story, so the Most Read and Latest News blocks carry 2012 content on a 2008 article. The block runs from a "Read more about security" line through two RSS icons, a newsletter sign-up and the full Industry, Job Title, Company Size and Country select lists dumped as prose - one line alone is the entire ISO country list. published is empty although the source carries the date in TWO meta tags, publicationDate and DC.date.issued, both 2008-08-07. Note cited_title records the string "2)", a parse artefact of the citing list's multi-link line (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:13`

## http://www.ngssoftware.com/research/papers/InterProtocolCommunication.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PAPER'S CENTRAL PAYLOAD IS PUBLISHED AS LIVE MARKUP, TWICE, so the reader sees an empty pair of quotation marks where the reflected script should be, and the document itself becomes live script. THE RECORDED TITLE IS THE CITING LIST'S LINK TEXT, not the paper's: its cover page and its PDF metadata both read "Inter-Protocol Communication", and the fabricated heading now sits directly above the real cover title. CONTENT IS MISSING AND IT IS SOURCE-INHERENT: page 6 extracts to 885 characters against roughly 2,700 for its neighbours, and the three sections its own table of contents promises there exist in NO extraction - the page carries a run of garbage instead. DO NOT RE-RENDER: pdftotext reports syntax errors and returns the identical garbage, and pypdf raises an exception on that page outright; the damage is in the archived original. Every listing is unfenced and 37 lines at four-space indent become accidental code blocks. publisher is empty although every page footer names the research group, and published is empty although the document states its date twice (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:41`

## http://www.paulosyibelo.com/2017/05/twitter-xss-csp-bypass.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title glues the blog masthead in front of the article title and the slug carries the masthead TWICE; the source title tag has them the other way round and the recorded string came from the keywords meta. Also the script src URL was autolinked INSIDE the attribute so the renderer emits a real script element and the payload is invisible, and the proof-of-concept iframe was reduced to a line containing one space (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:39`

## http://www.procheckup.com/Vulner_PR0703.php

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAYLOAD RENDERS AS A WORKING LOGIN FORM. All fifteen escaped pairs in the advisory were decoded into live markup, and the fourth proof of concept - which injects a credential-harvesting form - is unfenced in a paragraph, so a real form with a password field and a submit button now renders on the archived page instead of the payload text. WORSE, THE TECHNIQUE ITSELF IS ERASED BY EMPHASIS: the finding is that the filtered keyword can be split with CSS comments, and those comment markers are asterisks, so a renderer pairs them and DELETES them - the comment-splitting trick that is the entire advisory disappears from three payload lines. The recorded title is the vendor page's own site title; the document's own title is the advisory heading naming the request-filtering bypass. 1,255 characters of promotional furniture remain at the end - but the advisory's own copyright and permission block above it is part of the document and must stay. All fourteen images failed against the dead host rather than the Wayback replay. published is empty although the advisory states a public date. Worth recording as a POSITIVE: unlike the 2008 sibling, the research here IS archived in full (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:22`

## http://www.procheckup.com/vulnerability_manager/vulnerabilities/paper-04

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RESEARCH WAS NEVER CAPTURED - THIS IS THE VENDOR'S SITE CHROME. The whole stored page is navigation, a phone number, a Contact Us box, a Services box and an events promo, wrapped around the line "This document is a external file. Previews: Please, click here to download". The cited research is the external PDF SNMP_injection.pdf, "Paper-04 SNMP Injection: Achieving Persistent HTML Injection via SNMP on Embedded Devices", and it is nowhere in the archive. rule:pointer-page graded it records, which is why an unarchived document reaches no gap report - a genuine pointer page this time rather than a broken extraction, but the effect is identical. The recorded title in frontmatter and body H1 is the SITE title, "ProCheckUp - Penetration Testing, PCI DSS Compliance, Application Testing", where the page's own heading names the paper; cited_title records the string "1", a parse artefact of the citing list's multi-link line. Nine images recorded 0 preserved, and five of them are PAGE PREVIEWS OF THE PAPER - the only surviving trace of its content. Checked and cleared: the sibling citation on the same list line holds a DIFFERENT ProCheckUp paper and does not cover for this one (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:27`

## http://www.procheckup.com/vulnerability_manager/vulnerabilities/pr08-21

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ADVISORY'S FIELD TABLE IS CONCATENATED WITH NO SEPARATOR AT ALL. The source is a definition list, and the extraction ran the terms and values together, so one line reads "Vulnerability found:03 October 2008Vendor informed:03 October 2008Severity level:CriticalCredits:Adrian Pastor of ProCheckUp Ltd" and another runs the proof of concept, the fix and the references together. 81 characters of a back-to-list link remain at the end; the copyright and redistribution block above it is a LICENCE THE RESEARCH CARRIES and must stay (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:77`

## http://www.secforce.co.uk/media/demos/PHP_socket_hijacking_demo.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAGE IS MOSTLY CHROME AND ITS SUBSTANCE IS AN UNPRESERVED FLASH VIDEO. Of 51 body lines about 40 are navigation and spacer-GIF table rows; the article's real content is three lines. The chooser had NO precision candidate at all - only recall at 4,999 characters with 63 images and 23 tables, and raw, which won - so a clean extraction was never on offer. The recorded title in frontmatter and body H1 is the site title "SECFORCE :: Research", which is also the name of a DIFFERENT page; this page's own title, from its heading and its breadcrumb, is "PHP Socket Hijacking Demo". What the page exists to show is a screencast, and all that survives is the placeholder line "Get the Flash Player to see this player." directly above a description of what the video shows. 1,478 characters of furniture remain at the end and a comparable block remains at the head. All 19 images recorded an empty response, resolved against the dead host rather than the Wayback replay; all 19 are chrome, so no real figure was lost (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:76`

## http://www.secforce.co.uk/media/presentations/OWASP_Abusing_PHP_sockets.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: SIX SLIDES PUBLISH AS BARE HEADINGS WITH NOTHING UNDER THEM. Pages 6, 8, 11, 12, 14, 15 and 16 of the deck - Windows Socket Reuse, the Apache listen.c source, PHP Socket Reuse before and after, the PHP shell attack and the man-in-the-middle attack - carry between 4 and 47 characters of text each because their content is entirely screenshot, and no images step ran, so the demonstration is absent from the Markdown. 72 of 135 body lines, 53 percent, carry four or more leading spaces from the slide layout and render as indented code; the socket calls are unfenced. published and publisher are empty although slide 1 states OWASP April 2008. The entry has a NULL decision - it was never adjudicated. DO NOT RE-RENDER for the twelve soft hyphens (Low-level, re-using, Man-in-the-middle): they are OpenOffice hyphenation baked into the source text layer and the archive preserves them faithfully (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:76`

## http://www.secniche.org/papers/SNS_09_01_Evad_Xss_Filter_Msword.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 45 confirmed MID-WORD LINE SPLITS - an\|d, otherwis\|e, Micr\|osoft, maliciou\|s, conversio\|n, efficien\|tly, Java\|Script - plus reference URLs shattered across five lines each and a page footer fused into a heading, giving "Evading Web XSS Filters through MS Word 14Appendix 3:". pdftotext -layout on the SAME stored PDF reads all of it cleanly, so the damage is extractor-introduced and a repair path exists. Note also that the recorded title is the citing list's wording; the paper's own page-1 title is "Evading Web XSS Filters through Word (Microsoft Office and Open Office) in Enterprise Web Applications" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:61`

## http://www.sectheory.com/file-sharing.htm

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: published is empty although the body's second line states "Date: 02/02/2008". Recorded because the same publisher's sibling in this collection has the same gap and the live host is now a domain takeover serving a casino, so the stated date inside the document is the only date evidence that survives (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:43`

## http://www.sectheory.com/intranet-hacking.htm

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THREE NETWORK PREFIXES WERE EATEN AS EMPHASIS MARKERS. The sentence is about companies that use 1.*, 2.* and 3.* rather than RFC1918, and the line has an odd asterisk count, so the rendered PDF prints "Some companies use 1. or 2.. General Eletric uses 3. for instance" with the /8 prefixes gone and a stray asterisk leaking at the end. A blockquote is also broken - only the first of three address ranges carries its marker, so 172.16/12 and 192.168/16 fall out of the quote and merge into a run-on paragraph. BOTH of the paper's figures are dead: they are the mechanism diagram the prose points at, and they resolve against the original host, which now serves a casino after a domain takeover, rather than the Wayback replay the text itself came from. published is empty although the body states its date (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:39`

## http://www.sectheory.com/rfc1918-security-issues.htm

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL FOUR DIAGRAMS ARE DEAD, and the prose depends on them - it says "as seen in Fig 1" through "Fig 4", each followed by a Click to enlarge caption. Every one failed with UnidentifiedImageError because the original host now chains through redirects to an online-casino page, so it answers with HTML rather than an image. The same Wayback snapshot that supplied the text would supply the images. The right-page recovery itself is sound and recorded in overrides (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:13`

## http://www.securiteam.com/securitynews/5JP051FKKE.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE IMAGES WERE HARVESTED FROM THE REDIRECT TARGET, NOT THE DOCUMENT. The images map holds two 2025-dated product screenshots from the ACQUIRING COMPANY'S marketing site, which appear nowhere in the archived text - the live host was taken over by acquisition and the image step followed the redirect instead of the Wayback replay the body came from. The document's own four images are chrome and have no bytes. The byline is empty although the document states it outright in a Credit line naming the researcher, who is distinct from the publisher. published is empty although the source's header cell states its date - and that header was dropped from the body, so only the comment dates survive. The recorded title carries the publisher's masthead ahead of the document's own. 479 characters of furniture remain at the end. TWO READER COMMENTS CARRY TECHNIQUE and should not be cut with it: one rebuts the article's implied mitigation by explaining that redirecting the temp directory does not help because the environment variable still discloses it, and another links a companion bypass. Note the slug carries the ACQUIRER's hostname while publisher correctly records the original (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:76`

## http://www.securityfocus.com/archive/1/437948/30/0/threaded

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline is missing although the advisory is SIGNED IN ITS OWN BODY with the researcher's name and company, and published is empty although the body states its date explicitly. Each advisory line became its own paragraph, so the listing renders double-spaced, though nothing is lost. Note the threaded view carries fifteen reply subjects as links only and none of the replies is archived - that is source-inherent to this publisher's threaded view rather than a converter loss, but also_at is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:69`

## http://www.securityfocus.com/archive/1/441014/30/0/threaded

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PROOF IS A HOLE IN THE LISTING. The single escaped payload in the whole source is the researcher's Expect-header script, and it was decoded into live markup OUTSIDE any fence - this file has none - so at render it becomes a real script element and the reader sees the request line with nothing between the quotes. That payload IS the finding. Every listing is also unfenced, so consecutive lines join and the ActionScript renders as one wrapped line; the underline rules beneath headings become setext headings; and three wrapped reference URLs are joined with a space, so they are unclickable and wrong. published is empty although the body's own dateline states the month and year (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:10`

## http://www.sift.com.au/36/172/xml-port-scanning-bypassing-restrictive-perimeter-firewalls.htm

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE CITED RESEARCH IS NOT ARCHIVED AND NOTHING RECORDS THAT. What is archived is the vendor's PUBLICATIONS LISTING page - its own headings show it is a list, carrying two other announcements alongside - with a two-paragraph abstract and a download icon linking the paper. The paper is in no manifest entry anywhere, also_at is empty and there is no linked_document_url; rule:pointer-page graded it records and content_gap was empty, so it reaches NO gap report. This is the 2007 vendor-page pattern reproducing itself. WORSE, THE ONE ROUTE TO THE RESEARCH IS AN INVISIBLE BROKEN IMAGE: the download link's only content is an icon that failed against the now-DNS-dead host, so it renders as a broken image with no text. The recorded title is the site masthead carried on every page of that site; the document's own heading names the port-scanning technique and carries the only date, which is unrecorded. Six navigation headings publish as top-level headings above the article (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:40`

## http://www.slideshare.net/inquis/expanding-the-control-over-the-operating-system-from-the-database

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: 2,615 characters of SlideShare thumbnail-navigation chrome are retained at the head of the content, twenty numbered slide links plus three "Most read" labels. Six of the 31 slide images have no stored bytes, so a quarter of a deck whose text lives entirely in image alt attributes is unpreserved. authors also records only "Bernardo Damele A. G.", which is SlideShare's abbreviation rather than the document's own spelling, and the deck's "Who we are" slide names a SECOND author, Guido Landi. Note the manifest raw_sha256 is absent from the store while the file's is present (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:82`

## http://www.spidynamics.com/spilabs/education/articles/JS-search.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RESEARCH IS LINKED, NOT HELD, AND THE GRADE SAYS OTHERWISE. This page is a 277-word abstract that ends "Click here to view the entire article", pointing at a PDF that is NOT in the archive - yet the entry is graded research with the reason "carries technique". It does not carry it; it points at it. This is the INVERSE of the recorded pointer-page footgun: there a broken extract was graded records, here a genuine pointer page escaped the rule entirely. The byline is also missing although THE PUBLISHED BODY ITSELF states it plainly as an organisational credit, and this URL is absent from the byline review file, so it was never read rather than read and found wanting. Three cp1252 apostrophes were silently DROPPED in decoding, so the text reads "a users search queries" and "an HMOs website". An emphasis run is broken so a heading prints with four literal asterisks, and the page's right-hand MARKETING SIDEBAR is merged into the article flow, ending on a stray table cell. The slug takes its host from the ACQUIRING COMPANY. Checked and CLEARED: the images were NOT harvested from the acquirer's site (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:21`

## http://www.spidynamics.com/spilabs/education/articles/MySpace-QuickTime%20Worm.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE BELONGS TO A DIFFERENT ARTICLE. The page's title element is a stale SEO template left over from another piece, and the accompanying meta description proves it - it describes an article about SQL injection and cross-site scripting, which this document is not. This document's own heading reads "Security Brief: MySpace Quicktime Worm", matching the year list's citation. THE SLUG IS DOUBLY WRONG: it takes its host from the ACQUIRING COMPANY, because the live URL now lands on their 404 redirector, and its topic from the wrong article's title. ATTRIBUTION TRAP, worth recording before anyone runs a byline sweep here: the meta description carries "By Caleb Sima" - that is a meta field, which does not count, AND it is the byline of the WRONG article; the brief itself names only the lab. One cp1252 character was silently DROPPED in decoding, leaving a double space in the title. Four emphasis runs are broken so the reader sees literal asterisks around all four Additional Information headings, and the page's right-hand MARKETING SIDEBAR is merged into the article flow, ending on a stray table cell. Checked and CLEARED: the images were NOT harvested from the acquirer's site - every one is a period-correct relative path from the original publisher (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:76`

## http://www.suspekt.org/2008/08/18/mysql-and-sql-column-truncation-vulnerabilities/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE CAPTURE IS A THIRD-PARTY RE-HOST, NOT THE RESEARCH BLOG. The redirect chain runs suspekt.org to suspek.org - the letter t is gone - and the stored page is a consumer affiliate site whose navigation reads Parental Control, Phone Monitoring, Buying Guide, VPN, Antivirus, Password Managers, carrying the old posts across. Its JSON-LD declares datePublished 2019-12-26, a republication artefact on a 2008 article. The article TEXT is verbatim and complete, so the document is right, but the provenance is not: the re-host strips the byline, so the archive cannot credit Stefan Esser from these bytes, and published is empty although the original URL carries 2008/08/18. A Wayback capture of the suspekt.org URL would be the sound source. Note the manifest raw_sha256 is ABSENT from the store while the frontmatter hash resolves, so this must not be re-rendered with --force (reported 2026-08-15) REPAIRED 2026-08-17: the third-party re-host was replaced with a 2009 Wayback capture (20090206030917) of the original suspekt.org page, so the provenance is now the research blog rather than a consumer affiliate site, and Stefan Esser is credited from the page's own byline line - the credit the re-host had stripped. The recorded title was corrected back from the blog masthead form via overrides, which also rebuilt the slug from suspek-org to suspekt-org and the old pair was removed.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:42`

## http://www.techcrunch.com/2006/09/08/metaverse-breached-second-life-customer-database-hacked/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED AUTHORS ARE A PLACEHOLDER LABEL AND THE PUBLISHER'S OWN ACCOUNT - "Contributor", which is the modern site's generic byline slot, and the publication's social handle. Neither is a person and neither counts. THE STORED 2007 CAPTURE NAMES THE REAL AUTHOR in a heading directly under the headline. published also contradicts the source's own words: the document states it was posted on Friday, September 8th, and the URL path agrees, while the recorded date is a day later. HASH DISAGREEMENT WITH A --force TRAP: the manifest names the Wayback capture, which is present, but steps.acquire AND the published frontmatter both name a different object which is ABSENT from the store - so a --force re-render would go to the network. 380 characters of tag and disclosure furniture remain at the end. The body itself was diffed sentence by sentence against the capture and is complete (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:61`

## http://www.thesecuritypractice.com/the_security_practice/2009/03/socket-capable-browser-plugins-result-in-transparent-proxy-abuse.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 2,512 characters of tail - 36 percent of the file - are TypePad furniture: a trackback URL, a ten-entry month archive, eight category links, a feed link and a four-item blogroll, ending on an empty list bullet. Note also that the SLUG is named after the dead redirect target host rather than the publisher, which the entry itself records separately, and that linked_document_url is unset although the post exists to announce a PDF that appears nowhere in the manifest (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:22`

## http://www.webappsec.org/lists/websecurity/archive/2006-10/msg00063.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 1,320 characters - 20 percent of the file - are list furniture at the end, and head furniture sits above the message too. The message body is split into TEN separate code blocks, one per paragraph, so plain prose renders monospaced and unwrapped - that mirrors the archive software and is source-inherent. published is empty although the message header states the date, and the header block itself IS intact here, so the known mailing-list failure did not occur. One archive-side aggravation: markdown link syntax was emitted INSIDE a code fence, where it renders literally, so the reader now sees bracket and parenthesis characters the source rendered away. CLEARED as source-inherent: the mangled link targets and the doubled semicolon are the archive software's own over-greedy auto-linker (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:49`

## http://www.webappsec.org/lists/websecurity/archive/2009-04/msg00047.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 1,478 characters - 19 percent of the file - are mailing-list furniture at the end, opening on a rule inside a fence and running through an IRC invitation, an archive search pitch, an RSS link, a LinkedIn link, the Prev/Next by Date and thread navigation and a Brought to you by line, ending on a stray table cell. Head chrome is retained as well, the Date Prev / Thread Next index navigation. Separately, a single plain-text mailing-list message is chopped into NINETEEN separate fenced code blocks, one per source pre element, so a continuous message reads as nineteen disconnected listings. The message text itself was verified word for word against the decoded source and nothing is lost (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:87`

## http://www.webappsec.org/projects/articles/121106.shtml

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: EVERY PLACEHOLDER IN THE PAPER'S NOTATION IS DESTROYED. All 45 escaped placeholders were decoded into live markup with no fences to protect them, so at render each is an unknown element the browser drops - the authentication line publishes as "AUTH LOGIN" with both operands gone, the message-identifier URLs lose their parameter, and every mail-from and recipient address vanishes. ONE IS ALREADY GONE FROM THE FILE: the source left a single occurrence unescaped and the converter deleted it outright, so the defensive rule the paper prescribes now reads "filter the sequence  in the vulnerable parameters" - the sequence it names is the fix. The unfenced listings also JOIN their lines, which is fatal for a paper whose whole point is that the injected sequence produces SEPARATE command lines. The comparison table is exploded into orphan cells with no separator row, the byline's emphasis never closes so it prints with six literal asterisks, and a version line became a bullet. The recorded title carries the publisher's section label and index brackets. The one diagram's bytes ARE preserved in the store, but the file points at the remote host instead of the local copy. published is empty although the document states its own version date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:22`

## http://www.whitehatsec.com/downloads/WHXSSThreats.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE FONT DAMAGE CORRUPTS THE WORM SOURCE, AND IT IS REPAIRABLE OFFLINE. The extractor mapped the embedded font's byte codes straight to Latin-1 instead of applying the character map, so ligatures and curly quotes publish as accented capitals - the archived Samy worm code reads proÞle.myspace.com and ÞndIn(), which will not run, and several end-note URLs are corrupted the same way. BOTH pdftotext AND pypdf read the stored PDF cleanly, so this is extractor-introduced and fixable from the 1.76MB object already in the store with no fetch. The same pass also collapsed all word and line spacing, so each page publishes as one run-on paragraph with tabs surfacing as punctuation. THE APPENDIX'S CENTREPIECE IS INVISIBLE: the complete worm exploit sits inside an attribute of a div that was decoded into live markup, so a reader sees an empty element where the code should be. The recorded title is the CITATION LABEL, not the document's - its own cover reads "Cross-Site Scripting Worms and Viruses: The Impending Threat and the Best Defense". Ten figure captions survive with nothing behind them, including the propagation comparison the analysis section argues from. publisher and published are empty although the cover states both (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:13`

## http://www.wisec.it/sectou.php?id=4698ebdc59d15

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE HEADER THE WHOLE TECHNIQUE KEYS ON IS EATEN AS EMPHASIS. The baseline request line "Accept: */*" publishes with its asterisks read as an emphasis run, so both this repository's renderer and its website print "Accept: /" - and that request is what produces the Content-Location, Vary and TCN fingerprint the article exists to describe. SEVEN EMPTY BLOCKQUOTES: each is a bare marker whose quoted block follows unquoted, so the Apache specification text and every HTTP request and response render as the author's own prose behind an empty grey bar. The researcher's 406 response body was escaped at source and is now 21 LIVE RAW TAGS in the Markdown, so a CommonMark reader emits a real title element and a real heading and shows nothing. THE RECORDED TITLE IS THE BARE SITE NAME, "Wisec - The WIse SECurity", in frontmatter and body H1, where the document's own subject line reads "Multiviews Apache, Accept Requests and free listing" - which is also what cited_title already says - and ALL SIX wisec entries in this collection carry that same identical title, separated only by a numeric suffix that says nothing. The three reader COMMENT BODIES were dropped while their BYLINES WERE KEPT, so the file shows three orphan names; one of the dropped bodies is a platform caveat reporting the technique does not work on FreeBSD, and another was the only in-document evidence naming the author. 1,852 characters of nav and a thirteen-item recent-posts sidebar sit ABOVE the article, so the first screen is a link list. published is empty against a stated Saturday, July 14, 2007, 17:26 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:35`

## http://www.wisec.it/sectou.php?id=46d592056b008

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE VULNERABLE-CODE EXAMPLE WAS DECODED INTO LIVE MARKUP - the PHP processing instruction is now real, and a browser reads it as a bogus comment terminating at the first angle bracket, so the example disappears from a plain reader's view. Nested unclosed bold tags in the source also leave literal asterisks printed around three of the warning messages that are the technique's evidence. THE RECORDED TITLE IS THE SITE NAME, shared identically with five siblings; this document's own subject line names the internal-LAN scan. published is empty although the source states Wednesday, August 29, 2007, 17:24 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:32`

## http://www.wisec.it/sectou.php?id=4706611fe9210

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE COMMENT BODIES WERE DROPPED WHILE THEIR BYLINES WERE KEPT, AND THE ARTICLE STILL PUBLISHES A NUMBER ITS AUTHOR RETRACTED. Eight comments totalling 3,889 characters are gone, leaving eight orphan author-and-date lines; in the first of them the author CORRECTS HIS OWN ARITHMETIC in the article, and the published text still shows only the figure he withdrew. Also lost: a reader's full derivation of when the technique beats bisection, the comment in which the technique IS FORMALLY NAMED, and a follow-up from the author of a well-known injection tool. Four blockquotes are broken - a lone marker with its content on the next line - so the pseudo-code collapses onto one line, and several emphasis runs are unpaired. THE RECORDED TITLE IS THE SITE NAME, identical across all six entries from this publisher; this document's own subject line names the blind-injection optimisation. published is empty although the source states Friday, October 05, 2007, 17:47 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:36`

## http://www.wisec.it/sectou.php?id=472a5b8d1a4cd

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE INJECTED PAYLOAD WAS DECODED INTO LIVE MARKUP - the script element the response-splitting attack delivers is now real inline HTML in two places, so a conforming reader sees nothing where the payload should be. Two blockquotes are broken, leaving the HTTP traces stranded as paragraphs, and one of the author's DELIBERATELY DEFANGED URLs had its bare scheme linkified into an empty-text link. THE RECORDED TITLE IS THE SITE NAME; this document's own subject line names the response-splitting and data-URI finding. published is empty although the source states Thursday, November 01, 2007, 23:29 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:50`

## http://www.wisec.it/sectou.php?id=472f952d79293

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE COMMENT BODIES WERE DROPPED WHILE THEIR BYLINES WERE KEPT, and the article explicitly asks for what the comments supply - it says the authors hope a reader will provide information about other web servers, and a later comment delivers exactly that with a full request and response for a server absent from the published lookup table, plus the finding that POST data cannot be sent that way. The table the article publishes is incomplete without it. Five blockquotes are broken, leaving the HTTP traces as paragraphs. THE RECORDED TITLE IS THE SITE NAME; this document's own subject line names the bandwidth optimisation. published is empty although the source states Monday, November 05, 2007, 22:54. Note the article credits a named co-contributor from another company in its own text (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:37`

## http://www.wisec.it/vulns.php?id=11

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWO CROSS-REFERENCE ANCHORS WERE DROPPED, so the advisory's own pointers - "see updated details here" and "affected by this issue" - now go nowhere, although the section they point at is present further down the file and the repair is in place. The recorded title in the FRONTMATTER is the site name, but note this document is the exception in its group: ITS REAL TITLE IS ALREADY IN THE BODY H1, taken from the page's own heading, so the correction is sitting in the file. published is empty although the document states a release date, a discovery date and a dated sign-off - the most clearly stated date of its group. The entry is recorded as an article, which understates a vulnerability advisory (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:54`

## http://xs-sniper.com/blog/2007/07/17/firefoxurl-uri-handler-flaw/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "Firefoxurl URI Handler Flaw". authors is empty although the published body itself prints the handle byline. 1,803 characters of comments were dropped while the empty comment form was kept; they carry an argument about vendor responsibility rather than new technique, and two trackbacks recording that the post was a nominee. published is empty against a stated Tuesday, July 17th, 2007. The body itself is otherwise sound - two apparent faults were checked and cleared: an apparent word loss is the publisher's spellchecker markup which the extractor correctly rejoined, and the odd apostrophes are source-inherent (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:10`

## http://xs-sniper.com/blog/2007/07/20/more-uri-stuff-ies-resouce-uri/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "More URI Stuff... (IE's Resouce URI)", keeping the publisher's own misspelling, which is correct fidelity. authors is empty although the published body itself prints the handle byline, and a dropped comment independently names the author. 2,228 characters of comments were dropped while the empty comment form was kept; one of them records the CVE assignment for this finding, which is metadata the document does not otherwise carry. published is empty against a stated Friday, July 20th, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:51`

## http://xs-sniper.com/blog/2007/09/01/firefox-file-handling-woes/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DROPPED COMMENTS CARRY THE MITIGATION AND THE ARTICLE DOES NOT. 4,979 characters were removed while the empty comment form was kept, and they contain the working workaround - the exact preference lines to disable the external protocol handlers, the safer per-scheme variant, the about:config guidance, and an analysis of why sandboxed versus unsandboxed handler bindings are the root cause. None of it is in the archived text. The article's ONLY evidence is also gone: it deliberately withholds the exact details and substitutes a screenshot, and that image recorded an empty response against the DEAD ORIGIN rather than the Wayback replay, so the sentence introducing it now leads to nothing. A blockquote is broken, so the vendor advisory quotation reads as the author's own prose. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "Firefox File Handling Woes". authors is empty although the published body itself prints the handle byline. published is empty against a stated Saturday, September 1st, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:33`

## http://xs-sniper.com/blog/2007/09/20/bk-for-mayor-of-oak-tree-view/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE EVIDENCE IS THE FIGURES AND NONE OF THEM SURVIVE. This is a 313-word post carrying four screenshots, and ALL FOUR recorded an empty response against the DEAD ORIGIN rather than the Wayback replay the text itself came from. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "BK for Mayor of Oak Tree View". authors is empty although the published body itself prints the handle byline. 1,238 characters of comments were dropped while the empty comment form was kept, carrying no technique. GRADING QUESTION: rule:pointer-page graded this records on a count of sixteen links, but four of those are image-wrapper links that the review workflow itself classes as known-normal, two are the byline's category tags and one is the post's own permalink - leaving three real outbound links in the prose, on a post that discloses an access-control failure rather than pointing at one. published is empty against a stated Thursday, September 20th, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:28`

## http://xs-sniper.com/blog/2007/09/24/stealing-pictures-with-picasa/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE NAME ALONE WITH NO POST TITLE IN IT - frontmatter and body H1 both read "Billy (BK) Rios", because this 2008-era theme put only the blog name in its title element. It identifies the blog and never the article. The post's own heading, preserved further down the file, is "Stealing Pictures with Picasa", which is also exactly what cited_title says. 3,530 characters of reader comments were dropped while the empty comment form was kept. A theme credit leaked INTO the article text as its first body line. published is empty although the byline in the body states the date inside itself - this is the one document here whose byline carries its own date. Attribution is otherwise the strongest in its group: the body prints a full explicit byline naming Nate McFeters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:18`

## http://xs-sniper.com/blog/2007/09/26/google-docs-puts-google-users-at-risk/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DROPPED COMMENTS HOLD THE ONLY DISCUSSION OF THE ARTICLE'S DELIBERATELY OMITTED STEP. 3,635 characters were removed while the empty comment form was kept; they contain a simpler variant of the attack with the exact unprotected endpoint spelled out, the sharp question about how the attack reaches a second Google host when the policy file authorises only one, and the author's own two replies - including his distinction between causing the browser to display sensitive content and pulling it to an attacker-controlled domain, and his note that the second step is left as an exercise. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "Google Docs puts Google Users at Risk". authors is empty although the published body itself prints the handle byline, and the dropped comments contain the author self-identifying. published is empty against a stated Wednesday, September 26th, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:29`

## http://xs-sniper.com/blog/2007/09/28/all-your-google-docs-are-belong-to-us/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PARAMETER THE VULNERABILITY TURNS ON IS SWALLOWED. Four escaped placeholders in the ARTICLE REGION were decoded into live pseudo-tags, and CommonMark parses each as a valid open tag and drops it, so a reader of the Markdown sees "DocID= HTTP/1.1" with the identifier gone. The archive's own PDF and website escape it, so the fault is invisible to those two surfaces and bites anyone reading the file directly. An emphasis run also opens and closes across a blank line, so one request listing prints with literal asterisks while the listing beside it renders correctly - the two are inconsistent side by side. The eleven document identifiers that demonstrate the increment are unfenced and reflow into a three-line run-on, destroying the one-per-line structure that makes the pattern visible. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "All Your Google Docs are Belong To US...". 2,727 characters of comments were dropped while the empty comment form was kept, carrying no technique. The one figure recorded an empty response against the dead origin. published is empty against a stated Friday, September 28th, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:30`

## http://xs-sniper.com/blog/2007/11/04/java-applets-and-dns-rebinding/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ATTRIBUTION NEEDS A HUMAN DECISION - DO NOT AUTO-CREDIT. authors is empty and the published body prints the site owner's handle byline, but THE ARTICLE'S OWN FIRST PERSON EXCLUDES HIM: it reads "For those of you who were able to see Billy and I present at Hack In The Box", and the dropped comments corroborate, with a second researcher writing in the first person about the same work and a later trackback calling it an article posted on the site owner's blog. The probable author is the co-presenter posting under the owner's account. This is the one entry here where the handle byline and the document disagree. THE DROPPED COMMENTS ALSO CARRY THE MECHANISM: 2,781 characters were removed, including the fullest account anywhere of how the applet is cached and then rebound, a capability the article never mentions, and a prior-art challenge. A blockquote is broken so a third-party advisory reads as the author's own words, and an emphasis run is broken so the slide-deck link prints with literal asterisks. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "Java Applets and DNS Rebinding". published is empty against a stated Sunday, November 4th, 2007 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:31`

## http://xs-sniper.com/blog/2008/01/08/theres-an-oak-tree-in-my-blog/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE BODY IS TRUNCATED AND THE BYLINE IS WHAT WAS CUT OFF. The file stops after the closing sentence; the very next thing in the source is the post's own byline block, so this is the ONE document in its group that lost its byline entirely and states no author at all - the credit survives only as metadata. The extractor most likely stopped on the unclosed paragraph and malformed list nesting just above. A further 1,370 characters of comments were dropped after that, carrying praise only. The closing line's own emphasis is broken by a trailing space inside the link label, so the document's final sentence prints with literal asterisks. A blockquote is broken, leaving the POST request unquoted, and an escaped placeholder was decoded into a live pseudo-tag that CommonMark swallows. The recorded title is the blog masthead plus a breadcrumb in BOTH frontmatter and body H1 - the publisher's title element is its site name followed by the post title - where the post's own heading, preserved further down the same file, is "There's an OAK TREE in my blog!?!?!". ALL THREE FIGURES recorded an empty response against the DEAD ORIGIN rather than the Wayback replay the text came from, and one of them is the proof shot the closing line points at. published is empty against a stated Tuesday, January 8th, 2008 (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:27`

## http://xs-sniper.com/blog/2010/12/17/will-it-blend/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the site masthead plus breadcrumb in both frontmatter and body H1, where the post's own heading is 'Will it Blend?'. The byline reached the DOCUMENT but never the metadata - the published body itself states 'Posted by xssniper \| Filed in Security' while authors is empty and the file prints 'Author not stated'. The talk deck where the technique actually lives is linked in the body and was never resolved: also_at is empty and there is no linked_document_url, which matters because the records grade rests on this being a pointer page (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:67`

## http://xs-sniper.com/blog/2010/12/22/expanding-the-attack-surface/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the site masthead plus breadcrumb in both frontmatter and body H1, where the post's own heading is 'Expanding the Attack Surface'. The byline was dropped although the source carries a meta div reading 'Posted by xssniper', and the masthead independently gives Billy (BK) Rios. The one-line proof of concept is unfenced and renders as an ordinary paragraph (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:71`

## https://0x999.net/blog/exploring-javascript-events-bypassing-wafs-via-character-normalization

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: of 12 fenced blocks only 4 hold code - three are empty and four hold nothing but the widget's language label - and one holds the control "Show 10 more lines", showing the fuzzer listing is truncated; the surviving listings also carry the line-number gutter fused into each line; re-extract (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: browser-DOM captured and re-extracted; the fenced fuzzer listing still comes through as empty code blocks (the syntax-highlight widget and its 'Show N more lines' control are not populated into extractable text even in the headless DOM), and re-extraction additionally orphaned the digest and tags. Reverted. Fix is extractor support for the code widget, not a re-fetch.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:43`

## https://0xpwn.wordpress.com/wp-content/uploads/2024/09/a-deep-dive-into-openapi-security.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: roughly 60 of the deck's 100 slides yield only the template footer and a slide number, because the substance is in slide images with no text layer. Verified 2026-08-15 by running poppler against the stored object, which returns the same 3,703 characters - so a re-render cannot help; needs pdf-pages and a reader, or OCR (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:84`

## https://4diggers.blogspot.com/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS THE SITE NAME AND THE CAPTURE IS THE BLOG HOMEPAGE. The frontmatter title is the blog's own name, taken from the page title element and the site header; the document's own title, in the post's own heading and matching the citation, is "How to defeat digg.com". The capture is a homepage snapshot that happened to carry the cited post that day, which is why the title and slug came from site chrome; the post's permalink is available for a targeted recapture. The byline and date are both missing although the source states them plainly - a posted-by line naming the handle Digger, and a date heading. The listing is UNFENCED, so the renderer joins its lines and the form-building JavaScript collapses onto one line, and an emphasis run is broken by a trailing space so a heading prints with literal asterisks. THE PAGE'S OWN WORKING EXPLOIT IS NOT IN THE PUBLISHED DOCUMENT - it lives in the head script and the body onload attribute, posting to the target's own endpoint with a story id and ordering parameters; the body text carries only the illustrative bank example. 212 characters of furniture remain at the end, but the line above the cut is the byline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:30`

## https://acmccs.github.io/papers/p799-zuoA.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the body is destroyed - binary garbage and mojibake throughout, with a single unpaired backtick inside the garbage that opens an inline code span the renderer never closes; unusable as archived, needs re-acquisition (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:95`

## https://acmccs.github.io/papers/p957-vissersA.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 250 ligature glyphs read as U+FFFD inside words ("Certi<?>cate", "Cloud<?>are", "WHOIS<?>eld"); the 2026-08-14 poppler re-read recovered 59KB of previously missing text but maps this font's fi/ff ligature to a replacement character; needs a different extractor or OCR (reported 2026-08-14)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:88`

## https://amolnaik4.blogspot.com/2011/03/exploitation-of-self-only-cross-site.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: BOTH RESEARCH PAYLOADS WERE DELETED, and the article is 7.4 percent of the file. The stored source holds two ondragstart handlers; the published file holds zero. The climax payload - a div whose ondragstart sets the drag data to a script tag posting document.cookie to the attacker - publishes as <div draggable="true" ><h3>DRAG ME!!</h3></div>, with the telltale stray space before the closing bracket, so the sentence "By changing the Evil Data ... pointed to the attacker cookie grabbing script" is followed by a tag with nothing in it. The first payload additionally merged into a following Blogger separator div and lost its heading. Escaped markup was unescaped into live HTML and nothing is fenced, leaving unbalanced script tags. Separately the capture is CONTAMINATED: the Blogger Dynamic Views theme loads the whole blog into the DOM, so 17 unrelated posts came in - 7,652 characters of cited article inside a 102,872-character body - including a 2016 CTF walkthrough that dumps a wp-config.php with live salts. steps.wayback already holds a SINGLE-POST capture, so the remedy is available offline (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: a live refetch returns only a 78-character Blogger JS shell (the post now renders client-side), so a plain re-fetch cannot reach the article; the manifest entry was restored. The route is the stored Wayback capture (archive.org was returning 498/503 this session), then acquire --force offline.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:49`

## https://archives.iw3c2.org/www2007/papers/paper555.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE PDF FILE STEM and the body H1 is a synthesised label taken from the citation. The paper's own cover reads "Exposing Private Information by Timing Web Applications", and a SIBLING ENTRY - the author's abstract page on the same citation line - carries that title, so this one takes the (Paper) suffix. The paper is TWO-COLUMN INTERLEAVED throughout: the abstract publishes as "ABSTRACT to respond to HTTP requests. We experiment with two We show that the time web sites take to respond to HTTP types of direct attacks:", and footnotes are spliced into the body mid-page. Plain pdftotext and pypdf both read it in correct order, so this is repairable offline. FOUR ESSENTIAL FIGURES ARE ABSENT with captions only - the timing distributions that are the paper's entire experimental evidence. publisher and published are empty although the paper states its conference, dates and copyright holder (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:98`

## https://archives.iw3c2.org/www2008/papers/pdf/p525-jacksonA.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: publisher and published are both empty although the paper states both - "WWW 2008, April 21-25, 2008, Beijing, China" and "Copyright is held by the International World Wide Web Conference Committee (IW3C2). ACM 978-1-60558-085-2/08/04" - and licence records unknown although the paper carries an explicit distribution statement. The two figure screenshots are absent from the Markdown while their captions survive. DO NOT RE-RENDER: this is a hand import, so acquire --force would skip it, and the extraction is already BETTER than pdftotext, preserving seven real curly-quote pairs the reference tool flattens. The crossdomain.xml sample interleaving with the neighbouring column is source-inherent - pdftotext -layout produces the identical interleave (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:88`

## https://archives.iw3c2.org/www2013/proceedings/p1191.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: ALL NINE FIGURES came out as Caesar-shifted mojibake from two subset-encoded fonts, one shifted +2 and one +3 - ENKEMU for clicks, 4GHGTTGTU for Referrers, WZLWWHUFRP for twitter.com, 0RELOH6DIDUL for MobileSafari, about 40 distinct strings. The chart numbers are lost outright: y-axis values are blank and the x-axis categories collapsed to "aN NaN NaN Na0" where the paper reads ~1k / 1k~10k / 10k~100k / 100k~1M. The set-intersection operator is also dropped from all five probability definitions, so \|U RT\| stands for \|U intersect RT\|. Prose itself is readable but uses 100 precomposed ligature codepoints, so ASCII searches for specific, different, difficult and profile all return zero (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:61`

## https://arxiv.org/abs/1211.4812

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the arXiv abstract page stands in for the paper. The research content is a single 599-character abstract out of 7,198 body characters - about 8 percent - and the rest is arXiv chrome including the whole arXivLabs promo carousel. The paper is linked inside the captured page twice as arxiv.org/pdf/1211.4812. The entry is nevertheless graded class research with reason "carries technique" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:88`

## https://arxiv.org/abs/1309.1416

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the arXiv abstract landing page stands in for the paper, which the tooling never followed - also_at is empty, there is no linked_document_url, and retrieved_from is the same /abs/ URL, although the document itself twice offers [View PDF] for arxiv.org/pdf/1309.1416 and records the submission as 508 KB. The abstract is whole and supports the citation, but roughly 4,100 of the 8,557 characters are arXiv site chrome and the paper is not preserved. Remedy: acquire the linked paper PDF (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:53`

## https://blackhat.com/archive/europe/2017/briefings.html#breaking-out-hsts

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the Black Hat Europe 2017 Briefings INDEX standing in for the single talk cited at 2016-17.md:113, Breaking Out HSTS (and HPKP) on Firefox, IE/Edge and (Possibly) Chrome; the citation URL is the index URL with a jump anchor, so the citation needs repointing rather than re-fetching (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:113`

## https://blackhat.com/presentations/bh-dc-09/Marlinspike/BlackHat-DC-09-Marlinspike-Defeating-SSL.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: two anchor tags publish as live markup on the sslstrip rewriting slide, and because their attributes are curly-quoted CommonMark reads them as unquoted-attribute open tags, so on a pass-through renderer both vanish and the rest of the paragraph is swallowed into an unclosed link. SCOPE NOTE for a maintainer, not a document fault: the recorded title promises null-prefix certificates, but the word null occurs ZERO times in this deck and zero times in pdftotext of the same PDF - that material is from the later Black Hat USA talk, and the parenthetical comes from the citing list rather than the document (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:98`

## https://blackhat.com/presentations/bh-dc-09/Sutton/blackhat-dc-09-Sutton-persistent-storage.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THREE CODE LISTINGS GO LIVE BECAUSE THE FILE HAS ZERO FENCES. The deck's HTML examples publish unfenced, so an h2 renders as a real heading, an anchor as a live link and a div as an UNCLOSED element; worse, the Paymo client-side SQL injection proof - two script blocks carrying db.execute('SELECT * FROM __DOJO_STORAGE') - sits at two-space indent, below the four Markdown needs, so the whole proof is swallowed into a live script element and never displays. Roughly seven slides are raster-only screenshots with no images map, so the Sony persistent client-side XSS and Paymo csSQLi evidence is unpreserved. publisher and published are empty although every slide reads Copyright 2009 Zscaler, Inc. DO NOT RE-RENDER FOR THE FONT DAMAGE: the roughly 297 damaged tokens (Ques9ons, Twi+er, Sejngs) come from a broken ToUnicode map in the source PDF and pdftotext reproduces every one - this archive's render is already the better of the two (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:108`

## https://blackhat.com/presentations/bh-jp-08/bh-jp-08-Kaminsky/BlackHat-Japan-08-Kaminsky-DNS08-BlackOps.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: an UNCLOSED script tag sits mid-paragraph in the body - the slide text reads "codebase commonly loaded via an external <script src>". This repository's renderer escapes all prose so the archived PDF is safe, but any consumer using an ordinary Markdown or HTML pipeline would have it swallow the remaining 588 lines. publisher is empty although slide 1 states "copyright IOActive, Inc." Note also an inconsistency in the PDF path worth knowing: this deck carries NO page markers at all while a sibling in the same collection has all nine. Two slides are absent from the Markdown and both are image-only, which is faithful (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:89`

## https://blackhat.com/presentations/bh-usa-07/Feinstein_and_Peck/Presentation/bh-usa-07-feinstein_and_peck.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: BACKTICKS INSIDE AN OBFUSCATED PAYLOAD ARE DELETED BY THE RENDERER. The highly-obfuscated JavaScript slide contains nine literal backticks, and because the listing is unfenced the renderer pairs them into inline code spans and removes the delimiters - so six backtick characters disappear from the substitution alphabet and from the encoded blob, and the PDF and website copies decode to something different from the malware the paper is about. The alphabet IS the finding. A blockquote is also broken where the malware's split closing tag puts a quote marker at column 0. Separately three bar-chart slides publish their ROTATED AXIS LABELS as roughly 530 lines of one and two-character fragments, which the renderer joins into vast run-on paragraphs and which the binary-stream gate does not catch because the fragments are letters; and the two tool screenshots and three demo screenshots are absent, so five slides publish as a heading with nothing under them. published and publisher are empty although every slide footer states Black Hat USA 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:104`

## https://blackhat.com/us-16/briefings.html#crippling-https-with-unholy-pac

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the Black Hat USA 2016 Briefings INDEX standing in for the single talk cited at 2016-17.md:78, Crippling HTTPS with Unholy PAC; the citation URL is the index URL with a jump anchor, so the citation needs repointing at the talk slides or whitepaper rather than re-fetching (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:78`

## https://blackhat.com/us-17/briefings.html#the-adventures-of-av-and-the-leaky-sandbox

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - this is the Black Hat USA 2017 Briefings INDEX, 118 talk abstracts and a roster of ~230 speakers, in which the cited talk occupies about 3,300 of 225,688 characters between two unrelated talks; the citation URL at 2016-17.md:111 IS the index URL with a jump anchor, so re-fetching reproduces this page - the index itself names the real targets us-17-Kotler-The-Adventures-Of-Av-And-The-Leaky-Sandbox.pdf and its -wp.pdf, neither archived (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:111`

## https://blog.assetnote.io/2022/10/28/exploiting-static-site-generators/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the document is truncated mid-listing, stopping at " const svgReg = /" right after "As long as the SVG file matches the following regex", so the SVG payload, the Next.js material and the disclosure are absent; re-acquire the article (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2022.md:50`

## https://blog.chromium.org/2011/07/using-cross-domain-images-in-webgl-and.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. Of 17,871 published characters, 17,453 - 97.7 percent - are the blog label sidebar: 229 list items from $200K to "yerba beuna center for the arts". Published counts of CORS, crossOrigin, texture and origin-clean are all ZERO, while the stored raw HTML holds CORS x20, crossOrigin x10, texture x10 and the complete ~2,300-character post. Same cause as the mindedsecurity autocomplete entry: the extraction chooser preferred the raw candidate because it counted 1,348 words, every one a label name. The byline was dropped with the article - the store carries "Posted by Eric Bidelman, Developer Advocate" TWICE - and authors instead records the platform handle @ChromiumDev. Note for the re-render: the missing article contains img.onload handlers, exactly what the handler-stripping defect eats (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:47`

## https://blog.isec.pl/xss-fun-with-animated-svg/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every ASCII 01-32 character was stripped from the printed payloads, which is precisely what the article demonstrates - the base64 block at line 208 decodes to the intended payload and proves the loss - and the HTML-entity example at line 190 is now byte-identical to the plain one at line 172, so it silently illustrates nothing; re-acquire (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2020.md:56`

## https://blog.pspaul.de/posts/bench-press-leaking-text-nodes-with-css/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the publisher's code-block line-number gutter is published as content - bare numbers in their own fenced blocks, or fused into each code line - so listings as published are wrong to copy ("1POST /product/lookup_agg HTTP/1.1", one run of 206 consecutive numbers); re-extract with the gutter excluded (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:27`

## https://blog.teddykatz.com/2019/11/23/json-padding-oracles.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: both code listings are absent - the file ends on "Exploit code (prints out the flag after a couple minutes):" with nothing after it, and refers to a server.js that was never published; the stored source shows both were GitHub gist embeds that the capture route dropped, and a superscript exponent was also lost (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2019.md:24`

## https://blog.voorivex.team/cloudflare-image-proxy-as-a-cspt-gadget-a-cross-origin-cspt-exploit

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the opening paragraphs are missing, so the body starts mid-argument on an unbound antecedent ("When that input can alter the request path...") and later back-references text that is not in the file ("As mentioned, in CSPT the attacker's input is placed into one of the request paths"); re-acquire (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:97`

## https://blog.voorivex.team/javascript-functions-overload-confusion

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the opening paragraphs are missing, so the body starts mid-argument on a back-reference with no antecedent ("That is why a small postMessage challenge on Twitter caught my eye ... the exact confusion I had been abusing in the wild"); re-acquire (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:50`

## https://blog.wired.com/27bstroke6/2008/05/man-allegedly-b.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the stored source names the writer immediately after the headline, in a contributor span reading "By Kevin Poulsen" with his mailto address beside it; the thirteen other "Posted by" hits on the page are comment footers and were correctly not taken. published is empty although the same block states May 27, 2008. The one image failed with UnidentifiedImageError and still points at the dead blog.wired.com host, but it is a decorative stock cartoon rather than evidence and its caption survives. Worth recording that the extraction otherwise went RIGHT where siblings went wrong: the chooser rejected the high-link recall candidate in favour of precision, and the document was taken from a Wayback snapshot rather than the live 404 redirect (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:31`

## https://blog.xss.am/2023/08/cve-2023-39968-jupyter-token-leak/

- Outcome: `broken-capture`
- Kind: advisory
- Reason: faulty capture: all 17 images point at the site's lazy-loading placeholder svg rather than the figures; the real paths survive in the alt text, so a re-extract that reads the alt attribute or the wrapping link target should recover them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:96`

## https://blogs.adobe.com/psirt/2008/09/clipboard_attack_update.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title in frontmatter and body H1 is the blog masthead plus the post title, where the post's own heading is "Clipboard attack update". The byline was dropped although the source carries a post-footer reading "Posted by David Lenoe on September 19, 2008", and published is empty despite the same line. 78 characters of publisher boilerplate remain at the end. The body itself is byte-for-byte the whole post - it is genuinely short, not truncated (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:22`

## https://blogs.securiteam.com/index.php/archives/166

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: EVERY CODE LISTING IN THE DOCUMENT IS AN UNFENCED INLINE CODE SPAN, so the worm's source renders as single joined lines - and THE WORM'S DELIBERATE LINE BREAKS ARE THE EVASION ITSELF, splitting the filtered keyword across lines that the browser reassembles. They now render as two spaces. TEN COMMENTS AND A PINGBACK - 2,829 characters - were dropped, and ONE OF THEM IS THE CLEAREST STATEMENT OF THE TECHNIQUE ON THE WHOLE PAGE, naming the blocked keyword and explaining that the browser renders it even with line breaks in the middle. THAT MATTERS PARTICULARLY HERE: the article's own two mentions of that keyword are BLANK IN THE 2006 SOURCE ITSELF - source-inherent, not a converter fault, and NOT fixable by re-rendering - so the deleted comment was the only surviving place the archive named it. A real blockquote around the defacement message was also collapsed to an empty one. The byline is missing although the source names the author, and published is empty although the byline states the date. The recorded title is the publisher's masthead plus the headline; the document's own heading is the headline alone. The images map holds two assets from the ACQUIRER'S PRESENT-DAY MARKETING SITE with zero overlap with the document. CLEARED: every escaped pseudo-tag landed inside a code span and nothing became live HTML (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:66`

## https://blogs.securiteam.com/index.php/archives/604

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS NINE LINES INSIDE AN 18,000-CHARACTER FILE - roughly 93 percent is sidebar and footer, including a comment list of OTHER posts, an author roster, archive rows, a blogroll and 29 category links. A REAL BLOCKQUOTE WAS COLLAPSED TO AN EMPTY ONE, so the quoted third party's finding and the archived author's own commentary are now INDISTINGUISHABLE - the reattribution class seen across 2007 and 2008. The single comment was dropped; it corroborates the finding on another site named in this same collection. The byline is missing although the source links the author's handle and the article signs off with his name and address, and published is empty although the byline states the date. The recorded title is the publisher's masthead plus the headline. The images map holds the same two ACQUIRER MARKETING-SITE assets with zero overlap (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:58`

## https://blogs.securiteam.com/index.php/archives/746

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWENTY-ONE COMMENTS - 7,755 characters - WERE DROPPED WITH NO LABEL OR TRACE, while the comment form and the site footer were KEPT. THE ARTICLE'S ONLY IN-THE-WILD EXAMPLE IS CORRECTED THERE AND NOWHERE ELSE: the owner of the site named as the victim replies that his site was never vulnerable and that the search result came from someone else's query being logged. Two commenters cite PRIOR ART from five years earlier with the reference; another extends the technique to a redirect chain that makes the crawler do the exploitation without indexing the result; another reports the same abuse by a second search engine. The byline is missing although the source links the author's name and the article signs off with it, and published is empty although the byline states the date. The recorded title is the publisher's masthead plus the headline. The images map holds two assets from the ACQUIRER'S PRESENT-DAY MARKETING SITE, captured from the redirect target rather than the Wayback replay, with ZERO overlap with the document (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:71`

## https://bryceboe.com/2010/12/09/ucsbs-international-capture-the-flag-competition-2010-challenge-6-fear-the-ear/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a superscript was FLATTENED into a silently wrong number. The stored source reads 'the username space was 27^6-1 in size'; the published file reads '276-1'. The real value is 387,420,488, and this is the document's only superscript but it carries the argument (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:91`

## https://buganizer.cc/hacking-gemini-a-multi-layered-approach-md

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every figure is missing while the prose keeps handing off to one ("the process would look like this:" and "as you see from the above screenshot" with nothing after them); the argument is carried by diagrams and screenshots that did not come through, so re-acquire with an extraction that keeps them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:52`

## https://bugs.webkit.org/show_bug.cgi?id=15936

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE BUG REPORT AND ITS ENTIRE COMMENT THREAD ARE ABSENT - the disclosure the year list cites. All three extraction candidates were tiny, the best being 55 words, and what survived is the header strip and an attachment table; a bug page whose extraction yields 55 words has failed. THIS ENTRY HAS NO HEALTH BLOCK AND NO CHECK STEP AT ALL, so nothing in the record evidences the document's identity. The attachment table is exploded - a separator row with no cells and rows breaking across lines - so it renders as neither table nor prose. The byline is empty although the captured fragment itself names the patch author; the reviewer named beside him must not be credited. The frontmatter's cited_by line number is stale and disagrees with the manifest. Note the raw object is absent from the store, so a --force would go to the network (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: the page is alive and was refetched, and a container-browser DOM was captured, but extraction still keeps under a third of the 9,230 probed characters - the extractor does not handle the Bugzilla comment-thread page shape, so nothing was published and the old capture stands. The fix is extractor support for Bugzilla threads, not another fetch.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:101`

## https://bugzilla.mozilla.org/show_bug.cgi?id=369814

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED TITLE IS BUGZILLA'S VIEW CHROME - "Full Text Bug Listing" - which is the name of the export format, not of a document. The bug's own summary, present in the body H1 and in the health record, names the protocol and the hazard. The byline is empty although the document's own header table states its reporter and the first comment gives that person's address. published is empty although the body states a reported timestamp. Quoted patch hunks also become blockquotes joined with spaces, so a C++ diff prints as one run-on line. STALE RECORD, worth clearing: a fault-report on this entry says the comment thread is absent, but a later hand import supplied all 149 comments and the thread is complete - the report now misdescribes the document. The payload survives exemplary well: six vectors were byte-compared against the stored source and all six are present (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:7`

## https://bugzilla.mozilla.org/show_bug.cgi?id=370445

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE BUG'S DISCUSSION IS ABSENT - THE CAPTURE IS CHANGE HISTORY AND ATTACHMENT METADATA ONLY. There is no Comment 0, no description and no thread; the body opens mid-attachment-table and consists of update entries, flag changes, summary renames and one patch block, so the technique is inferable only from the title and the diff. THIS IS THE SAME FAULT ALREADY FILED AGAINST ITS SIBLING BUG, where Bugzilla loads comments through a dynamic modal the extractor cannot see - but here content_gap was empty and no fault-report existed, so nothing reached any gap report. The credit that should be recoverable, the researcher named in the bug's own title, is recoverable only by fixing the capture; the names the document does carry are the assignee and reviewers, which correctly do not count. published is empty and the archived text states no date either - every timestamp renders as a relative phrase. Note the raw object is absent from the store, which is a store gap rather than a document gap (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: the page is alive and was refetched (30,774 characters probed), but extraction keeps under a third - the same Bugzilla page-shape limitation proven on the WebKit sibling, so nothing was published and the old capture stands. The fix is extractor support for Bugzilla threads, not another fetch.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:78`

## https://carnal0wnage.blogspot.com/2008/08/owning-client-without-and-exploit.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE CREDIT NAMES THE BLOGGER TEMPLATE DESIGNER. authors records "Ar-themes" and the file prints it as the byline; it comes from a meta Author tag naming the theme author, whose own site is ar-themes.blogspot.com and who is credited again in a carousel script comment - a site-builder credit, which the rules exclude. The real byline is in the same file: a post-author vcard naming dean de beer, corroborated by the author signing his own comment reply "Cheers, Dean". THE DROPPED COMMENT THREAD IS ALSO LOAD-BEARING: a reader asks whether the missing closing braces are deliberate and the author confirms "I tend to leave a little something out... same reason I left out actually obfuscating it" - without it a reader cannot know the listing's CLSID is deliberately mistyped, and two more comments record that it fails on SP3 but works on SP2. The file has ZERO backticks and ZERO fences, so both JavaScript listings sit below the four-space threshold and render as paragraphs - the dropper collapses into prose. published is empty although the source states 2008-08-27 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:13`

## https://code.google.com/p/dominator/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: A GOOD DOCUMENT WAS OVERWRITTEN BY A JAVASCRIPT APP SHELL - the documented --force trap, actually happening. The published body is ONE LINE of 72 characters, "Google Code Archive - Long-term storage for Google Code Project Hosting.", which is the shell of a JS-driven page. The manifest still records the good render (content_sha256 748ab6c6, 1,594 characters, result ok, 2026-08-10T13:07:12) and THAT OBJECT IS STILL IN THE STORE; the published file records a different content_sha256 of 73 bytes, retrieved live at 2026-08-10T15:39:59 - a re-render 32 minutes later followed the redirect, got the shell and published it, and the manifest was never brought into line. The lost 1,594-character extraction holds the project description, the DOMinatorPro 2012 and Firefox 3.6 warnings, the install link, the sponsor image the manifest still lists, and the Project Information block. The title survives correct in the frontmatter but the phrase now appears NOWHERE in the body. REMEDY IS OFFLINE: re-render from the stored 748ab6c6 object. Do NOT re-acquire - the manifest raw_sha256 is missing from the store and the URL redirects to the shell (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:8`

## https://code.google.com/p/puzzlemall/downloads/list

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the title is generic SITE CHROME - "Google Code Archive - Long-term storage for Google Code Project Hosting." is identical for every project on that host and is carried by both the frontmatter and the body H1, with no retitle step on this entry although its sibling got one. Three of the ten link destinations are also broken Markdown, containing literal spaces so a parser truncates at the first one - and those three are exactly the research artefacts (the TSRC presentation, the Session Puzzles whitepaper and its presentation). The year list itself uses the percent-encoded forms (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:53`

## https://coeus.ece.gatech.edu/2008/10/01/DNS_Forgery/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL EIGHT FIGURES ARE ABSENT FROM THE MARKDOWN while their captions remain, and the paper leans on them - "Figure 5 illustrates the following steps" now introduces nothing. There is no images step at all. 275 of 681 body lines, 40 percent, carry four or more leading spaces because the two-column layout is preserved as whitespace rather than reflowed, which also interleaves the columns so a figure caption lands mid-paragraph inside the round-trip-time discussion. published is empty although the paper states CCS '08 and its landing URL is dated 2008-10-01, and the attribution step recorded a publisher that never reached the published file, which still prints "Publisher not stated". The figures are preserved in the archived PDF, so this is a Markdown-completeness fault rather than a loss. Note the landing_sha256 is absent from the store and correctly filed on store-gaps (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:91`

## https://coeus.ece.gatech.edu/articles/domain-z-ieee.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: about 38 percent of the file - 570 lines - is Figure 6 and Figure 7 rendered as 1,543 bullet glyphs across 513 lines plus bare axis ticks, which also splits a sentence from its continuation 580 lines later (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:84`

## https://crypto.stanford.edu/~dabo/pubs/abstracts/webtiming.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAPER'S TWO COLUMNS ARE PUBLISHED AS INTERLEAVED PROSE. The body is layout-preserved two-column text with no fence, so the renderer collapses the runs of spaces and joins the lines, and the opening reads "ABSTRACT to respond to HTTP requests. We experiment with two We show that the time web sites take to respond to HTTP types of direct attacks:" - every page of the paper reads this way in the published PDF and on the website. Repairable offline. ALL FOUR IMAGE FIGURES ARE ABSENT while their captions survive with nothing under them, and those four are the paper's entire experimental evidence - distinguishing valid from invalid accounts, counting hidden albums, detecting whether a user is logged in, and counting items in a shopping cart; only the code listing survives, because it is text. The recorded title is the ABSTRACT PAGE'S breadcrumb rather than the paper's, which reads "Exposing Private Information by Timing Web Applications" on its own cover. published is empty although the copyright block states WWW 2007, May 8-12, 2007, Banff. DO NOT RE-RENDER: the acquire step is deliberately skipped because the cited URL is a 1,343-character abstract stub, and a --force would replace the paper with it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:98`

## https://cure53.de/fp170.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the paper's HEADLINE FINDING is the text that renders wrong. Listing 4's attack vector contains two two-backtick runs, on the lines carrying alt="``onload=xss()", and lines 266 to 279 hold no blank line, so they form one paragraph and CommonMark pairs those runs into a single inline code span that swallows 302 characters of interleaved prose and listing - including the sentence defining mXSS. The backtick mutation is precisely what the paper is about. Fix by fencing or separating the listing. Separately, one glyph is source-inherent and NOT repairable by re-render: line 341 reads \20AC for the inverted-question-mark glyph where the euro sign belongs, and an independent pdftotext of the stored object fails the same way because the PDF embeds it with no usable ToUnicode entry (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:5`

## https://dinaburg.org/data/DC19_Dinaburg_Presentation.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: a broken ToUnicode CMap in the deck maps ft to m, so in a talk ABOUT lookalike domains the text reads microsom.com, Microsom BITS, mic2osom.com and iicrosom.com - 13 occurrences - while the SAME domains appear correctly spelled earlier in the experiment list as microsmft.com, iicrosoft.com and mic2osoft.com. One document spells one domain two ways and a reader cannot tell an artefact from a deliberate bitsquat. Also ti mapped to Y in 27 places (OperaYng, MiYgaYons, IdenYficaYon) and tt to w, giving hwp:// nine times. Separately and ARCHIVE-INTRODUCED, where poppler returns clean ASCII: 13 precomposed ligature codepoints, so traffic, office, staff and identification all return zero on an ASCII search; 186 soft hyphens; and 1,285 bare carriage returns embedded mid-line. Figures lost their numbers, leaving "There are [blank] daily DNS requests per person" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:72`

## https://dl.acm.org/doi/10.1145/2810103.2813633

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: pervasive wrong-character ligature mapping through the whole paper - thorn stands for fi (Thrstname.lastname, our Thndings, conThgurations, signiThcantly, certiThcates), exclamation for ffi (clean tra!c), double-quote for ff (the e"ectiveness, Thve di"erent), O-with-tilde for the apostrophe, O-with-diaeresis pairs for curly quotes and capital eth for the en dash, plus five U+FFFD. Page 9 is published as decoded font-program bytes (lines 190-208, 647 characters) and a second font dump trails the reference list at page 25. Words are also split across the injected page markers. Strongest recapture candidate in the 2015 collection (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:62`

## https://dl.acm.org/doi/10.1145/2810103.2813652

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the archived body is the ACM Digital Library record behind a Premium Access wall, opening Several features on this page require Premium Access with Learn more and Sign in links; of 23,291 characters the research content is a single abstract paragraph and the rest is the bibliography with a Google Scholar link after every entry plus DL chrome. Both images are DL placeholders, the recorded title is the DL page title with the proceedings name appended, and the Contributors block captured only a loader gif so the paper authors are named nowhere (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:66`

## https://dl.packetstormsecurity.net/papers/attack/Aspect_File_Download_Injection.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: EVERY WORD IN THE DOCUMENT IS ENCIPHERED, AND 16 OF 17 PAGES ARE MISSING. The extractor read glyph codes without the font's ToUnicode map, so the body is a consistent monoalphabetic substitution - the attack string publishes as jjjhttpXYYyourcompanyNcomYdownloadYfnzattackNbat-qd-qa-qd-qawordpadjk where the real one is http://yourcompany.com/download?fn=attack.bat%0d%0a%0d%0awordpad, and "Aspect Security" publishes as goacBikdcBRruiF. On top of that, pypdf reads 17 pages and 36,964 characters while the published body is 2,667 characters carrying one page marker, so the attack description, the Java, C#, ASP and PHP vulnerable-code examples, the defences, the About the Author section and all 11 references are absent; the last line is jj. pdftotext -layout reads the archived PDF PERFECTLY, so both faults are extractor-introduced and repairable offline. authors and publisher are unset although the paper has an About the Author section naming Jeff Williams, founder and CEO of Aspect Security and then OWASP chair, and every page footer reads Aspect Security \| www.aspectsecurity.com; published is empty although the PDF creation date is 2008-04-07 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:63`

## https://docs.google.com/View?docid=ajfxntc4dmsq_14dt57ssdw&pli=1

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: FIVE SECTION HEADINGS RENDER WITH THEIR ASTERISKS SHOWING, AND ONE ORPHANED MARKER BOLDS A WHOLE PARAGRAPH. The first heading opens an emphasis run whose partner sits after a blank line, so it never closes; the four that follow put a space after the marker, which also stops emphasis. Because the first run was orphaned it then pairs with a later marker instead, and an entire paragraph of guidance publishes bold. A Question and Answer pair renders mangled with stray asterisks either side. The ActionScript comment block becomes a BULLET LIST, because each of its continuation lines begins with an asterisk. The Document History table has exploded header cells, so no separator row can match and its four version rows print as literal pipe soup, with one row additionally split across a paragraph break. The payload itself is intact and was checked carefully: the researcher's own handler survives, and all four asfunction and javascript vectors are preserved. published is empty although the document states January 2, 2008 and a revision date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:5`

## https://doi.org/10.1016/j.cose.2015.07.004

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG DOCUMENT - the citation is the Elsevier Computers and Security journal article 10.1016/j.cose.2015.07.004, but what is archived is the two-page IEEE POSTER abstract, whose body opens Poster: Man-in-the-Browser-Cache. The stored source is a 2-page pdfTeX file dated May 2015, i.e. the poster, not the July 2015 journal paper - so the manifest decision claiming the complete publisher PDF was supplied by hand does not match what is in the tree. It also has dropped ligatures throughout (certicate, we nd, trafc, difcult, sufcient) and a bogus fourth page of decoded stream bytes against a 2-page source (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:77`

## https://doi.org/10.1109/SP.2019.00049

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the extraction stops after page 1 of a 15-page IEEE S&P paper - there is one page marker and the file ends mid-word on "signif-" - and the surviving fragment also carries dropped ligatures (lters, ndings, nancials, veried) and a glyph mapping that renders the authors email braces as the letters f and g; re-acquire from either mirror already recorded in also_at (sefcom.asu.edu or docs.apwg.org) (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2019.md:75`

## https://download.vusec.net/papers/glitch_sp18.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the Markdown extraction stops after page 1 of a 16-page IEEE S&P paper - page 2 holds only figure axis labels and page 3 is the fragment DEFG - and the surviving page carries heavy dropped-ligature damage (rst, signicantly, insufcient, ips, efcient) with no precomposed glyphs; the published PDF is byte-identical to the stored 728,105-byte raw object whose page tree reads /Count 16, so an offline re-extract from the store is the safe remedy (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:73`

## https://dsecrg.blogspot.com/2011/12/excel-formula-injection-in-google-docs.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL TEN FIGURES ARE THE SAME PLACEHOLDER, and the run reported success. Every screenshot resolves to one object - 1,678 bytes, 200x200, JPEG despite .png URLs - which is Google's grey warning-triangle 'image unavailable' graphic, while steps.images records "10 of 10 image(s) preserved". The post is screenshot-driven, ten of about fourteen content blocks, so the visual evidence is entirely gone. The byline was also dropped although the body states "Here is a short story from @_chipik and @asintsov", and the recorded title carries the blog masthead where the post's own title is just "Excel formula injection in Google Docs" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:56`

## https://ethiack.com/info-hub/research/write-once-shell-everywhere-arbitrary-file-writes-into-rce

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: seven fenced code blocks are published empty, including the newline-sled payload and the Kubernetes Job example - the known HTML-comment-wrapped-listing class sanitise.py and extract_html.py were fixed for; re-extract offline with acquire --force (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:61`

## https://fabianmonrose.github.io/papers/morton18.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the body font maps no underscore, so every _ inside an identifier is lost - ngx_http_request_t publishes as ngx http request t and a macro reads #define ngx http get module loc conf(r, module) - and it corrupts URLs too; proved to be the extractor rather than the paper because the figure-layer text kept its underscores, only 4 of 21 surviving and all 4 in figures (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:89`

## https://fahrplan.events.ccc.de/congress/2006/Fahrplan/attachments/1158-Subverting_Ajax.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PAPER'S TWO COLUMNS ARE INTERLEAVED LINE BY LINE, so the abstract alternates with a later section sentence by sentence and several pages cannot be read in order. This is REPAIRABLE OFFLINE: pdftotext's default reading-order mode de-interleaves the same stored bytes cleanly - it is only the layout-preserving mode that reproduces the interleave. All five figure captions survive with nothing behind them, and three of them are the attack diagrams. published is empty although the paper states its month and year. CROSS-LINK GAP: the year list cites this paper and the conference programme page on the same line, both are archived, and also_at is empty on BOTH - the only linkage is that the programme page's body happens to contain this paper's filename. Note no format-word suffix is needed here: the programme page already disambiguates itself with its event-code prefix (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:85`

## https://fahrplan.events.ccc.de/congress/2006/Fahrplan/events/1602.en.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED DATE IS THE SCHEDULE PAGE'S GENERATION DATE, NOT THE TALK'S. published is taken from a Dublin Core meta tag stamped when the conference system rendered the page, over a month after the event; the talk is day three of a congress that ran 27 to 30 December 2006. That wrong date is what produced this entry's slug prefix, filing a 2006 talk under 2007. The recorded title also carries the event-code prefix from the page title; the document's own title is "Subverting AJAX", with the subtitle naming next-generation vulnerabilities in Web 2.0 applications. Cleared for the record: the records grade is CORRECT here - the extraction is sound and the page genuinely points at a whitepaper, which it links and which is preserved as a link (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:85`

## https://flattsecurity.medium.com/finding-an-unseen-sql-injection-by-bypassing-escape-functions-in-mysqljs-mysql-90b27f6542b4

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Medium capture dropped every embedded gist and screenshot, so code the prose introduces is absent ("The following code is an example of a vulnerable snippet" followed straight by "The code above seems secure at first sight"), and two img payloads lost their onerror handler mid-string; re-acquire from the recall extraction, which the manifest records as holding the images (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:26`

## https://fortbridge.co.uk/research/vesta-admin-takeover-exploiting-reduced-seed-entropy-in-bash-random/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every figure is missing while the prose keeps handing off to one ("the process would look like this:" and "as you see from the above screenshot" with nothing after them); the argument is carried by diagrams and screenshots that did not come through, so re-acquire with an extraction that keeps them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:64`

## https://fscked.org/blog/fully-automated-active-https-cookie-hijacking

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE FIVE-STEP METHOD PUBLISHES AS FIVE EMPTY BULLETS FOLLOWED BY FIVE ORPHAN PARAGRAPHS. The source is an ordered list with each step in its own paragraph; the extraction emitted the bullets and the text as separate blocks, so the reader sees five empty list items and then five detached paragraphs, and an ORDERED list introduced by "The steps are as follows" has become unordered. 57 characters of furniture remain at the end, and the raw page title line carrying the site masthead sits at the head of the content. published is empty although the source states "Submitted by mikeperry on Thu, 08/14/2008" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:93`

## https://fscked.org/projects/cookiemonster

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAGE WAS TAKEN FROM A DOMAIN-TAKEOVER SITE. The stored HTML is a WordPress rebuild whose navigation is Korean gambling SEO and whose footer reads "Fscked (c) 2026" - the same takeover the maintainer's own decision note describes on this reference's SIBLING, which was repaired by taking a 2008 Wayback capture. This one was left on the takeover host, snapshot is empty, and the prose has been re-typeset by the new owner, so the archive holds a third party's reprint rather than the original. The body also opens with three lines of the new owner's furniture, including a date widget reading "last updated 4 December 2024" sitting exactly where a reader looks for the document's date. published is empty although the source states Mon, 08/04/2008 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:93`

## https://fscked.org/talks/ActiveHTTPSCookieStealing.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the deck's own cover slide reads "Defcon 2007" while the PDF keywords record "Defcon 16, dc-16", which is 2008, and the maintainer's own decision note calls it the DEF CON 16 media copy - the cover text was carried over from the earlier talk, and a reader has no way to tell which year they are looking at. published and publisher are both empty although the cover states a year and the PDF creation date is 2008-06-30. 49 of 143 body lines carry four or more leading spaces from the slide layout. The extraction itself is sound: pdftotext returns an identical word set, and this archive's copy is the BETTER of the two, preserving bullets and curly quotes that pdftotext mangles (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:93`

## https://gist.github.com/hackermondev/68ec8ed145fcee49d2f5e2b9d2cf2e52

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all eight screenshots point at GitHub signed private-user-image URLs carrying a five-minute expiry, so they were dead five minutes after capture and resolve to nothing now; the path segments keep the object names, so rewriting to the durable user-images host may recover them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:90`

## https://github.com/musana/fuzzuli

- Outcome: `broken-capture` (no bytes stored)
- Kind: repo
- Reason: faulty capture: only LICENSE was preserved, the repository README.md (504,657 bytes at commit 91b0ed22fb38, present in the store) was skipped so the document never says what the tool does; re-render the repository capture to include README.md (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: re-acquired from the stored git at commit 91b0ed22fb38, and README.md was skipped again - it is 504,657 bytes, over the repo route's MAX_BLOB_BYTES=256KiB cap (refslib/repo.py), so only LICENSE is preserved. The fix is a repo-route blob-size policy change with tests, not a re-fetch; the git bytes are held, so it is an offline re-run once the cap is lifted.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2023.md:69`

## https://gwern.net/doc/cs/js/2013-stone.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: multiple fonts mis-decoded at a +3 codepoint shift, so 77 tokens across 62 lines are unreadable - 7KHIROORZLQJFRGH... for "The following code...", EURZVHU·VOD\RXW for "browser s layout" - plus 44 x for Wingdings bullets, 4 q for checkmarks, 2 w for the minus sign inside code. Page 17 publishes a decoded font stream as prose. PAGES 27 AND 28 ARE ABSENT, which is the entire Works Cited list [1]-[19], and the page ORDER is wrong: 8, 9, 15, 18, 20, 22, 23 are emitted after page 26. Body H1 also still reads "# 2013 stone", the mirror PDF file stem - the decision.title override reached the frontmatter and the slug but never rebuilt the body heading. REPAIRABLE OFFLINE: raw_sha256 ad49079c IS present in the store (437,530 bytes) and pdftotext -layout on it yields 62,062 clean characters across all 29 pages with no shift and the full bibliography, against 49,772 published. Route: pdf-text then import --redo --only (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:7`

## https://hackademix.net/2007/08/07/java-evil-popups/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline is empty although the stored source carries a rel=author element and the published document itself states "By ma1" with a link to that author's posts - a handle, which counts. published is empty although the source states an explicit machine-readable timestamp of 2007-08-07. Note the block carrying that byline sits below the document's Credits section, so it must be recorded before any tail is cut. Eighty-four reader comments were dropped; they qualify the post's claim that the window cannot be closed, by naming keyboard and task-manager routes and a multi-monitor limitation (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:25`

## https://hackademix.net/2007/09/26/cross-browser-proxy-unmasking/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: BOTH CODE LISTINGS ARE MISSING AND THE LEAD-INS DANGLE. The lines "Here's the ActionScript code:" and "And here's the Perl server:" are each followed by an UNEXPANDED WORDPRESS SHORTCODE rather than the code - the plugin that rendered them is gone from the live site, so the capture preserved the shortcode text. The two resources are pbp.as and pbp.pl on the author's demo host, and the stored Wayback replay recorded in the wayback step predates the plugin's removal, which is the obvious recovery route. The byline is empty although THE PUBLISHED DOCUMENT ITSELF states "By ma1" with a link to that author's posts - a handle, which counts. The body also carries mojibake: two registered-trademark signs publish as a stray capital A-circumflex pair, while other special characters on the same page are correct, so the decode is partial. Note the raw object is absent from the store, which is a store gap rather than a document gap (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:39`

## https://hackerone.com/reports/3511792

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the report's comment thread - which is the document for a disclosed HackerOne report - came through as empty entries (a reporter link followed by a lone "."), leaving about 700 characters of technique; recapture (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:62`

## https://hal.inria.fr/hal-01652021

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the archived document is the HAL-Inria BIBLIOGRAPHIC RECORD, 4,055 characters with no full text and whose only non-search link is the reference list rather than the paper; the archived PDF is a render of the record page and also_at is empty; 2016-17.md:106 cites the record itself, so the citation must move to /hal-01652021/document before the paper can be archived (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: hal.inria.fr, inria.hal.science and hal.science all serve an Anubis proof-of-work wall to non-browser clients on both the record and /document routes, so the linked paper cannot be fetched; a linked-document acquisition captured the wall page and was reverted. Retry when the wall lifts, or import the paper by hand (a third-party GitHub mirror exists but was not used for provenance reasons).
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:106`

## https://i.blackhat.com/BH-USA-25/Presentations/USA-25-Barnett-Lost-In-Translation-Exploiting-Unicode-compressed.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 121 of the 285 non-blank body lines are nothing but the conference footer "#BHUSA @BlackHatEvents" and most of the rest is bare repeated slide labels, because the talk's substance was in screenshots the text layer does not carry; needs pdf-pages and a reader, or OCR (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:8`

## https://i.blackhat.com/BH-USA-26/Presentations/CoreBreak_BlackHat2026_FINAL.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: ti/tt ligatures render as wrong characters inside words ("virtualiza?on", "Founda9ons", "Ques%ons", "AXack", "AcLons"). Poppler was tried on 2026-08-15 and reproduced the SAME forms, so the fault is the source PDF's own ToUnicode table rather than our extraction; a re-render cannot fix it and rewriting quoted source text is not allowed - needs the publisher's corrected PDF or OCR (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:94`

## https://i.blackhat.com/USA-20/Wednesday/us-20-Klein-HTTP-Request-Smuggling-In-2020-New-Variants-New-Defenses-And-New-Challenges.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the final extracted page is decoded binary noise published as prose, and text was dropped mid-slide throughout so sentences begin inside a word (page 13 opens "-Length header.-Length headers?"), with the slide titles for Variants 1 and 2 missing entirely while the status slide reports on all five; inter-word spaces are also lost; the entry is additionally recorded as type Whitepaper when the document is the Black Hat USA 2020 slide deck; re-extract and correct the kind (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2020.md:22`

## https://ieeexplore.ieee.org/document/8418635/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 1,144 decoded symbol-font characters across 53 lines are published as prose, interleaved into the two-column abstract and clustered around the Figure 1 to 3 captions, so the diagram labels are unreadable dingbats; prose is unaffected (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:72`

## https://img2.helpnetsecurity.com/dl/articles/CrossSitePrinting.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PROOF OF CONCEPT WAS DELETED AND THREE PAGES WERE FABRICATED. The central demonstration - the image tag aimed at the printer's port that fires the print job - is GONE from the published body, which prints only "printer:9100/". At least seven more runs of text were deleted the same way, every one of them starting at a straight-quote or angle-bracket boundary: "the browser will request the 'image' resource" publishes as "the browser will request the resource", and a whole clause vanishes from "Since data on a GET request is URL encoded the first line will run off the page because there isn't a line break", which publishes as "Since data on a GET request is break." Surviving form and escape sequences show the loss is SELECTIVE, not wholesale. BOTH pdftotext AND pypdf read every one of these correctly from the same stored PDF, so this is extractor-introduced and repairable offline. Separately the PDF has FIVE pages and the body publishes EIGHT page markers: pages 6, 7 and 8 are decoded stream noise published as prose, and they slip the binary-stream gate only because each is shorter than its character threshold. Three screenshot figures are lost, leaving three dangling lead-ins - "Results in the printer outputting:", "So how about an advertisement for frogs?" and "Which prints the following in Courier 20." are each followed by nothing. published and publisher are empty although the cover states 11/23/2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:8`

## https://img2.helpnetsecurity.com/dl/articles/JavaScript_Hijacking.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: NINE UNCLOSED SCRIPT TAGS CAN ERASE THREE QUARTERS OF THE PAPER. The published file contains eleven script openings against two closings. Two are balanced blocks that a sanitiser removes; the other nine sit in RUNNING PROSE - the first is the sentence "The attack works by using a <script> tag to circumvent the Same Origin Policy", where a conforming renderer opens a real script element and swallows 9,469 characters to the next close: the summary, the introduction, the client listing, the HTTP request and response, and the exploit itself. A later one has no close anywhere after it and takes 13,947 characters to end of file. Of 31,348 characters, roughly 23,400 can vanish. Separately 91 lines of listings are unfenced, so 69 paragraphs swallow 415 lines - the longest run is the 36-line framework comparison table, which collapses into one blob - and the footnotes are interleaved mid-sentence and mid-listing by reading order, so two of them are injected INTO the middle of a code listing. The survey figure is absent while its caption and two references to it remain. published is empty although the cover page states March 12, 2007 in plain text (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:102`

## https://iseclab.org/publications/wondracek2010a_practical/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the two halves of the pair are different documents. The Markdown is the landing page - 913 characters of title, two download links, an author list, a venue line and a BibTeX block, with no abstract and no paper - while the archived PDF IS the genuine 16-page paper, 109,053 characters of extractable text. The tooling did resolve the PDF, and also_at and the paper and pdf steps record it; only the Markdown was never rebuilt from it, so a reader of the .md gets a stub. The title also carries the site-chrome prefix in the frontmatter and header while the in-body H1 is correct, and both download links carry an unclosed bold marker from a FontAwesome icon element (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:86`

## https://jeremiahgrossman.blogspot.com/2006/01/my-tv-interview-with-abc-news-au.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE INTERVIEW IS THE CITATION AND IT IS GONE. The article ends on "Here is a shortened version of the 45min show. Enjoy." followed by nothing; the source carries TWO embeds at exactly that point, an empty anchor to one video service and an object element for another, and both are absent from the published file. Recording the identifiers so they are not lost: the video service document id -3991413321998606355, and the video id 5lhTJaT1gmo. 1,256 characters - 27 percent of the file - are furniture at the end, including a present-day author biography on a 2006 post. The date is missing although the source states it in a machine-readable timestamp and a date heading. THIS ENTRY IS THE DIAGNOSTIC for its group: it used the RAW extraction candidate, not precision, and the byline STILL did not reach the document - so the converter is dropping this platform's post-author block itself (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:64`

## https://jeremiahgrossman.blogspot.com/2006/08/i-know-what-youve-got-firefox.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The byline and the date were both dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a machine-readable datePublished timestamp. DIAGNOSTIC WORTH KEEPING: a sibling in this same collection used the RAW extraction candidate rather than precision and STILL lost the byline, so the loss is the converter dropping this platform's post-author block, not only the precision candidate cutting the footer - which is why all six entries from this blog lost their dates. The document is otherwise the cleanest of its group - its 47-signature table and its detection function are PROPERLY FENCED and intact, and the handler inside the payload survives because it is a string argument rather than an attribute (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:63`

## https://jeremiahgrossman.blogspot.com/2006/08/i-know-where-youve-been.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE TECHNIQUE ITSELF IS GONE FROM THE NUMBER-FOUR ENTRY OF 2006. The proof of concept's unescaped pseudo-tags were deleted BEFORE fencing, so the fence could not protect them: the line that opens the style block publishes as document.write('') with the tag removed, and three further lines - INCLUDING THE VISITED-LINK COLOUR RULE THAT IS THE ENTIRE MECHANISM - collapsed into the fragment "for (var i = 0; i <>');". The same fault hits the list-writing line later, stripping its anchor and list markup. The archived copy no longer contains the technique it is cited for. RECOVERABLE: the manifest's raw object is absent from the store, but a Wayback object of the same URL IS present and carries the code intact. The byline and date were also lost - this 2006 template predates the markers a byline sweep looks for, but it carries their older equivalents and both are in the source: a date header and a posted-by line naming Jeremiah Grossman. The precision candidate correctly cut a second proof of concept from the sidebar but took the headings and links carrying that byline with it (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: a 2007 Wayback capture WAS obtained and stored (20070206151140, 67,515 bytes, object 77ae63499ced) and extracted WORSE - it puts the blog masthead back into the title ('Jeremiah Grossman: I know where you have been'), adds a comments-and-navigation tail, restores no byline or date, and carries the SAME payload damage. Reverted. CORRECTION to this report: the technique is NOT wholly gone - stealHistory(), getComputedStyle and the rgb(0, 0, 255) visited check are present in the published file and in every capture; what is destroyed is only the style-block writing lines (document.write with the tag removed, and the collapsed 'for (var i = 0; i <>);' fragment). That damage is the converter decoding the author-escaped markup and then stripping it, so NO recapture can fix it - it needs the extractor fix, and the 2007 object is in the store ready for an offline re-run once that lands.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:8`

## https://jeremiahgrossman.blogspot.com/2006/09/how-to-get-linked-from-slashdot.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE REPRODUCTION URL CANNOT BE COPIED. The demonstration URL is unfenced across four lines, so the renderer's paragraph rule injects spaces mid-URL and the reader cannot use it. A RESOURCE WAS ALSO DROPPED: the source wraps the demonstration in an anchor with EMPTY LINK TEXT, which the converter discarded, and that href is the COMPLETE URL including the two parameters the article then discusses snipping - the visible copy in the body stops short of them, so the article discusses removing something the reader cannot see. The byline and the date were both dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a machine-readable datePublished timestamp. DIAGNOSTIC WORTH KEEPING: a sibling in this same collection used the RAW extraction candidate rather than precision and STILL lost the byline, so the loss is the converter dropping this platform's post-author block, not only the precision candidate cutting the footer - which is why all six entries from this blog lost their dates. Note the four-line wrap itself is source-authentic and not a mid-token split. Grading note: this is graded records on a word count, for a post that carries a five-step reproducible technique (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:56`

## https://jeremiahgrossman.blogspot.com/2006/11/browser-port-scanning-without.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THREE LISTINGS ARE UNFENCED AND ONE OF THEM IS THE EVIDENCE. The attacker's web-server log block joins into a single paragraph, so the up-versus-down timing contrast that the whole technique rests on is destroyed - the reader sees the request, the epoch and the verdict run together on one line. The two HTML lines and the three iframe lines are likewise unfenced. The byline and the date were both dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a machine-readable datePublished timestamp. DIAGNOSTIC WORTH KEEPING: a sibling in this same collection used the RAW extraction candidate rather than precision and STILL lost the byline, so the loss is the converter dropping this platform's post-author block, not only the precision candidate cutting the footer - which is why all six entries from this blog lost their dates. Cleared: the asterisked pseudo-tags here are the author's own de-fanging convention (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:50`, `2007.md:14`

## https://jeremiahgrossman.blogspot.com/2006/11/bypassing-mozilla-port-blocking.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The byline and the date were both dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a machine-readable datePublished timestamp. DIAGNOSTIC WORTH KEEPING: a sibling in this same collection used the RAW extraction candidate rather than precision and STILL lost the byline, so the loss is the converter dropping this platform's post-author block, not only the precision candidate cutting the footer - which is why all six entries from this blog lost their dates. The document is otherwise entirely clean - no listings to fence, no furniture to cut, and its one figure is correctly preserved. Grading note: this is graded records on a word count, for a post that carries an original technique - the handler-specific bypass of the browser's port ban - so the grade is a length artefact rather than a pointer page (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:29`

## https://jeremiahgrossman.blogspot.com/2007/03/i-still-know-where-youve-been-without.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 1,223 characters - 26 percent of the whole document - are furniture at the end, and they are ANACHRONISTIC: the block runs from post navigation through an Atom subscribe line to a biography written after 2022, which mentions a corporate acquisition and a car club on a 2007 post. NEITHER EXTRACTION CANDIDATE WAS RIGHT: the article is a single 733-character paragraph, so the precision candidate would have cut it and the raw candidate pulled in the masthead, the post navigation and that biography - and the records grade was then computed from the raw candidate's link count. published is empty although the source states a 2007-03-01 timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:24`

## https://jeremiahgrossman.blogspot.com/2007/05/intranet-hacking-take-2-for-bh-usa-2007.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE FIGURE'S STORED BYTES CANNOT BE MATCHED TO THE BODY. The image was preserved successfully, but the publisher's proxy token in the body's URL differs from the token the images step recorded - the platform reissued it between the image run and the re-acquire, hours apart - so the lookup fails and the reader gets a placeholder while the stored bytes sit unused. The image here is furniture-grade, but the mechanism would lose a real figure elsewhere. The byline is empty although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and the sibling entry in this same collection already credits him. published is empty although the source states a 2007-05-25 timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:42`

## https://jeremiahgrossman.blogspot.com/2008/03/fun-with-cups.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DETECTION PAYLOAD WAS DELETED AT ACQUIRE, AND THE POINTER-PAGE RULE THEN GRADED THE REMAINS. The source reads <* img src="http://localhost:631/images/navbar.gif" onload="alert('CUPS is listening')" onerror="alert('No CUPS')"> and the published file reads <* img src="http://localhost:631/images/navbar.gif" > - both handlers gone. The author wrote the leading <* precisely so browsers would show the payload as TEXT, and a sanitiser stripped on* attributes from a construct that was never a live element. The post is cited as "CUPS Detection" and that line IS the detection. The loss is in the stored CONTENT object too, so it happened at acquire, not render. rule:pointer-page then recorded "only 307 words of prose and 13 links, so it points at research rather than carrying it" and graded it records - the extraction deleted the two attributes that made the post carry technique, and the grade was computed on what was left. The byline was dropped although the source carries a rel=author vcard naming Jeremiah Grossman, and published is empty although the source states 2008-03-19 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:18`

## https://jeremiahgrossman.blogspot.com/2008/09/i-used-to-know-what-you-watched-on.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: HTML TAGS ARE PUBLISHED INSIDE THE TITLE. The frontmatter title, the body H1 and the attribution line all read "I <i>used to</i> know what you watched, on YouTube" with literal markup, and the rendered PDF shows the angle brackets escaped and visible; the document's own title is "I used to know what you watched, on YouTube" - the emphasis is styling, not text. The slug inherited the tags too. The proof was also dropped: a line reading exactly "Video:" is followed by the resumption "Clever eh? :)", where the source carries a Vimeo embed, and the clip id 1762861 is recorded nowhere. The byline was dropped although the source carries a rel=author vcard naming Jeremiah Grossman, and published is empty although the source states 2008-09-18 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:21`

## https://jeremiahgrossman.blogspot.com/2008/10/clickjacking-web-pages-can-see-and-hear.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DEMO WAS DROPPED. The body reads "I recorded a quick and dirty clickjacking video demo with my version having motion detection built-in" and the source carries a Vimeo embed after it; the published file has nothing, and the clip id 1912736 is recorded nowhere - on a disclosure whose whole claim is that a webcam and microphone can be turned on by one click, the video was the evidence. The byline was dropped although the source carries a rel=author vcard naming Jeremiah Grossman, and published is empty although the source states 2008-10-07. Note the archive already made exactly this correction for a 2009 post on this same blog (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:8`

## https://jeremiahgrossman.blogspot.com/2009/06/results-unicode-leftright-pointing.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: FOUR OF THE SIX XSS VECTORS WERE DESTROYED BY ENTITY RE-DECODING. The author deliberately double-escaped his vectors so readers could see them, and noted in the article that the spaces before the semicolons are there on purpose; the extractor unescaped the outer entity and then leniently decoded the semicolon-less numeric reference into its glyph, so the source's eight double-escaped sequences publish as zero. The angle-quotation vectors now read as bare glyphs with orphaned semicolons and the author's own note about them reads as nonsense; only the two percent-encoded vectors survived. The vectors are unfenced at column 0. Two quoted passages, 3APA3A's email and Arian Evans's words, lost their blockquotes and read as the author's own prose. The byline and date were dropped although the stored source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and a date-header of Thursday, June 04, 2009 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:50`

## https://jeremiahgrossman.blogspot.com/2010/02/converting-unimplementable-cookie-based.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE SANITISER ATE THE AUTHOR'S PLACEHOLDER ELEMENT AND DESTROYED BOTH ILLUSTRATIONS. The source writes a literal xss element twice, unescaped, as its placeholder; the published file contains zero of them. So the sentence that should read 'the resulting page would read something like, "Hello <xss>."' publishes as 'Hello .', and the line that should read 'Cookie: username=<xss></xss>' publishes as 'Cookie: username=' with nothing after the equals sign. The post has only those two illustrations and both are now empty. No event handler was involved - this is the same decode-then-strip path eating a whole element - and fencing those two lines would have preserved them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:19`

## https://jeremiahgrossman.blogspot.com/2010/07/i-know-who-your-name-where-you-work-and.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the document ENDS ON A DANGLING LEAD-IN - the last line is the bold words 'Video Demo' with nothing after it, and the stored source continues with the Blogger video iframe that was dropped. The byline was dropped too: the source carries a post-author vcard with rel=author and schema.org markup naming Jeremiah Grossman, and the extraction chose the precision candidate over recall, taking the footer and the byline with it. The form listing is unfenced and glued to the preceding image link with no separator, so the author's deliberately defanged tags collapse into one run-on paragraph. Note the store hazard: the manifest raw_sha256 is absent from the store, so this must not be re-rendered with --force (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:7`

## https://jeremiahgrossman.blogspot.com/2010/07/in-firefox-we-cant-read-auto-complete.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the textbook case. The stored article region has two handlers - the proof of concept button whose onclick calls fillAutoComp(), and a Blogger image onblur. The chrome one was correctly dropped; THE REAL ONE WAS NOT. The published input reads type=button value=Start with no handler, so function fillAutoComp() is defined and NEVER INVOKED while the prose still says "some javascript magic to automatically submit the form. Like so...". The whole listing is unfenced, so its line structure is lost as well (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:7`

## https://jeremiahgrossman.blogspot.com/2011/03/robert-rsnake-hansen-age-34-has-passed.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the stored source carries the full Blogger vcard with rel=author and schema.org markup naming Jeremiah Grossman, so the file prints "Author not stated" on the author's own blog (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:50`

## https://kuza55.blogspot.com/2007/01/more-user-login-detection-via.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PROOF OF CONCEPT LOST BOTH ITS ESCAPING AND ITS INDENTATION. The escaped listing was decoded into live markup - script and style elements now real - and the 224 non-breaking spaces that carried its four, eight and twelve-space indentation collapsed to single spaces. The opening backtick is separated from its closing partner by blank lines, so no code span forms at all and the reader sees a stray delimiter followed by five element tags at column 0. The byline is empty although the stored source carries a post-author vcard with rel=author naming the handle kuza55, and published is empty although the source states a 2007-01-01 timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:61`

## https://kuza55.blogspot.com/2007/03/non-persistent-untraceable-xss-attacks.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWENTY LINES OF THE PROOFS OF CONCEPT WERE DECODED INTO LIVE MARKUP AT COLUMN 0, including four script elements and TWO META REFRESH TAGS - so a conforming renderer not only hides the four listings but fires a redirect. Twenty-eight escaped angle brackets in the source publish as none. The byline is empty although the stored source carries a post-author vcard with rel=author naming the handle kuza55, and published is empty although the source states a 2007-03-30 timestamp (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:69`

## https://kuza55.blogspot.com/2008/02/exploiting-csrf-protected-xss.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PAYLOAD SLOT WAS DELETED FROM THE EXPLOIT LISTING. The author wrote the placeholder as an unescaped pseudo-tag inside a code element - req.send("http://site.com/page.php?csrf_token=our_csrf_token&variable=<xss>", - and the HTML parser treated <xss> as an unknown element and dropped it, so the published line reads ...&variable=", . The one line whose entire purpose is to show WHERE the XSS payload goes now shows nothing there. Separately the eleven-line ActionScript class is a MULTI-LINE SINGLE-BACKTICK span rather than a fence, so the PDF prints it as one run-on and its two // comments swallow the closing braces - anyone copying the listing gets code that will not compile. The byline was dropped although the source carries a post-author vcard with rel=author naming the handle kuza55, and the sidebar gives "Name: Alex Alias: kuza55"; published is empty although the source states 2008-02-22 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:51`

## https://kuza55.blogspot.com/2008/02/exploiting-logged-out-xss.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE SAME PAYLOAD PLACEHOLDER DELETION as this author's other 2008 entry. The source reads req.send("http://site.com/page.php?variable=<xss>", and the published file reads ...?variable=", - the literal <xss> was treated as an unknown element and dropped, and that token IS the injection point of the exploit. The ActionScript listing is also a MULTI-LINE SINGLE-BACKTICK span rather than a fence, so it renders as one run-on line. The byline was dropped although the source carries a post-author vcard naming kuza55 and an About box giving "Name: Alex Alias: kuza55"; published is empty although the source states 2008-02-22 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:50`

## https://kuza55.blogspot.com/2008/02/racing-to-downgrade-users-to-cookie.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE LISTING IS BROKEN INTO ELEVEN PARAGRAPHS WITH LITERAL BACKTICKS SHOWING. The main.php listing is wrapped in a single-backtick span that crosses blank lines, so the span never matches and the renderer prints the delimiter itself - the PDF reads "main.php: `<html> <body> <script> document.domain = document.domain;" - with the code split across eleven paragraphs and every indent gone. The escaped listing was decoded but a line-by-line diff proves nothing was stripped, so the content is complete; it is the presentation that is destroyed. The byline was dropped although the source carries a post-author vcard naming kuza55, and published is empty although the source states 2008-02-22 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:41`

## https://kuza55.blogspot.com/2008/07/cookie-path-traversal.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the source carries a post-author vcard with rel=author naming the handle kuza55, and the sidebar About box gives "Name: Alex Alias: kuza55"; published is empty although the source states 2008-07-04. All six kuza55 references in the manifest are uncredited despite carrying this marker. A dropped comment also carries the only recorded defence - that disallowing parent paths, now the IIS default, prevents it - which the post itself never states (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:40`

## https://lcamtuf.coredump.cx/cross_site_cooking.txt

- Outcome: `broken-capture`
- Kind: code
- Reason: faulty capture: THE BANNER STATES A PROVENANCE THE BYTES DO NOT HAVE. It reads that the document was preserved from the author's own site, but that URL answers 404 and the archived bytes are the author's posting re-hosted by a MAILING-LIST ARCHIVE. WORSE, THIS ENTRY AND ITS SIBLING IN THE SAME COLLECTION ARE THE SAME BYTES: the year list cites a paper and its mirror, and both are archived as the mirror - a line-by-line diff of the two published bodies shows three differing lines, all relative-versus-absolute URL forms. The author's original text is unarchived and no gap records it. ELEVEN URLS INSIDE THE FENCE were rewritten into Markdown link syntax that renders LITERALLY, and this is a plain-text advisory whose exact line wrapping is the artefact - every line carrying a URL is now the wrong length. The masthead image and its link are ROOT-RELATIVE and resolve against the archive, showing nothing. A FOLLOW-UP POSTING BY THE SAME AUTHOR is linked by a bare relative target and is not archived. published is empty although the message header states the date. The entry is also classified as code when it is an advisory (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:86`

## https://mazinahmed.net/uploads/Evading%20All%20Web-Application%20Firewalls%20XSS%20Filters.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the ENTIRE BODY is scrambled font-encoding output - only 23 of 2,495 alphabetic tokens are recognisable English, so line 70 reads ouble UDbRRDLoE and line 104 URoLEnulcddlibg. REPAIRABLE OFFLINE: the stored source raw_sha256 c38f62eb (19 pages) extracts perfectly with pdftotext -layout, returning EVADING ALL WEB-APPLICATION FIREWALLS XSS FILTERS and clean prose - the archive extractor ignored the PDF ToUnicode mapping. The author is also unrecorded because his name survives only in scrambled form: the cover reads MAZIN AHMED \| MAZIN@MAZINAHMED.NET \| @MAZEN160 and every page footer repeats Mazin Ahmed (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:38`

## https://media.blackhat.com/bh-us-11/Johansen/BH_US_11_JohnasenOsborn_Hacking_Google_WP.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the published text is materially worse than the stored bytes, and pdftotext on the same PDF is clean, so the damage is EXTRACTOR-INTRODUCED. Thorn stands for fi five times (the Thorn-le manifest.json, identiThorn-ed) so ASCII file returns 0 against 4 in the source; sharp-s stands for fl; and a bare exclamation mark stands for an apostrophe 38 times (Google!s, isn!t) including every indent in the JavaScript listing. All five pages publish as five run-on lines with 33 word-joins (OSTaking, InsideKyle, IntroductionGoogle, manifest.jsonWhen), both JavaScript examples inlined into prose with zero backticks in the file, and page 5 publishes binary fragments where the stored page extracts to nothing. The recorded title is the citation's wording; the paper's own title is "Hacking Google Chrome OS - Taking Down the Browser from the Inside" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:55`

## https://media.blackhat.com/bh-us-11/Sullivan/BH_US_11_Sullivan_Server_Side_WP.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: pages 8 to 11 publish decoded FONT STREAMS as prose and the document ends on binary noise, the last non-empty line being "..!". The page count is fabricated: the stored PDF has exactly 7 pages and 11 embedded font files, and each font was counted as a page. Six section headings are destroyed outright by subset fonts with no ToUnicode map, emitting raw glyph codes from 0x21 - the headings for Denial of Service, File System Access, Execution of Binary Files, NoSQL Injection, Blind NoSQL Injection and Conclusions and Mitigations are all unreadable. 45 further substitutions come from MacRoman bytes decoded as CP1252 (curly quotes as O-acute and O-grave, ellipsis as E-acute, JaegerMonkey as JS-caron), and 17 apostrophes publish as exclamation marks (user!s, doesn!t, attacker!s). Every code listing is unfenced. This is the defect commit e7ed20f targeted, still live here (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:67`

## https://media.blackhat.com/bh-us-12/Briefings/Forshaw/BH_US_12_Forshaw_Are_You_My_Type_Slides.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the page count is fabricated - 115 page markers against a stored PDF of 112 pages, so every marker after 33 is shifted and the References slide labelled 114 is really page 112. Three pages publish binary garbage where the PDF has readable text (+--222222222222222222-./$' and similar). The body H1 is still the PDF FILE STEM, "BH US 12 Forshaw Are You My Type Slides" - the 2026-08-12 title correction reached the frontmatter but never rebuilt the body heading. Around 50 slides of C# and a WinDbg register dump publish unfenced as running prose, and each slide's bullets are joined into one line (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:91`

## https://media.defcon.org/DEF%20CON%2034/DEF%20CON%2034%20presentations/DEF%20CON%2034%20presentations/DEF%20CON%2034%20-%20Aviv%20Donenfeld%20-%20LGTM%20Bypassing%20an%20LLM%20Build%20Gate%20When%20Prompt%20Injection%20Fails%20-%20LGMT%20v2.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: only 46 of the deck's 110 pages are present (the stored PDF declares /Count 110; the document ends mid-talk at "## Page 46", before the payoff its own agenda promises). Poppler was tried on 2026-08-15 and returned NO text at all - the PDF is image-only, so pdf-text and acquire --force cannot help; needs pdf-pages and a reader, or OCR (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:101`

## https://media.defcon.org/DEF%20CON%2034/DEF%20CON%2034%20presentations/DEF%20CON%2034%20presentations/DEF%20CON%2034%20-%20Ji%27an%20Zhou%2C%20Lei%20Lu%20-%20One%20Chain%20to%20Own%20Them%20All%20-%20Breaking%20AI%20Infrastructures%20-%20azraelxuemo%20v3.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the 143-slide deck yielded only title text - 148 of 351 non-blank lines are a bare page number and slide headings stand with nothing under them, because the substance is in screenshots the text layer does not carry; needs pdf-pages and a reader, or OCR (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:91`

## https://medium.com/@renwa/client-side-path-traversal-cspt-bug-bounty-reports-and-techniques-8ee6cd2e7ca1

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Medium capture dropped every embedded gist and screenshot, so code the prose introduces is absent ("The following code is an example of a vulnerable snippet" followed straight by "The code above seems secure at first sight"), and two img payloads lost their onerror handler mid-string; re-acquire from the recall extraction, which the manifest records as holding the images (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:61`

## https://misc0110.net/files/timers.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: every NUMERAL was dropped from prose, headings and code - W C for W3C, Amazon EC for EC2, 68 empty citation brackets, every figure table and section number gone, and appendix code reduced to Uint Array ( b u f f e r ) and while ( ); the loss is selective so the document still reads while every cross-reference in it is unrecoverable (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:93`

## https://mizu.re/post/under-the-beamer

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: HTML entities inside a code listing were decoded, so the article's escapeHtml function now reads as replacing every character with itself and contains the invalid JavaScript token '"""'; re-extract keeping entities inside listings (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:23`

## https://mlfbrown.com/malicious.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: font damage, gibberish and code-listing damage together - readable but wrong throughout; needs re-extraction with a correct encoding map (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:91`

## https://nathandavison.com/blog/exploiting-email-address-parsing-with-aws-ses

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the article is entirely about how parsers read crafted email addresses, and Cloudflare email obfuscation replaced EVERY address with the placeholder [email protected] - 66 occurrences, including the whole per-language results table, so the document no longer contains its own subject; re-acquire from a source without Cloudflare email obfuscation, e.g. a Wayback snapshot (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2020.md:31`

## https://nealpoole.com/blog/2011/10/java-applet-same-origin-policy-bypass-via-http-redirect/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Pygments LINE-NUMBER GUTTER publishes as a fenced code block containing only "1 2 3 4 5", with three orphan pipe rows around it, debris from the source's highlight table. Two block quotations also lost their quoting - a bare angle bracket then the quoted text as an ordinary paragraph - so the Wikipedia same-origin definition and the Mozilla advisory read as the author's own words. The recorded title carries the site masthead in both the frontmatter and the body H1. The byline was dropped although the sidebar states "My name is Neal Poole" and the post is first person throughout (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:11`

## https://octagon.net/blog/2022/05/29/bypass-csp-using-wordpress-by-abusing-same-origin-method-execution/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the HTML listing the argument depends on published EMPTY (lines 138-140 are a blank html fence) with a second empty promise at line 146, and none of the three figures the prose calls for were preserved; re-acquire the article (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2022.md:53`

## https://opnsec.com/category/flash/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the archived body is the blog CATEGORY LISTING /category/flash/, four post teasers each cut off mid-sentence and ending in a Read More link, about 2.3 KB of excerpts with no article content; the correct title is already in cited_title as Advanced Flash Vulnerabilities, and if more than one part is archived the parts own numbering must disambiguate them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:12`

## https://pastebin.com/f7ac1cced

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: TWO INNER BACKTICKS INSIDE THE PAYLOAD SILENTLY STRIP THEMSELVES. The vector uses backticks as the CSS url delimiter - style=`behavior:url(#default#time2)` - and because the listing is unfenced those two backticks pair into an inline code span and vanish from the rendered vector. The escaped markup was also decoded into live markup, so the anchorclick example renders as an actual link labelled IE8 rather than as code. Seventeen lone hyphen lines remain where the paste's list structure flattened into empty list items. The recorded title is platform chrome plus a bare username, and roughly 1,400 of the 2,384 body characters are Pastebin toolbar furniture, which is also what inflated the pointer-page link count (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:83`

## https://ptolemy.berkeley.edu/projects/truststc/pubs/883.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a bibliography record stands in for the ACSAC 2011 paper - title, authors, abstract and the same citation rendered three times as HTML, plain text and BibTeX. also_at is empty and the tooling did not resolve it; the only document link on the page is an ACM paywall URL, so this one cannot be fixed by following a link and needs another source. Aggravating: the entry is graded research with the reason "carries technique", where a sibling in the same collection was correctly graded records by rule:pointer-page. Publisher furniture also remains at the end - a repository clerk's byline, a webmaster pointer and a site-wide copyright notice, 498 characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:78`

## https://pure.york.ac.uk/portal/en/publications/stealing-pins-via-mobile-sensors-actual-risk-versus-user-percepti/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the archived document is the York PURE RESEARCH-DATABASE RECORD, 4,777 characters with no full text, no PDF link and no author list at all, which is why it prints Author not stated; the archived PDF is a render of the record; 2016-17.md:112 cites the record, so the citation must move to the open-access article behind the DOI (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:112`

## https://repository.root-me.org/Exploitation%20-%20Web/EN%20-%20Expression%20Language%20Injection.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: SECTION 5, "Open Source Examples", IS MISSING - 996 characters present in the archived PDF but absent from the Markdown, and it carries the paper only in-the-wild evidence, the CAS logout JSP and the GBIF login JSP. The phrase survives only inside the table of contents. Page labels are shifted by two as a result. Page 20 publishes a decoded stream fragment as prose, and it is the last line of the document. Headings are welded to the following paragraph throughout ("1. IntroductionThis paper", "2. Related WorkMeder") and tables flatten into unreadable strings; a fresh pdftotext of the stored raw shows none of this, so it is introduced by the extractor. There are ZERO code fences in a paper of Java, JSP, EL and a 40-line JavaScript exploit, so a script tag, a closing script tag, a body onload and an iframe pointing at the victim host all sit live and inline mid-paragraph (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:10`

## https://research.securitum.com/security-analysis-of-portal-element/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a single 5,915-character raw-HTML line swallows two whole sections as HTML rather than Markdown, so the article headline finding RISK 1: Accepting unsafe URI schemes has no heading at all and the heading structure jumps from What is portal to RISK 2; the same line strips onclick=portal.activate() from the visible example, which is the technique itself; the WordPress Crayon highlighter is also extracted twice per listing and never fenced (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2019.md:29`

## https://roberto.greyhats.it/pubs/dimva08-web.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 166 body lines carry four or more leading spaces and 64 more carry one to three, and this renderer has no indented-code branch, so the paper's figures flow into the surrounding prose - Figure 1's PHP listing has no blank line separating it from the paragraph above and merges with it, and Table 1 is space-aligned with no pipes so its columns collapse onto one line. Hyphenation is preserved then space-joined, giving se- quential, exe- cution and par- allel. DO NOT RE-RENDER FOR THE MISSING UNDERSCORES: the embedded typewriter font has no mapping for _, so mysql query, $ POST, GET LOCK and RELEASE LOCK are missing theirs, and pdftotext reproduces every one identically - it is source-inherent. On one construct this archive is BETTER than pdftotext, recovering math angle brackets the reference tool drops entirely (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:90`

## https://scarybeastsecurity.blogspot.com/2009/06/apples-safari-4-also-fixes-cross-domain.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the article is 1,589 characters inside an 8,473-character body; the rest is 390 characters of nav and 5,594 characters of Blogger BLOG ARCHIVE sidebar - 70 month and year rows running 2008 to 2021. Same chooser fault as the tag-cloud cases: the raw candidate was preferred at 7,853 characters and 151 links over the precision candidate at 1,467 characters and 6 links, purely on volume. The byline was dropped with the rest: the source carries a rel=author element naming Chris Evans (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:53`

## https://scarybeastsecurity.blogspot.com/2009/06/apples-safari-4-fixes-local-file-theft.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 84 PERCENT OF THE BODY IS THE BLOGGER SIDEBAR. The real post is 1,174 characters inside a 7,157-character content section; the remaining 5,983 characters are post navigation, an Atom subscribe line and a Blog Archive index of 72 month and year links running 2008 to 2021, and the file ENDS mid-sidebar. The chooser's own record shows the fault: it took the raw candidate at 7,436 characters and 150 links over the precision candidate at 1,059 characters and 5 links, and precision was the correct extraction. The byline went with it - BOTH stored captures carry it, the live one as a post-author vcard with rel=author naming Chris Evans and the 2010 Wayback copy as "Posted by Chris", and the blog description reads "Hacking everything, by Chris Evans". Bookkeeping: the published file was re-rendered a day after the manifest's own render step and the manifest was never updated, so its raw_sha256 and content_sha256 both disagree with the frontmatter; both object pairs are in the store and both contain the sidebar, so the re-render did not cause this (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:54`

## https://scarybeastsecurity.blogspot.com/2009/06/bonus-safari-xxe-only-affecting-safari.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the real post is 901 characters of HTML, about 640 of text, and the published body is 9,143 characters of which 5,594 - 61 percent - is the Blogger blog-archive sidebar, 142 lines of year and month links running 2008 to 2021. The file ENDS inside that sidebar. The chooser's own record shows why: precision offered 785 characters and 2 links, recall offered 7,080 characters and 147 links, and it took the sidebar. Both the content and raw hashes in the frontmatter also disagree with the manifest, and the byline was dropped although both stored captures name Chris Evans (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:52`

## https://scarybeastsecurity.blogspot.com/2009/12/generic-cross-browser-cross-domain.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: both code blocks were emitted with SINGLE-backtick delimiters, which do not fence, so each pair collapses into an inline code span inside the surrounding paragraph. The source bold highlights then became asterisks INSIDE those spans, so they render literally while the next sentence tells the reader "the interesting pieces are highlighted in bold". An escaped ampersand entity was also double-decoded to a bare ampersand. The byline was dropped although the source carries a rel=author element naming Chris Evans - note the Chris Evans inside the stolen-subject demo data is his own test account, and the byline element is the evidence (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:79`, `2010.md:46`

## https://seclab.cs.ucsb.edu/publications/nikiforakis2013cookieless_monster/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a publication-record stub stands in for the paper, and it carries NO ABSTRACT - verified against the stored source object, which holds only nav, title, two download links, an author list, venue, BibTeX and a footer. Total source content 1,198 characters, most of it the BibTeX block; no prose, no technique. The entry is nevertheless graded research with decision.reason "carries technique", which the document contradicts. The real PDF is named in the body: seclab.cs.ucsb.edu/files/publications/Nikiforakis2013Cookieless_monster.pdf. Title also carries the site masthead "SecLab - ", which the slug embeds, so a corrected title rebuilds the slug. Remedy: acquire the linked paper PDF (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:57`

## https://seclab.stanford.edu/websec/csrf/csrf.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PAPER'S TWO COLUMNS ARE INTERLEAVED LINE BY LINE THROUGH ALL THIRTEEN PAGES. The extraction preserved the print layout as literal lines, so left and right columns alternate and the renderer's paragraph join produces sentences like "ABSTRACT of attention [14], and the effective mitigation of SQL injec- Cross-Site Request Forgery (CSRF) is a widely exploited tion through parameterized SQL queries" - the entire body reads this way, and the charts survive only as axis labels scattered through the soup. pdftotext WITHOUT the layout flag reads the same stored PDF in correct single-column reading order and de-hyphenated, so a clean re-extraction is available offline and the --force trap does not apply here because the raw object is present. The archived PDF is a byte-identical copy of the original and is unaffected; only the Markdown and everything built from it are scrambled. publisher and published are both empty although the paper's own footer states "CCS'08, October 27-31, 2008, Alexandria, Virginia, USA" and "Copyright 2008 ACM" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:85`

## https://seclab.stanford.edu/websec/frames/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE CITED PAPER WAS NEVER FETCHED, AND THE POINTER-PAGE RULE KEPT IT OFF EVERY GAP REPORT. This is a project index; the year list cites the paper it links, and that paper is not in the manifest - nor are the two other publications the page lists. also_at is empty, linked_document_url is unset, content_gap was empty, and rule:pointer-page graded it records, so the reference published cleanly while the research it stands for has no local copy. This is the same trap as the 2008 vendor page. The byline is empty although the page states the authorship of everything it lists, and published is empty although it dates the cited work. The three publication entries are shredded into image-link rows, loose prose and orphan table cells (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:101`

## https://seclists.org/fulldisclosure/2006/Jan/943

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THIS ENTRY AND THE PAPER ENTRY ARE THE SAME BYTES, LINKED ONE WAY ONLY. A line-by-line diff shows the two published bodies differ by THREE LINES, all relative-versus-absolute URL forms - and the manual-source note states plainly that the "paper" was itself fetched from this mailing-list archive. So the year list's paper and its mirror are archived twice as the mirror, while the author's original site answers 404 and its text is unarchived, with content_gap empty on both entries so the gap reaches no report. The paper entry points here via also_at; THIS entry's also_at is empty, and linked_document_url is unset on both. Separately the thread marker was decoded into live markup and renders invisible - the casualty is furniture, not the advisory, which is fenced and intact. 207 characters of thread navigation remain at the end, and that block NAMES A FOLLOW-UP POST BY THE SAME AUTHOR WHICH IS NOT ARCHIVED, so it is worth keeping visible rather than cutting blind. published is empty although the message header states the date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:86`

## https://seclists.org/isn/2006/Sep/67

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE RECORDED AUTHOR IS THE MAILING LIST'S POSTING ACCOUNT, not a person - the rules exclude a publisher account, and the document names its actual author plainly two lines into the body, with his masthead. published is empty although the document carries TWO dates, the message header's and the article's own. 96 characters of thread navigation remain at the end. Note the original news article this re-posts is not archived, but its full text is quoted in the message, so nothing is lost (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:59`

## https://seclists.org/securecoding/2007/q2/0

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE MANIFEST ADVERTISES BYTES THE PUBLISHED FILE WAS NOT MADE FROM. A later wayback step rewrote the manifest's raw_sha256 to point at a 2013 replay, while the document was rendered days earlier from the live copy that steps.acquire and the frontmatter both still name. Both objects hold the same article, so no content is at stake, but the provenance record is wrong. 742 characters of thread-navigation furniture remain at the end, carrying links to follow-ups but no technique. published is empty although the message states its Date header. Note the whitepaper this message announces IS archived separately, but neither entry cross-links the other (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:102`

## https://secniche.blogspot.com/2010/11/malware-paradox-cia-aavar-2010.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: extraction failure - the published body is the blog-archive sidebar. Of 7,590 characters, the content is a masthead line, a Blog Archive heading and 67 archive post links with ZERO prose paragraphs; the chooser again preferred the raw candidate for having the most words. UNUSUALLY THE STORE CANNOT REPAIR THIS ONE: the captured post body is only 1,197 characters of HTML whose entire text is a SlideShare caption, because the post is a bare Flash embed. A re-extract would recover one caption line. The resolvable target is in the capture - the deck at slideshare.net/adityaks/aavar-2010-aksrje - and 'Cross Interface', the phrase in the citation title, occurs zero times in the source. The recorded title also carries the blog masthead rather than the post's own (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:63`

## https://secunia.com/advisories/22477/

- Outcome: `broken-capture`
- Kind: advisory
- Reason: faulty capture: 4,293 characters - 40 percent of the file - are furniture at the end, including a reader poll, a banner advertisement and a list of ten UNRELATED advisories that a reader could mistake for related findings. The page's table layout produced 179 pipe rows with no separator row, and an emphasis span straddles a table row and a blank line so both markers render as literal content. Sixteen of the 33 published image references are layout spacers. published is empty although the advisory states it in a labelled release-date field. The empty authors field is CORRECT - this advisory names no discoverer. Worth recording as a POSITIVE: this entry is the good repair in its group - a fault was filed, it was recaptured from Wayback, its stale figure rows WERE pruned, the title was corrected off the acquirer's chrome, and content_gap was cleared; three siblings in this same collection were recaptured without that prune (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:6`

## https://sirdarckcat.blogspot.com/2008/01/exploiting-xss-vulnerabilities-on.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the source carries a post-author vcard reading "Posted by sirdarckcat", and published is empty although the source states 2008-01-03. The document itself is otherwise sound - the ActionScript payload is correctly fenced and the percent-encoded cookie header survives byte for byte. Note steps.acquire.raw_sha256 is absent from the store while the manifest and frontmatter hashes resolve (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:48`

## https://sirdarckcat.blogspot.com/2008/05/browsers-ghost-busters.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH PROOFS OF CONCEPT WERE DELETED AND THE POST NOW POINTS AT NOTHING. The source carries two javascript: bookmarklets as real links; the published file has zero, and zero rewrites to # either - the anchors were removed outright and only their link TEXT was kept as prose. So the article reads "go here and then use this code" with the code gone, and "it does work on IE7 on windows gotten from open()" as flat text where the working exploit used to be. The first of the two also contained a handler inside the researcher's own payload, document.onkeydown=hey, the keystroke capture that is the point of the post. The byline was dropped although the source carries a post-author vcard reading "Posted by sirdarckcat", and published is empty although the source states 2008-05-11. Note steps.acquire.raw_sha256 is absent from the store while the manifest and frontmatter hashes resolve (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:47`

## https://sirdarckcat.blogspot.com/2008/10/about-css-attacks.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE BYLINE WAS LOST BECAUSE THE FILE WAS BUILT FROM THE WRONG CAPTURE. The manifest's own raw_sha256 is the Wayback object, which carries a post-author vcard reading "Posted by sirdarckcat"; the published frontmatter names a 2026 live object whose byline span is EMPTY, and that is the one the render used, so authors is empty. Both objects are in the store, so the fix needs no fetch. Two emphasis runs are also broken - the source pads its bold tags with spaces, so "overflow:hidden" and "border: 0" publish with literal asterisks around them rather than as bold - and one link is split across a line break with its opening bracket alone on a line, while another leaves a trailing slash outside the link target. All three sirdarckcat entries in this collection carry an empty authors, so the loss is systematic (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:48`

## https://sirdarckcat.blogspot.com/2009/08/our-favorite-xss-filters-and-how-to.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DOCUMENT WAS RENDERED FROM THE 2026 LIVE PAGE WHILE A 2009 SNAPSHOT SITS IN THE STORE. Three raw_sha256 values disagree - the manifest names the 2009 Wayback copy (present), steps.acquire names a third hash (absent) and the published frontmatter names the 2026 live fetch (present) - and sources lists no capture at all. The date was dropped with the rest of the Blogger metadata: the source carries a date-header reading Tuesday, August 04, 2009 while published is empty. The byline is recoverable and was not taken: the post gives the author's own address sird@rckc.at in its body and the title element names sirdarckcat. The one command line renders as prose, unfenced at one-space indent (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:12`

## https://soroush.me/blog/cross-site-url-hijacking-by-using-error-object-in-mozilla-firefox

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the file ENDS ON AN UNTERMINATED LINK - the last line opens a bracket whose text runs across two blank lines and never closes, because the converter dropped the next-post card's href. The body is also a pointer page: four short paragraphs, with the actual research in a PDF offered on two now-dead hosts and also_at empty, so the landing page was never resolved. A live copy exists at packetstormsecurity.org/papers/general/xsuh-firefox.pdf, which another document in this same collection cites as a reference. Publisher furniture remains at the end, 342 characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:58`

## https://soroush.me/blog/mongodb-nosql-injection-with-aggregation-pipelines

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the publisher's code-block line-number gutter is published as content - bare numbers in their own fenced blocks, or fused into each code line - so listings as published are wrong to copy ("1POST /product/lookup_agg HTTP/1.1", one run of 206 consecutive numbers); re-extract with the gutter excluded (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:114`

## https://soroush.me/downloadable/iis-semicolon-report.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE TEXT IS EXPLODED ONE WORD PER LINE - 456 of 466 non-empty body lines hold a single token, each its own paragraph - and the page count is FABRICATED, nine page markers inserted into a two-page PDF, breaking mid-sentence between "versions" and "successfully". Worse, EVERY in-body hyphen was deleted, so semi-colon, Aria-Security, switch-case, select-case, alpha-numerical and iis-semicolon all lose theirs: the recommended construct now reads "switch case or select case" and the report's own URL is silently wrong. URLs are also split mid-token across lines. pdftotext -layout on the SAME stored PDF returns clean laid-out prose in 3,449 characters and 77 lines, so this is extractor-introduced and repairable offline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:10`

## https://speakerdeck.com/shhnjk/piloting-edge-copilot

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: 6,926 characters - 28 percent of the file - are two Speaker Deck carousels advertising other people's decks, sitting between the header and the transcript; re-extract without the recommendation rails (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:59`

## https://storage.googleapis.com/google-code-archive-downloads/v2/code.google.com/puzzlemall/Session%20Puzzles%20-%20Indirect%20Application%20Attack%20Vectors%20-%20May%202011%20-%20Whitepaper.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the title is the PDF FILE STEM with its hyphens spaced, reaching both the frontmatter and the body H1, where the document's own cover reads "Session Puzzles" over "Indirect Application Attack Vectors". It has a same-title sibling in this collection, the PuzzleMall project page, so a correction wants the (Whitepaper) disambiguator. Eight captioned diagrams publish with NO images at all although the source PDF holds 17 image XObjects, nine of them JPEG - and this is a diagram-driven whitepaper. Three page-number footers also publish wrong, 10, 20 and 30 rendering as 11, 21 and 31 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:53`

## https://teamusec.de/publications/conf-ccs-fahlhmsbf12/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Markdown half of the pair is the publication landing page - title, authors, venue, abstract and BibTeX, 2,777 characters - while the PDF half is the genuine 12-page CCS 12 paper with no archive banner. So one slug publishes two different documents and the paper survives only on the PDF side. The entry is graded depth full, class research, reason "carries technique", where the same rule classified comparable siblings as pointer pages. authors also records only Sascha Fahl where the document names six: Marian Harbach, Thomas Muders, Matthew Smith, Lars Baumgaertner and Bernd Freisleben were dropped, only the linked name surviving (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:86`

## https://thomas.stacey.se/posts/CRLF-Powered-Desync-Attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the capture is the pre-talk teaser and Black Hat abstract, not the write-up - 1,902 characters with no case studies, payloads or code; a live re-fetch on 2026-08-15 returned byte-identical bytes (raw_sha256 unchanged), so the author has not published the write-up at this URL - watch the post, and meanwhile the research is archived from the PortSwigger article and the Black Hat slides cited alongside it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2026-ai.md:35`

## https://tinyurl.com/5w6koqj

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: authors is empty although the document has an explicit "About the authors" section naming FIVE writers - Ory Segal, Omri Weisman, Adi Sharabani, Yair Amit and Lotem Guy - so the file prints "Author not stated". Seven superscript footnote markers were also flattened into the base text, and one of them changes a number: the source reads "AppScan Standard Edition software v8.0" followed by footnote 6, and it publishes as v8.0.6. The paper's central vulnerable example is published unfenced at column 0, so a renderer swallows the one code sample the argument is built on, and Figure 4's taint-trace screenshot is absent although its caption remains (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:21`

## https://trinhmt.github.io/home/S3/ccs14-trinh.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the star glyph is emitted as a question mark throughout - 17 occurrences of Z3-str-? where the paper spells out Z3-str-star three lines earlier - and it also hits the rule names [REP-?], [HT-?], [TL-?] and, more damagingly, the Kleene stars inside regex membership constraints, where ? and * mean different things. A fidelity loss, and not the ligature shape, so a ligature sweep will not catch it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:70`

## https://ucla-sec-lab.netlify.app/publication/2024-authsaber/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the archived page is the publication landing stub, not the paper - its Abstract heading is followed only by an ellipsis and three lines are the site theme's unedited demo placeholders, leaving 1,859 characters of which 866 is an author-bio card; re-acquire from the CCS 2024 paper (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:149`

## https://unit42.paloaltonetworks.com/github-repo-artifacts-leak-tokens/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every figure is missing while the prose keeps introducing one ("consider the following code:", "This is what the patch looks like:", captions Figure 1 through Figure 10 with nothing above them), so the screenshots carrying the evidence are absent; re-acquire with an extraction that keeps them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:76`

## https://vttynotes.blogspot.com/2011/10/cve-2011-3230-launch-any-file-path-from.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: advisory
- Reason: faulty capture: THE EXPLOIT IS INERT. The stored source has the proof of concept as <input type=button value="Launch" onclick="DoIt()">; the published file has <input type=button value="Launch" > - one event handler in the source listing, zero published, with the documented stray space before the bracket. The consequence is that function DoIt() is defined and then NEVER INVOKED anywhere in the published PoC, while the prose still tells the reader "you can run or launch anything you can access by using the method in the html provided below". The method is gone. Mechanism confirmed: the author escaped the whole PoC inside a pre block, extraction decoded the entities into live markup and the handler was deleted - and the damage is already baked into the stored CONTENT object, so a plain re-render reproduces it. The byline was dropped too, although the body states "Found By: Aaron Sigel" and the source carries a Blogger byline naming aaron (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:41`

## https://web.archive.org/web/20070101051946/http://michaeldaw.org/md-hacks/csrf-with-msword/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: PROVENANCE FAULT, AND IT IS A PRE-ARMED --force TRAP: the entry's raw_sha256 names an object that IS present in the store and is NOT this article - it is a DOMAIN-FOR-SALE PARKING PAGE for an unrelated domain, containing neither this article's title nor its text, and THE SAME HASH IS RECORDED ON THIS DOCUMENT'S SIBLING. The published document itself is fine because it came from a hand import, but a --force would find that object present, skip the network, and republish a for-sale page over the article. The byline is correct but published is empty although the document states its date directly below it, November 24th, 2006. TWO figures are BROKEN RELATIVE TARGETS into a browser save-page sidecar folder that does not exist in the repository. 362 characters of trackback and comment-form furniture remain at the end. The exploit itself is fenced and survives unaltered (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:24`

## https://web.archive.org/web/20070102032610/http://michaeldaw.org/md-hacks/backdooring-pdf-files/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: PROVENANCE FAULT, AND IT IS A PRE-ARMED --force TRAP: the entry's raw_sha256 names an object that IS present in the store and is NOT this article - it is a DOMAIN-FOR-SALE PARKING PAGE for an unrelated domain, containing neither this article's title nor its text, and THE SAME HASH IS RECORDED ON THIS DOCUMENT'S SIBLING. The published document itself is fine because it came from a hand import, but a --force would find that object present, skip the network, and republish a for-sale page over the article. The byline is correct but published is empty although the document states its date on the line directly below it, September 13th, 2006. One figure is a BROKEN RELATIVE TARGET into a browser save-page sidecar folder that does not exist in the repository, with empty alt text. Bold markers were also emitted INSIDE a code fence, so the reader sees literal asterisks inside a captured SOAP body, and one prose sentence was pulled inside the fence. 372 characters of trackback and comment-form furniture remain at the end (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:25`

## https://web.archive.org/web/20071124032809/http://ha.ckers.org/blog/20070506/turn-any-page-into-a-greasemonkey-popup/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: The recorded title is site chrome in both frontmatter and body H1, where the document's own title - recoverable with certainty from its own bookmark heading, still present in the body, and matching the year list's citation - is "Turn Any Page Into A Greasemonkey Popup"; the recorded string is the masthead plus an Archive breadcrumb. The body also retains the literal Wayback Machine replay banner as its first content line. 760 characters of furniture remain at the end, and that block is the ONLY place the date appears: Sunday, May 6th, 2007 at 11:07 am. Worth recording as a POSITIVE: alone in its group, this entry's image targets are Wayback replay URLs rather than the dead host, and the size heuristic's two rejections here are correct - both are site chrome (reported 2026-08-15) FURNITURE TRIMMED 2026-08-17: the WordPress post-meta line, the comment-form heading and the taxonomy row are gone; any other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:63`

## https://web.archive.org/web/20080106144155/http://aviv.raffon.net/2008/01/02/YetAnotherDialogSpoofingFirefoxBasicAuthentication.aspx

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS BURIED IN ITS OWN SIDEBAR. The chooser took raw at 10,213 characters with 53 tables and 50 links over precision at 3,587 characters, which was the article; the advisory survives but sits inside about 9,700 characters of blogroll, monthly archive, admin login and disclaimer out of 12,881. 6,111 characters of that furniture are a removable tail. Head chrome includes a duplicated title line and the literal Wayback Machine replay banner. The recorded title glues the blog masthead on AND CUTS THE POST TITLE SHORT - the document's own title is "Yet another Dialog Spoofing - Firefox Basic Authentication", which is also how the year list cites it. The byline was dropped although the source carries a signature naming Aviv Raff, and published is empty although the body states Wednesday, 02 January 2008. Note the slug is derived from a failed Chrome probe and names a browser error page (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:57`

## https://web.archive.org/web/20080519094504/http://aviv.raffon.net/2008/05/14/InternetExplorerQuotPrintTableOfLinksquotCrossZoneScriptingVulnerability.aspx

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 5,896 characters - 46 percent of the file - are blog sidebar furniture at the end. Same chooser fault: raw at 9,789 characters and 50 links beat precision at 2,831 characters and 4 links. The recorded title glues the blog masthead onto the document's own title in frontmatter and body H1, where the post heading is "Internet Explorer 'Print Table of Links' Cross-Zone Scripting Vulnerability". The byline was dropped although the source carries an explicit signature naming Aviv Raff, and published is empty although the body states the date twice. The proof of concept was also decoded out of its escaping and sits unfenced as live markup - it survives intact, defer attribute and all, but belongs in a fence (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:60`

## https://web.archive.org/web/20081014003640/http://aviv.raffon.net:80/2008/05/31/SafariPwnsInternetExplorer.aspx

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 6,846 characters - 52 percent of the file - are blog sidebar furniture at the end, the same Contact Me, blogroll, thirty-row archive, admin login and disclaimer block. Same chooser fault: raw at 10,556 characters and 59 links beat precision at 2,880 characters and 6 links. The recorded title glues the blog masthead onto the document's own title in frontmatter and body H1, where the post heading is "Safari pwns Internet Explorer". The byline was dropped although the source carries an explicit signature naming Aviv Raff, and published is empty although the body states the date twice (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:59`

## https://web.archive.org/web/20090207124101/http://securityvulns.ru/Udocument911.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: SAME MOJIBAKE TITLE AND SAME WRONG CREDIT as its sibling. The windows-1251 title was decoded as latin-1 in frontmatter, body H1 and manifest, and the correct reading is a section label plus the document's own Subject row title, "Code Execution via XSS in Internet Explorer". authors records the site owner 3APA3A from a site-wide meta Author tag where the document's From row and signature both name MustLive. The researcher's payload was also DECODED OUT OF ITS ESCAPING into live markup - seven escaped angle brackets in the source become none in the published file, and a script element and an iframe now sit unfenced at column 0. Both consumers escape on output so nothing executes, but because there is no fence the renderer joins the payload's wrapped lines with a space, and the archived PDF prints ActiveXObject('WScript. Shell') and c.Run('calc. exe') with spaces INSIDE the payload. 786 characters of site furniture remain at the end, published is empty although the document states 24 November 2008, and the manifest raw_sha256 points at a DIFFERENT SNAPSHOT from the one the published document was rendered from (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:71`

## https://web.archive.org/web/20090212215719/http://securityvulns.ru/Vdocument24.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE TITLE IS MOJIBAKE AND THE CREDIT NAMES THE WRONG PERSON. The source declares charset=windows-1251 and the title bytes were decoded as latin-1, so the frontmatter, the body H1, the manifest title and the sources title all read "Uvedomlenie o bezopasnosti" as the byte soup "Óâåäîìëåíèå î áåçîïàñíîñòè"; the same wrong decode runs through the body sidebar and the date line. Even decoded correctly that prefix is the site's SECTION LABEL, not a title - the document's own title, in its own Subject row, is "New vulnerabilities in CapCC for WordPress". Attribution: authors records 3APA3A, who is the list moderator and site owner and appears only in a site-wide meta Author tag and the footer copyright, while the document names the researcher twice in its own words - a From row reading MustLive and the signature "Best wishes and regards, MustLive, Administrator of Websecurity web site". Same shape as the 2009 fault where a vendor spokesperson was credited over the researcher. 786 characters of site furniture remain at the end, and published is empty although the document states its date. Note the slug names nothing in this document and looks inherited from a redirect page, and the manifest raw_sha256 is absent from the store, so this must not be re-rendered with --force (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:73`

## https://web.archive.org/web/20090403024932/http://aviv.raffon.net:80/2008/10/30/ADifferentOpera.aspx

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: HALF THE FILE IS THE BLOG SIDEBAR. 6,902 characters - 50 percent - are furniture at the end, running from a Contact Me row through RSS feeds, an eight-row blogroll including two Hebrew links to a wedding guitarist and a caterer, a thirty-row monthly archive covering 2005 to 2008, an admin login and an employer disclaimer. The chooser's own record shows the cause: it took the raw candidate at 11,277 characters and 61 links over the precision candidate at 3,594 characters and 8 links, and precision was the article. The recorded title glues the blog masthead onto the document's own title in frontmatter and body H1, where the post heading is "A different Opera" and the site name is a SEPARATE element in the source. The byline was dropped although the source carries an explicit signature naming Aviv Raff, so the file prints "Author not stated" and falls back to the hostname; published is empty although the body states the date twice. The article's only figure, the opera:config screenshot, recorded an empty response (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:9`

## https://web.archive.org/web/20101224204903/http://samuli.hakoniemi.net/how-to-conceal-xss-injection-in-html5/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE HAS ONE PAYLOAD AND IT WAS UNESCAPED INTO LIVE MARKUP. The stored source escapes the whole injection - a pushState call that repoints the login form action at an attacker host - and the published file decodes it into a real script element, so a reader sees only "?username=”>" and the proof of concept vanishes into the markup. Separately the figure target is wrong: the manifest preserved the image from the Wayback replay path, 7,966 bytes at 550x225, while the published Markdown points at the bare publisher host, which is the dead origin (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:70`

## https://web.archive.org/web/20110102233032/http://gregorkopf.de/slides_berlinsides_2010.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: FIVE SILENTLY WRONG NUMBERS from flattened superscripts - "at most 232 passwords", "only 232 possible passwords", "costs us 232 memory and 232 time" and "only 232 possibilities" are every one 2^32, and the type-juggling example publishes as 0e66631337 = 0 times 1066631337 where the exponent is 10^66631337. The flattening is inconsistent, since maths superscripts elsewhere kept a space. Identifiers also lost their underscores - t 3 l i b d i v for t3lib_div, TYPO3 CONF VARS, mt srand, php uname - and 48 lines publish one space per glyph, so the listings are unusable as code. A passwd-file dump line beginning with a hash renders as an H1. Note the store hazard: the manifest raw_sha256 is a 9,768-byte Wayback toolbar shell rather than the PDF, so a re-render here is the documented trap (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:73`

## https://web.archive.org/web/20140516030049/http://www.securatary.com/Portals/0/Vulnerabilities/PayPal/Paypal%20Manager%20Account%20Hijack.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: pages 24 and 25 publish decoded font-stream bytes as prose, four screenshot pages render as plus-sign junk, pages 5, 16 and 21 are missing outright, the text carries dropped-ligature damage (ifrst, diferent), and NOT ONE of the figures is preserved - in a document that is a Burp Intruder walkthrough, so the screenshots are the method. Needs re-acquisition (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:30`

## https://web.archive.org/web/20140725134411/http://community.websense.com/blogs/securitylabs/archive/2014/02/14/msie-0-day-exploit-cve-2014-0322-possibly-targeting-french-aerospace-organization.aspx

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all five figures resolve nowhere. The source attribute is a ROOT-RELATIVE Wayback path and it was resolved against the dead canonical host, producing http://community.websense.com/web/<timestamp>im_/... instead of web.archive.org. 13 URLs affected; the 15 that were absolute in the source are fine. Same shape as the-duo-security PayPal 2FA entry, so this is a rewrite and not a re-fetch (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:28`

## https://web.archive.org/web/20141220045918/http://www.tripwire.com/state-of-security/latest-security-news/verizon-wirelesss-customer-portal-exposed-text-messages/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: one image target is the HTML ARTICLE PAGE ITSELF rather than an image file, which the manifest confirms with UnidentifiedImageError for that exact URL, and 0 of 13 images were preserved. 67 percent of the file (10,734 of 15,952 characters) is publisher furniture - categories, tags, a gravatar author box, and 14 related-post teaser cards - leaving roughly 1,400 characters of actual article (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:32`

## https://web.archive.org/web/20160403035045/http://miki.it/blog/2014/7/8/abusing-jsonp-with-rosetta-flash/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: a whole section is missing. The source has an h3 "zlib header hacking" and the archived body jumps from the Flash-parsers figure straight to "ADLER32 checksum bruteforcing", losing the CMF/CINFO/FLG paragraph and the sentence naming the 0x68 0x43 = hC sequence Rosetta Flash always uses, plus two figures. Heading and image diffs confirm this is the only gap: 13 source headings to 12, 11 article images to 9 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:8`

## https://web.archive.org/web/20160403035045/http://www.slideshare.net/dnkolegov/wh102014

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: 49 of the 53 image targets point at the HTML PAGE rather than an image - every one is web.archive.org/.../slideshare.net/dnkolegov/wh102014 - and the manifest already records UnidentifiedImageError for that URL. The CORRECT per-slide JPG URLs DO survive in the file, in the transcript links, so this is a target rewrite rather than a re-fetch. The recorded title is also SlideShares display-truncated string ending in a single f, and 2,239 characters of SlideShare chrome sit MID-document between two content blocks, so no suffix cut reaches it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:14`

## https://web.archive.org/web/20160403035045/https://cwiki.apache.org/confluence/display/WW/S2-020

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH Workaround listings are missing, so the advisory remedy is unusable - one sentence ends "the following dependency needs to be added:" and the next line is a heading, the other ends "add ^class\.* to the list of excludeParams as below" and the next line is the Confluence label row. Confluence code macros did not survive: the file contains zero backticks. Re-filed after direct re-reading on 2026-08-15 confirmed the fault survived the 2026-08-11 re-acquire; steps.fault-report says filed while content_gap had been emptied, which hid it from document-gaps.md (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:12`

## https://web.archive.org/web/20160403035045/https://medium.com/@da/need-a-last-minute-flight-45af88ec8df3

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the body stops at the dangling lead-in "Delta’s response:" - the colon introduces material, in the original a screenshot, that the capture does not contain, and the manifest holds only the two images already used earlier in the post. The entry decision.reason asserts the article is complete; three observations are present but the fourth element the article announces is not (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:43`

## https://web.archive.org/web/20160403035045/https://www.blackhat.com/us-14/briefings.html#call-to-arms-a-tale-of-the-weaknesses-of-current-client-side-xss-filtering

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the Black Hat USA 2014 Briefings INDEX standing in for the single talk cited at 2014.md:21, CTA: The weaknesses in client side xss filtering targeting Chromes XSS Auditor; the citation URL is the index URL with a jump anchor, so the citation needs repointing rather than re-fetching (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:21`

## https://web.archive.org/web/20160403035045/https://www.duosecurity.com/blog/the-paypal-2fa-bypass-how-legacy-infrastructure-impacts-modern-security

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every archived image resolves nowhere because the Wayback path was pasted onto the publishers own host - eight URLs of the form www.duosecurity.com/web/TIMESTAMP im_/... against 16 correctly formed web.archive.org URLs in the same file. The manifest stores those same three figures under the CORRECT host with real bytes and dimensions, so this is a host rewrite, not a re-fetch, and a --force re-render would risk the documented trap instead of fixing it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:25`

## https://web.archive.org/web/20160403035045/https://www.openssl.org/~bodo/ssl-poodle.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 51 SOFT HYPHEN characters stand where ASCII hyphens belong, silently corrupting identifiers for anyone who copies from the page - CVE-2014-3566, the draft-ietf-tls-downgrade-scsv-00 reference, and the L-1 and Ci-1 notation the padding-oracle argument depends on. Invisible on screen. This is a hand import, so a --force re-render would skip it and a title correction would need the import --redo route (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:7`

## https://web.archive.org/web/20170903113359/http://armoredcode.com/blog/bypassing-basic-authentication-in-php-applications/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: 4 of the 8 fenced blocks contain NOTHING BUT the syntax highlighter's line-number gutter - one block is just "1", another "1 2 3 4 5 6 7". Each listing is additionally wrapped in stray one-cell table rows, so every code sample renders as a broken table plus a bogus numbers-only code block. The real code survives elsewhere in the file (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:38`

## https://web.archive.org/web/20170903113359/http://blog.ptsecurity.com/2012/08/not-so-random-numbers-take-two.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all four figures resolve to ONE object of 1,678 bytes at 200x200 - Google's 'image unavailable' placeholder - despite three of the four URLs being 640 pixels wide. The same placeholder stands in for ten figures on the DSecRG Excel-formula entry, so this is a class, not an accident (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:8`

## https://web.archive.org/web/20170903113359/http://seckb.yehg.net/2012/06/using-post-method-to-bypass-ie-browser.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE IS ABSENT. The whole 5,103-character body is the blog masthead, the Wayback toolbar text leaked in as prose, a Labels heading and 43 sidebar navigation links. The real article IS in the store - the post body holds "Up until now, XSS prevention has been built in some popular browsers... Unfortunately, IE does not prevent POST-based XSS" plus two captioned screenshots - and none of it reached the document. Neither screenshot was preserved. The correct URL was fetched, so this is extraction, not navigation (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:49`

## https://web.archive.org/web/20170903113359/http://seckb.yehg.net/2012/06/xss-gaining-access-to-httponly-cookie.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Java applet listing is wrecked. 57 bare hyphen lines are interleaved between every source line, from Blogger's line-number list elements, and the non-breaking spaces that separated tokens were dropped so the code fuses - importjavax.swing.*, publicclass HO extendsJApplet{, publicvoid init(){, newURL(target). A listing of about 1,600 characters is stretched over 209 lines with no code fence. Verified against the store, which reads import then a non-breaking space then the class name (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:13`

## https://web.archive.org/web/20170903113359/http://www.dc949.org/projects/stiltwalker/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title is the SITE NAME, "Defcon Group 949", where the document's own title is "Stiltwalker: Nucaptcha, Paypal, SecurImage, Slashdot, Davids Summer Communication". The body also publishes the source page's PHP failures as prose - two Warning lines naming include(menu.php) and a filesystem path - and both YouTube demo embeds were dropped, leaving dangling lead-ins that promise a video (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:44`

## https://web.archive.org/web/20170903113359/http://www.slideshare.net/andrewpetukhov/no-locked-doors-no-windows-barred-hacking-openam-infrastructure/11

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: 21 of the 23 image targets point at the deck's HTML PAGE rather than an image file, which the manifest confirms with UnidentifiedImageError; only one real slide JPEG survives. 21 lines also begin with an orphan bold opener. Separately the manifest raw_sha256 is absent from the store while the object the file was built from is present, so a --force re-render would go back to the network (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:53`

## https://web.archive.org/web/20170903113359/http://www.slideshare.net/d0znpp/ssrf-attacks-and-sockets-smorgasbord-of-vulnerabilities

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: 37 of the 44 image targets point at the deck's HTML page rather than an image file; only 2 resolve to a JPEG. 4,524 characters of SlideShare furniture also sit MID-DOCUMENT, from "Upcoming SlideShare" through view counts, six commenter profiles and "No notes for slide", which a tail-only trim cannot reach (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:6`

## https://webblaze.cs.berkeley.edu/blancura.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the project landing page stands in for the NDSS 2010 paper, whose PDF is linked in the captured page's own markup and was not followed - retrieved_from is the landing page, also_at is empty, there is no linked_document_url. A sibling in this same collection had exactly this situation resolved correctly, so the tooling can do it. The recorded title is also the HTML title tag with the site-name prefix, in both frontmatter and body H1, where the page's own heading gives the paper's real title (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:97`

## https://webblaze.cs.berkeley.edu/contentsniff.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the recorded title glues the research group's site name onto the page heading in both frontmatter and body H1, where the page's own heading is "Content Sniffing Attacks in Web Browsers" and the cited work's title is "Secure Content Sniffing for Web Browsers, or How to Stop Papers from Reviewing Themselves". This is a project page standing in for an IEEE S&P 2009 paper: the PDF link did survive into the body, but also_at is empty and linked_document_url is unset, so the archive holds a 240-word abstract and not the paper - and rule:default graded that "carries technique". published is empty although the body states Oakland 2009. A BibTex label publishes as bracketed text with no target because its javascript: href was stripped, and one link's syntax is broken across a line (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:103`

## https://webblaze.cs.berkeley.edu/papers/kudzu.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 96 PERCENT TRUNCATED, and fully repairable offline. The body is 5,301 characters with only a page-1 marker, ending mid-word on "th a" from "interacts closely wi/th a"; pages 2 to 18 are absent. Ligatures are DELETED not preserved - rst, nding, signicant, elds, difculty, specication - so ASCII first, finding, significant, fields, difficulty and specification all return ZERO while precomposed ligature codepoints also count zero, which distinguishes deletion from the masking case. LaTeX braces publish as the letters f and g around the author emails. pdftotext on the SAME stored object reads it perfectly: 18 pages, 124,528 characters, the email set intact, first x16, finding x12, significant x13, and zero damaged tokens. The archived PDF is intact too (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:85`

## https://www.adambarth.com/papers/2010/bates-barth-jackson.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: an unclosed script tag swallows the end of the paper. Line 531 is a bare script element at column 0, which opens a CommonMark HTML block whose only terminator is a closing script tag - and there is none after it, so 156 lines and 19,269 characters render as raw text: the rest of the false-positives section, all of the performance section, the conclusion and the entire reference list. It is the only such opener in the file; the other listings sit at sufficient indent and are safe. Separately the extractor introduced 156 lone carriage returns that pdftotext does not emit (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:89`

## https://www.aspectsecurity.com/tools/javasnoop/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: EVERY FIGURE IS DEAD. All 12 published image references failed with UnidentifiedImageError and none has a local copy, while every target points at a domain the maintainer's own decision records as now serving a sale page. Two figure links are additionally broken across lines, with the label on one line and the target on the next. Publisher furniture remains at the end - a services pitch, a spacer image and a contact form, 310 characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:12`

## https://www.assetnote.io/resources/research/insecurity-through-censorship-vulnerabilities-caused-by-the-great-firewall

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: four click-to-expand blocks were captured as their labels only ("Show Fastly Proof-of-Concept Code", "Show Known IPs"), so both proofs of concept and the poisoned-IP and keyword lists the article twice says are attached are absent, leaving a four-word appendix; re-acquire with the disclosures expanded (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:23`

## https://www.blackhat.com/docs/eu-14/materials/eu-14-Hayak-Same-Origin-Method-Execution-Exploiting-A-Callback-For-Same-Origin-Policy-Bypass-wp.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: fi/ff/fl ligatures are missing inside words ("aected", "conguration", "dierent"); re-read with poppler on 2026-08-14, which recovered text but not the ligatures because the PDF's embedded font carries no usable ToUnicode map for them; needs a different extractor or OCR (reported 2026-08-14)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:77`

## https://www.blackhat.com/docs/us-15/materials/us-15-Gavrichenkov-Breaking-HTTPS-With-BGP-Hijacking-wp.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 30 Mac-Roman misdecoded punctuation characters through the prose - the em dash publishes as a capital eth, curly quotes as O-with-diaeresis pairs, the apostrophe as O-with-tilde, the bullet as a yen sign - and the contents page is destroyed by a digit-to-punctuation font map so every page number is junk while the section names survive; the recorded title is the PDF file stem (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:19`

## https://www.blackhat.com/docs/us-15/materials/us-15-Nafeez-Dom-Flow-Untangling-The-DOM-For-More-Easy-Juicy-Bugs.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: Mac OS Roman read as Latin-1 throughout, so ligatures and quotes map to wrong letters inside words - The DOM speciThcation is rich, easily Thnd, tagged with a unique SSag, str.SSag = true, TodayOs web apps, var str = OhelloO (31 U+00D5, 21 U+00D4, 10 U+00DE, 6 U+00DF). Repairable offline: pdftotext on the stored source returns str.flag = true and specification intact. The recorded title is also the PDF file stem where the deck own title on slide 1 is DOM FLOW UNTANGLING THE DOM FOR EASY BUGS, and a junk page 53 trails a 50-page source (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:23`

## https://www.blackhat.com/docs/us-15/materials/us-15-Wang-FileCry-The-New-Age-Of-XXE-ie-wp.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the lowercase letter c is dropped and replaced by a space through most of the body - Abstra t, Mi rosoft x3, Introdu tion, Con lusion - and the damage reaches the payloads themselves, publishing s ript x2 and en oding x4 so the XML and JavaScript examples are not valid. Only 22 lowercase c survive in 10,673 characters. The closing bracket is also mapped to U+2104 nine times INSIDE the payloads, fi/fl ligatures are dropped (exltrate, les, dened), three mojibake remnants survive, and the recorded title is the PDF file stem (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:29`

## https://www.blackhat.com/presentations/bh-europe-07/Sotirov/Presentation/bh-eu-07-sotirov-apr19.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE PDF FILE STEM. Frontmatter and body H1 read the filename with its underscores as spaces; the deck's own title slide reads "Heap Feng Shui in JavaScript", preceded by the kanji the archive correctly recovered where pdftotext drops it. The body also carries a synthesised heading taken from the citation label, which is not the document's title either. Because a sibling entry may take the same corrected title, the repository's format-word disambiguation applies. Five build-up slides also lose their numbering: a pointer glyph before the first item blocks list detection, so the remaining steps restart and render 1, 1, 2, 3, 4. The whole deck loses its line structure, which flattens the library listing. published and publisher are empty although the deck states Black Hat Europe 2007 and names the author's company (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:99`

## https://www.blackhat.com/presentations/bh-europe-07/Sotirov/Whitepaper/bh-eu-07-sotirov-WP.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 774 LINES OF CODE, BACKTRACES AND MEMORY DIAGRAMS COLLAPSE FOR WANT OF FENCES. The file has zero fences and mixes one-to-three-space and four-plus indentation arbitrarily, so 207 paragraphs swallow 774 lines with a longest run of 16. Destroyed: the heap-spray listing, the vtable disassembly, the four-column dereference diagram, the string hex layout, every debugger backtrace, the decompiled allocator, the breakpoint script, the lookaside memory diagram and the fake-vtable byte layout - every one of which depends on column alignment. The inverse also happens: the deeply indented technique paragraphs render as a code block. The library's central demonstration additionally becomes an HTML block that a sanitiser removes outright. One image, the technique's illustration on page 7, is absent with no caption to mark its place. DO NOT CORRECT the misspelling in the exploit credit - it is in pypdf, pdftotext and the published file alike, so it is the author's own. published and publisher are empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:99`

## https://www.blackhat.com/presentations/bh-europe-09/Guimaraes/Blackhat-europe-09-Damele-SQLInjection-slides.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: KIND MISLABEL with a sibling collision. The entry records type Whitepaper and kind whitepaper on a URL ending -slides.pdf, and the deck's own slide 60 says "More acknowledgments and references ON THE WHITE PAPER". The actual whitepaper is a separate archived entry, so the repository's (Slides) and (Whitepaper) disambiguation applies to both. Separately the file has ZERO fences for about 20 code listings, several indented one to three spaces so they render as prose, and two of them lose meaning that way: the UNC paths in LOAD_FILE and COPY render with their backslashes halved (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:81`

## https://www.blackhat.com/presentations/bh-europe-09/Guimaraes/Blackhat-europe-09-Damele-SQLInjection-whitepaper.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: fi/ff/fl ligatures are missing inside words ("aected", "conguration", "dierent"); re-read with poppler on 2026-08-14, which recovered text but not the ligatures because the PDF's embedded font carries no usable ToUnicode map for them; needs a different extractor or OCR (reported 2026-08-14)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:81`

## https://www.blackhat.com/presentations/bh-jp-06/BH-JP-06-Stamos-Lackey.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE CITING LIST'S WORDING, NOT THE DECK'S. The cover page reads "Attacking AJAX Web Applications / Vulns 2.0 for Web 2.0"; two words differ from what is published. The entry is also classified as a whitepaper when it is a 68-SLIDE DECK, so if the title is corrected the archive's format-word convention applies. publisher is empty although the cover and every slide footer name the company, and published is empty although the cover states the venue and date. Every listing is unfenced - 869 lines at four-space indent - so the deck's code survives only by accident of indentation. CLEARED, and worth recording as a POSITIVE: there is NO font damage here and the archive is the CLEAN reading - the reference tool disagrees with it, emitting 478 replacement characters for bullets and dashes the archive reproduces correctly, so do NOT re-render on the strength of a tool comparison. All 68 pages are present, and the unbalanced script tags never produce live markup (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:89`

## https://www.blackhat.com/presentations/bh-usa-06/BH-US-06-Hoffman.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: FOURTEEN OF FIFTY-EIGHT SLIDES ARE FIGURE-ONLY AND THE MARKDOWN CARRIES ZERO IMAGES, so a reader gets a title with nothing under it fourteen times - including the slide whose entire content is the diagram explaining the worm's infection method, and the architecture, case study and attack-surface slides. The 5.7MB PDF preserves them all. Every bullet block is also unfenced with no indented-code fallback, and because the deck's bullet glyphs are not Markdown markers, each slide's hierarchy joins into one run-on paragraph - 146 lines carry four or more spaces of indent that do nothing. publisher and published are both empty although the PDF's own creation date is stated in its metadata. CLEARED for the record, against the slug's appearance: the recorded title is NOT a file stem - it is the document's own title, confirmed from both the PDF metadata and the cover slide - and there is NO font damage: this archive's extraction is BETTER than both reference tools here, reading the bullet glyphs correctly where pdftotext and pypdf both return replacement characters (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:88`

## https://www.blackhat.com/presentations/bh-usa-06/BH-US-06-Neerumalla.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: ROUGHLY 26 OF 42 SLIDES PUBLISH AS A TITLE AND NOTHING ELSE, so the Markdown is effectively a table of contents while the entire demonstration - the SQL Server sessions the deck walks through - exists only in the copied PDF. There is no images step and no image reference at all. DO NOT RE-RENDER FOR THIS: pdftotext reproduces the same title-only slides, which proves the content is raster screenshots inside the source PDF rather than an extraction failure; the fix is image extraction, not re-extraction. CLEARED for the record, against the slug's appearance: the recorded title is the document's own cover title, not a file stem, and there is no font damage - both reference tools agree with the published text. published is correctly empty; the deck states no date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:87`

## https://www.blackhat.com/presentations/bh-usa-07/Waissbein_Futoransky_and_Saura/Whitepaper/bh-usa-07-waissbein_futoransky_and_saura-WP.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE CITATION'S PLACEHOLDER LABEL - "Black Hat paper" - in frontmatter, body H1 and the slug. The paper's own cover page reads "Timing Attacks for Recovering Private Entries From Database Engines". FIVE SPURIOUS TOP-LEVEL HEADINGS were manufactured from the pseudocode's own comment lines, which begin with a hash at column 0 inside an unfenced algorithm block, so phrases like "Second step, narrowing." publish as document headings. Every listing also loses its line structure, so the algorithm renders as one run-on line. DO NOT RE-RENDER for the missing glyphs: the floor, ceiling and summation characters carry no usable mapping and pdftotext reproduces every gap - and on this document the archive is BETTER than the reference tool, recovering accented names, inequalities and en-dash bullets that pdftotext returns as replacement characters. published and publisher are empty although the cover names the company and the venue (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:100`

## https://www.blackhat.com/presentations/bh-usa-08/Stender_Vidergar/BH_US_08_Stender_Vidergar_Concurrency_Attacks_in%20Web_Applications_Whitepaper.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: SEVENTEEN HOLES IN THE PROSE, EACH AT A CURLY QUOTE. The source carries 25 curly quotes and 11 en dashes; the published body has ZERO of either, and at 17 of those 25 sites the pipeline deleted the surrounding text run as well - about 1,173 characters lost. Examples: "It is our opinion that today's common high-performance" publishes as "It is our opinion t-performance"; "we might capture HTTP POST requests to the TransferFunds.aspx page so that they can be replayed" publishes as "we might capture can be replayed"; "What constitutes appropriate synchronization" loses its opening letter. Both pdftotext AND the archive's own pypdf read every one of these correctly, so the damage is extractor-introduced and repairable offline. publisher and published are empty although page 1 states "(c) iSEC Partners, Inc. June 30, 2008" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:90`

## https://www.blackhat.com/presentations/bh-usa-09/BOJINOV/BHUSA09-Bojinov-EmbeddedMgmt-PAPER.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the file contains ZERO backticks, so Appendix 16, the 68-line CSRF testing tool, publishes UNFENCED at column 0 - a complete html document with two script blocks, a form, four inputs and an iframe. All the tags are closed so nothing is swallowed, but the listing renders as a live form plus iframe plus hidden script instead of as source, and a page footer is interleaved inside it between the closing form tag and two input lines. Separately, all 48 mark cells of Table 1 are blank and the sentence introducing them reads "We use the symbol  when one device is vulnerable" with the symbols gone - that loss is source-side, since pdftotext drops the same glyph, and the same data survives later in the paper (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:104`

## https://www.blackhat.com/presentations/bh-usa-09/WEBER/BHUSA09-Weber-UnicodeSecurityPreview-SLIDES.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: an UNCLOSED SCRIPT TAG sits at column 0 - the line demonstrating that NFKC normalisation turns a small-form less-than sign into a real one publishes the resulting open tag live, and the closing tag appears nowhere in the file, so 11,508 characters across 529 lines follow it inside a script element. The repository PDF escapes HTML so it is unaffected today, but any pass-through renderer loses the rest of the deck. Note the extraction is otherwise EXCELLENT and beats pdftotext, which drops the Latin small letter script g from the homograph example and mangles the copyright sign (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:107`

## https://www.cs.virginia.edu/~evans/pubs/usenix2013/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: coverage gap rather than a wrong page - the capture is the authors' own project page and is complete and faithful, but the 16-page paper it links as "Full paper (16 pages)" was never fetched, and steps.pdf.source is markdown so the archived PDF is a render of the project page. Remedy: acquire explicating.pdf (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:51`

## https://www.elttam.com/blog/ruby-deserialization

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every ASCII double quote in the BODY was replaced by a single quote - all 30 remaining double quotes are in the frontmatter and none in the body - which breaks the listings, so IO.popen(ruby -e Marshal.load(STDIN.read) rescue nil, r+) is invalid nesting and the quoted C source is not valid C; prose and curly quotes are unaffected, which is why it stayed invisible (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:91`

## https://www.feross.org/webcam-spy/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the byline was dropped although the stored source carries it directly under the post heading - "By Feross Aboukhadijeh" with an October 18, 2011 time element and an article:author meta - and the recorded title carries the site masthead in both the frontmatter and the body H1. Six list items publish as EMPTY bullets with their text detached into following paragraphs, two blockquote markers are empty so the quoted text escapes the quote, and the YouTube demo embed was dropped, leaving the "Video demo of the attack" heading nearly bare (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:40`

## https://www.gnucitizen.org/blog/backdooring-flash-objects/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVED SOURCE IS THE PUBLISHER'S 2025 STATIC REBUILD, NOT THE 2006 PAGE, and the rebuild alters what it republishes. Its footer replaces the site's 2006 Creative Commons BY-NC-ND 2.5 terms with an all-rights-reserved notice, so licence is recorded as unknown when the archive can state it from its own stored evidence: a 2006-era capture of a sibling page IS in the store and carries the CC terms three times. AN AFFILIATE LINK HAS BEEN INJECTED INTO THE RESEARCHER'S PROSE by that rebuild - a commercial URL substituted under an ordinary word - which the archive republishes as though the author wrote it. TWO injections here, and BOTH LAND INSIDE A CODE ELEMENT, so they render as literal Markdown syntax - the same pattern that broke code elements in the 2007 and 2008 collections. SIXTY PERCENT OF THE ARTICLE RENDERS AS ONE MONOSPACE CODE BLOCK: a fence opens and does not close for roughly 8,000 characters of narrative prose. THAT IS INHERITED, NOT INTRODUCED - the stored source contains a single unclosed code element 9,773 characters long, and a re-render will faithfully reproduce it; only a different source fixes it. TEN COMMENTS - 1,427 characters, three by the author - were dropped, and even the Archived Comments label is gone. THE ARCHIVE CARRIES AN UNCORRECTED ERROR WHOSE CORRECTION IT DELETED: a reader queries a misspelled method name, the author replies "fixed", and the published document still shows the wrong name. Also lost: a distinct filter-evasion technique, a prior-art pointer, and a tool. THE ARTICLE'S ONLY ILLUSTRATION was rejected by the image size heuristic as furniture, so nothing is preserved. A real blockquote was also collapsed to an empty one. published is empty although the source states the date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:74`

## https://www.gnucitizen.org/blog/backdooring-images/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVED SOURCE IS THE PUBLISHER'S 2025 STATIC REBUILD, NOT THE 2006 PAGE, and the rebuild alters what it republishes. Its footer replaces the site's 2006 Creative Commons BY-NC-ND 2.5 terms with an all-rights-reserved notice, so licence is recorded as unknown when the archive can state it from its own stored evidence: a 2006-era capture of a sibling page IS in the store and carries the CC terms three times. AN AFFILIATE LINK HAS BEEN INJECTED INTO THE RESEARCHER'S PROSE by that rebuild - a commercial URL substituted under an ordinary word - which the archive republishes as though the author wrote it. The injected link replaces THE ORDINARY DISCOURSE WORD in "For example", the same word-level substitution seen in the 2008 collection. FOURTEEN COMMENTS - 6,631 characters, MORE THAN THE ARTICLE'S OWN 6,409, two by the author - were dropped, leaving a bare Archived Comments label. THE ARTICLE'S CENTRAL CLAIM IS CONTRADICTED TWICE THERE by readers who could not reproduce it on the named site; the author confirms his browser claim ONLY in the comments; a commenter supplies the workaround for that very failure; another gives the defence landscape across two platforms; and a two-round argument about whether this is a browser or a server bug is lost, leaving the article's assertion standing with no rebuttal. A BROKEN BLOCKQUOTE also makes the article appear to open with an unattributed basketball definition - the source's real quote element, holding the opening joke, became an empty one. published is empty although the source states the date. CLEARED: the payload is fenced and renders escaped (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:75`

## https://www.gnucitizen.org/blog/backdooring-mp3-files/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVED SOURCE IS THE PUBLISHER'S 2025 STATIC REBUILD, NOT THE 2006 PAGE, and the rebuild alters what it republishes. Its footer replaces the site's 2006 Creative Commons BY-NC-ND 2.5 terms with an all-rights-reserved notice, so licence is recorded as unknown when the archive can state it from its own stored evidence: a 2006-era capture of a sibling page IS in the store and carries the CC terms three times. AN AFFILIATE LINK HAS BEEN INJECTED INTO THE RESEARCHER'S PROSE by that rebuild - a commercial URL substituted under an ordinary word - which the archive republishes as though the author wrote it. The injected link sits in THE SENTENCE THAT INTRODUCES THE PAYLOAD, immediately before the exploit listing. SEVENTEEN COMMENTS - 3,618 characters, three by the author - were dropped, leaving a bare Archived Comments label. THE ARCHIVE NOW PUBLISHES A CLAIM ITS OWN AUTHOR RETRACTED: the article closes by speculating that code running in a blank browser context can request special privileges and read and alter the local file system; a browser vendor engineer replies that that context is as unprivileged as it gets, and THE AUTHOR CONCEDES - both deleted. Also lost: the author's note that the issue reproduces in another media player, and a Mac platform matrix. FOUR PROOF-OF-CONCEPT ARTEFACTS are recorded as links only and none is archived. published is empty although the source states the date. CLEARED: the payload and all three listings are fenced and intact, and the Markdown is clean (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:19`

## https://www.gnucitizen.org/blog/backdooring-quicktime-movies/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVED SOURCE IS THE PUBLISHER'S 2025 STATIC REBUILD, NOT THE 2006 PAGE, and the rebuild alters what it republishes. Its footer replaces the site's 2006 Creative Commons BY-NC-ND 2.5 terms with an all-rights-reserved notice, so licence is recorded as unknown when the archive can state it from its own stored evidence: a 2006-era capture of a sibling page IS in the store and carries the CC terms three times. AN AFFILIATE LINK HAS BEEN INJECTED INTO THE RESEARCHER'S PROSE by that rebuild - a commercial URL substituted under an ordinary word - which the archive republishes as though the author wrote it. PROVEN AGAINST THE 2006 TEXT: the stored 2006 capture of THIS page has the same sentence as plain prose with no link. THE REBUILD ALSO SILENTLY COPY-EDITS THE RESEARCH. Diffing the published document against that 2006 capture shows seven reworded sentences, and worse, THE PROOF-OF-CONCEPT ARTEFACT'S FILENAME WAS CHANGED - the 2006 text names one file, the rebuild another, and the published link points at the new name. A fourth image present in 2006 is absent from the rebuild and therefore from the archive. RECOVERY IS IN THE STORE: the pre-rebuild 2006 object was stored but never adopted; the manifest's wayback step still names a much later snapshot. FORTY-SIX COMMENTS - 10,943 characters, more than twice the article's own 4,315, thirteen of them by the author - were dropped, leaving a bare Archived Comments label with nothing under it. THE ARTICLE DELIBERATELY WITHHOLDS ITS LAST STEP with "figure this out yourself", AND A COMMENTER SUPPLIES IT; the author adds a second attack mode there; a platform matrix exists only there; and the strongest published objection to this Top Ten technique - that it is not cross-site scripting at all - lives only in the deleted thread. published is empty although the source states the date in a dedicated element. CLEARED: the three bare quote markers correspond to EMPTY blockquotes in the source and are not a converter fault, and the image targets resolved correctly here (reported 2026-08-15) PARTLY REPAIRED 2026-08-17: the authentic 2006 page was recovered from Wayback (20061217072140) and published, so the archive no longer carries the 2025 rebuild. SETTLED: the injected commercial link is gone (chatbotkit.com/examples appeared under the word 'example' and is absent from the 2006 text), the seven reworded sentences are back to the author's own wording, the proof-of-concept artefact names match the 2006 text, the fourth image is present (4 of 4), and the recorded title was corrected back to 'Backdooring QuickTime Movies' via overrides after the capture put the blog masthead into it. A 57-character empty comment form was cut from the end. STILL STANDING: the 46 comments are not in this capture, licence still records unknown (this capture does not carry the CC BY-NC-ND 2.5 footer), and published is empty although the body states September 5th, 2006.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:9`

## https://www.gnucitizen.org/blog/bugs-in-the-browser-firefoxs-data-url-scheme-vulnerability

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: seventeen comments were dropped and the body ends on a dangling Archived Comments label. THE DEFINITIVE VENDOR ANSWER IS AMONG THEM: a Mozilla developer explains that the behaviour is intended, points at the bug where it was decided, and notes that blacklisting one scheme has never been safe because other browsers ship others - and a second Mozilla developer gives the design rationale and warns that changing it would break sites. A reader also supplies two payload variants the article does not have. A blockquote is broken, so a quoted specification passage reads as the author's prose. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:34`

## https://www.gnucitizen.org/blog/client-side-sql-injection-attacks

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: five comments were dropped with NO LABEL to mark the loss, and three carry technique - a direct rebuttal noting the API in question uses bind parameters and is immune, THE VENDOR'S OWN DEVELOPER conceding the residual risk and stating what developers must therefore do, and the author's forward prediction about persistent cross-site scripting in client-side storage and which browsers would follow. A blockquote is broken, so the quoted vendor description reads as the author's prose. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:84`

## https://www.gnucitizen.org/blog/content-disposition-hacking

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ONE FENCE SWALLOWS THE ENTIRE REMAINING ARTICLE - seven prose paragraphs and every listing - because the publisher's rebuild put the whole body inside a single preformatted block, which it round-tripped through Markdown and never re-rendered. The consequence is not cosmetic: code lines do not wrap, so NINE of the eighteen over-length lines are CUT OFF at the page edge in the PDF, including six of the seven prose paragraphs and the principal payload line. A --force re-render cannot fix this because the source is what is broken; the remedy is a 2007-era capture, and the store holds none for this slug. Seven comments were dropped with no label to mark it, two by the author, one of which supplies the browser-behaviour difference the article leaves implicit. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the source states its date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:85`

## https://www.gnucitizen.org/blog/cross-site-file-upload-attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the body ends on a dangling "Archived Comments" label with nothing beneath it, while the source carries THIRTEEN comments, four by the author, at least two carrying technique - a parallel textarea trick that smuggles a filename sub-field through an ordinary form, and the author's own note that URLRequest with sendToURL executes silently in the background with no restrictions applied, which is a materially better delivery than the LoadVars the article uses. published is empty although the body carries Thu, 21 Feb 2008. The payload itself was verified byte for byte against the source and is intact (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:33`

## https://www.gnucitizen.org/blog/flash-cookie-object-tracking/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE'S ENTIRE CONTRIBUTION IS MISSING. It says "the source can be found bellow" and the source is not there; all that remains is a ROOT-RELATIVE link that resolves against the archive. The released ActionScript - the thing nobody else had published - is absent; recording the resource path from the source. A NESTED ANCHOR injected by the rebuild also breaks the surrounding link, so the PDF prints literal Markdown brackets and a bare URL where a link should be. A fence swallows a link and a prose paragraph, and the line is truncated at the page edge in the PDF. Two comments were dropped with no label. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:86`

## https://www.gnucitizen.org/blog/frame-injection-fun/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE'S ONLY EVIDENCE IS A RELATIVE PATH THAT RESOLVES NOWHERE. The proof-of-concept screenshot publishes as /files/2008/10/frame-injection-fun-poc-300x210.png, a root-relative target that resolves against the archive host and shows nothing, and there is no images step; the source's own og:image gives the absolute URL. A blockquote is broken - a bare marker, a blank line, then the quoted paragraph at column 0. The body ends on a dangling "Archived Comments" label with nothing beneath it, while the source carries TWELVE comments, three by the author, that carry technique: a whitelist-by-id defence, a rebuttal distinguishing frame injection from open redirect, and frame-busting code. published is empty although the body carries Fri, 10 Oct 2008. Note the code fence that swallows three paragraphs of prose and two attack URLs is SOURCE-INHERENT - the publisher's own rebuild ships the prose inside a pre element (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:24`

## https://www.gnucitizen.org/blog/google-gmail-e-mail-hijack-technique/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVE HOLDS A LATER, SOFTENED EDITION AND THE EXPLOIT URL IS MISSING FROM IT. The store's 2008 capture opens with the author's own framing about installing a persistent backdoor and carries a disclosure-policy passage that the published copy does not; more importantly its update gives the ACTUAL ONE-SHOT EXPLOIT URL through the author's CSRF redirection utility, where the published copy says only that such a utility exists. The published copy's update is also less precisely dated. Three screenshot targets are ROOT-RELATIVE and resolve against the archive showing nothing, so the sentence introducing the sequence of screenshots describes an attack the reader cannot see; the 2008 capture has them absolutely, plus a fourth and a lead image. Sixty-six comments were dropped, including the only mitigation named anywhere in the record and the author's six replies. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the source states its date. Note the acquired raw is absent from the store: do NOT --force, it would re-fetch the same later edition (reported 2026-08-15) PARTLY REPAIRED 2026-08-17: the contemporaneous 2007 page was recovered from Wayback (20071230050756) and published; the body grew from 3,006 to 17,380 characters. SETTLED: the softened edition is gone, the author's own framing about installing a persistent backdoor is back, and THE ONE-SHOT EXPLOIT URL IS PRESENT - the full gnucitizen.org/util/csrf redirection URL that POSTs multipart/form-data to the GMail filter interface. No image target is root-relative any more (4 images). A 71-character empty comment form was cut from the end. STILL STANDING: the 66 comments are not in this capture, licence still records unknown, and published is empty although the source states its date.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:12`

## https://www.gnucitizen.org/blog/google-urchin-password-theft-madness

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: BOTH HEADLINE EXPLOITS WERE GUTTED. Each is published as a bare link followed by a comment marker and nothing else - everything between was dropped, because the author's unescaped pseudo-tags were removed as unknown elements and the nested anchor carrying the payload went with them. The credential-stealing script that reads the autofilled username and password and ships them to an attacker URL is absent from both exploits; only the harmless alert proof survived, and only because it was inside a code element. The demonstration video was dropped too - recording the resource: YouTube id wCUovL9WLVQ. Two list markers publish as empty bullets with their text orphaned below, and an emphasis run is mangled. The comments add a SECOND vulnerability in the same product that the article never mentions, and the author's own unresolved question about it. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the source states its date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:82`

## https://www.gnucitizen.org/blog/hacking-the-interwebs/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PROOF OF CONCEPT RESOLVES NOWHERE AND 96 COMMENTS WERE DROPPED. The line "The following code demonstrates the attack vector:" is followed by a link to a RELATIVE path, /files/2008/01/test.mxml, which resolves against the archive rather than the publisher, so the demonstration leads nowhere; the 2008 capture shows the original was an absolute URL. The body ends on a dangling "Archived Comments" label with NOTHING beneath it, while the captured page carries 96 comments - the most-discussed router-hacking post of the year - and the 2008 Wayback copy carries 92. The six-step ordered list publishes as unordered bullets, costing the numbering the prose refers to. Attribution is incomplete: the 2008 capture's opening line names ap, Adrian Pastor, as a stated CO-AUTHOR alongside pdp, and the modern edition the archive captured flattened that to "ap and I"; published is empty although the body states Sat, 12 Jan 2008. Note the raw object is absent from the store and correctly recorded on store-gaps (reported 2026-08-15) PARTLY REPAIRED 2026-08-17: the 2008 page was recovered from Wayback (20081028190949) and published. SETTLED: the proof of concept resolves again - the relative /files/2008/01/test.mxml is now the absolute gnucitizen.org/blog/hacking-the-interwebs/Test.mxml - the dangling 'Archived Comments' label is gone, and Adrian Pastor is credited as co-author alongside pdp, quoting the article's own opening line. STILL STANDING: the 96 comments are not in this capture, the six-step ordered list still publishes as unordered bullets so the numbering the prose refers to is still lost, licence records unknown, and published is empty although the body states Sat, 12 Jan 2008.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:65`

## https://www.gnucitizen.org/blog/hacking-without-0days-drive-by-java/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE TOOL THE ARTICLE OFFERS IS NOT IN THE CAPTURE AT ALL. The body says "the attach tar file contains a tool which I wrote", and no such attachment exists anywhere in the source - the modern rebuild dropped it, so the tool is lost unless a 2007 capture is fetched. The post's only figure, the signed-applet warning dialog that is the ENTIRE SUBJECT of the post, is published as a ROOT-RELATIVE target that resolves against the archive and shows nothing; the source itself carries it as un-rendered Markdown text, and the absolute target is available from the page's own metadata. Thirty comments were dropped, seven by the author, including a published field study confirming the premise, a port to another platform, and the author's full comparative argument with success rates - which the article only gestures at. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:81`

## https://www.gnucitizen.org/blog/hijacking-innocent-frames/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the body ends on a dangling "Archived Comments" label with nothing beneath it, while the source carries FIFTEEN comments, five by the author, several carrying technique - other ways to detect a logged-on user through dynamically generated css, image, swf and js files; a cross-origin URL leak via w.location with the actual leaked login URL; and a prior-art claim. published is empty although the body carries Thu, 11 Dec 2008. The document itself is otherwise sound: the handler inside the researcher's own listing survives intact inside a proper fence (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:64`

## https://www.gnucitizen.org/blog/java-jar-attacks-and-features

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE THREE-STEP RECIPE THAT IS THE POINT OF THE POST DOES NOT READ AS RUNNABLE. Its first step's listing became a fence at column 0, which TERMINATES THE LIST; its second glues the label to the command with no separator; and its third had a newline collapsed to a space by the extractor, so two distinct shell commands are now one invalid line. The article also carries THREE INJECTED COMMERCIAL LINKS in its prose, one of them mid-sentence in the explanation of the same-origin model. Twenty-four comments were dropped, eight by the author, and the thread is where the technique was actually worked out - the missing invocation syntax, the precedence rule the attack turns on, the author's answer to the obvious objection, a correction to a failed reproduction, the prior art, and the comment in which the technique IS NAMED, a year before the conference talk that popularised the name. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:87`

## https://www.gnucitizen.org/blog/javascript-global-namespace-pollution/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: published is empty although the body carries Thu, 07 Feb 2008. Recorded because this is the cleanest document of its group and the date is the only thing missing; note it is also the one document in the group that drops its comments WITHOUT leaving the dangling "Archived Comments" label its five siblings carry, which is the correct behaviour and the siblings should match it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:37`

## https://www.gnucitizen.org/blog/pwning-ubuntu-via-cups/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A DOUBLE ENTITY DECODE TURNS A PARAMETER NAME INTO A NOT SIGN. The cleanup script's line reads ...op=cancel-subscription&amp;notify_subscription_id=$i in the source; &amp;amp;notify decoded twice to &amp;notify and then to the single character NOT SIGN, so the published script reads cancel-subscription-notify_subscription_id=$i with a U+00AC in place of the ampersand and parameter name - copy and paste it and it cancels nothing. The line two above converts correctly, because only &amp;not matches a legacy named entity. The byline was dropped although the source names the author TWICE, a posted-by link naming pagvac and the signature inside the proof of concept reading "by Adrian 'pagvac' Pastor \| GNUCITIZEN.org", which survives in the published body; published is empty although the source states November 18th, 2008. The precision candidate also cut the post's title heading, so the document opens mid-sentence, and TWENTY-THREE READER COMMENTS WERE DROPPED that are the disclosure record itself - one identifies the crash as a NULL pointer dereference and points at CVE-2008-0047, Ubuntu Security discusses AppArmor confinement, the author establishes which release needs no authentication, and another traces the fix to cups-1.3.8 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:53`

## https://www.gnucitizen.org/blog/self-contained-xss-attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE DEMONSTRATION LINK WAS DELETED AND THE SCHEME IS BEING STRIPPED SELECTIVELY. The article's own live proof - an anchor whose href is a base64 data URI decoding to a script that raises an alert - publishes as the bare word "link", with the URI gone and the anchor flattened to text. The adjacent ftp and https anchors in the same paragraph both survived, so the converter is dropping the data: scheme specifically; the large base64 document blob elsewhere in the post IS preserved byte-identically. THE PDF THEN LOSES THAT BLOB ANYWAY: it is one 32,158-character line inside a fence, the renderer cannot wrap it, and the page box clips it at 33 of 32,088 characters - 99.9 percent lost, with the PDF carrying 3,817 characters against the file's 37,177. TWENTY-SIX COMMENTS WERE DROPPED, and they are the technique's adjudication: two readers refute it, the author CONCEDES that data URLs cannot reach cookies, another disputes novelty, then two more re-establish it and THE AUTHOR REVERSES HIMSELF. One of them also carries a live payload. The capture is of the 2025 rebuild, not the 2006 page: its footer is a 2025 copyright and an ANACHRONISTIC COMMERCIAL LINK to the author's present-day company now sits inside the archived 2006 text. published is empty although the body states its date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:90`

## https://www.gnucitizen.org/blog/severe-xss-in-google-and-others-due-to-the-jar-protocol-issues

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARTICLE'S AUTHOR SUPPLIED THE WORKING EXPLOIT IN THE FIRST COMMENT, AND THE COMMENTS WERE DROPPED. The article says only that someone proved open redirects could lead to domain-wide cross-site scripting and never says how; the author then opens the comment thread with "I forgot to mention how the attack works" and gives the complete jar-over-redirect URL against the named service. Fourteen comments are gone and the published body ends on a dangling Archived Comments label. A researcher's name is also MOJIBAKE in the body - a double-encoded Polish character that degrades further in the PDF, so a named researcher is misspelled in a published document; this one is source-inherent to the rebuild. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the source states its date (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:88`

## https://www.gnucitizen.org/blog/social-networks-evil-twin-attacks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the article's ONLY section heading is absent from the published document - three copies of the page in the store all carry it, and one of them predates the capture, so it was on the page on both sides. A blockquote is broken, so the author's pull-quote reads as ordinary body text. The body ends on a dangling "Archived Comments" label with nothing beneath it while the source carries seventeen comments. published is empty although the body carries Mon, 18 Feb 2008. Note the raw object is absent from the store and correctly recorded on store-gaps (reported 2026-08-15) PARTLY REPAIRED 2026-08-17: the 2008 page was recovered from Wayback (20080514062700) and published. SETTLED: the article's section heading is present again, the dangling 'Archived Comments' label is gone, and the lead image is preserved. STILL STANDING: the blockquote is still flat, so the author's pull-quote reads as ordinary body text; the seventeen comments are not in this capture; licence records unknown; and published is empty although the body carries Mon, 18 Feb 2008.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:67`

## https://www.gnucitizen.org/blog/the-attack-of-the-tiny-urls/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE ARCHIVED SOURCE IS THE PUBLISHER'S 2025 STATIC REBUILD, NOT THE 2006 PAGE, and the rebuild alters what it republishes. Its footer replaces the site's 2006 Creative Commons BY-NC-ND 2.5 terms with an all-rights-reserved notice, so licence is recorded as unknown when the archive can state it from its own stored evidence: a 2006-era capture of a sibling page IS in the store and carries the CC terms three times. FOUR COMMENTS - 2,242 characters, two by the author - were dropped, leaving a bare Archived Comments label with nothing under it. THEY CONTRADICT THE ARTICLE ON ITS OWN SAFETY CLAIM: the article states twice, in bold, that the service is NOT vulnerable to cross-site scripting, and the author replies in the thread that it does have a persistent hole which he is not disclosing. A commenter also supplies a data-URI technique the article never states, and another posts a working proof of concept implementing the article's prose algorithm. published is empty although the source states the date in a dedicated element. CLEARED, and worth recording: this is the ONE document of the five from this publisher with NO affiliate injection, and its payload and Markdown are clean (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:18`

## https://www.gnucitizen.org/blog/username-enumeration-vulnerabilities

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: FIVE BROKEN BLOCKQUOTES STRIP THE QUOTING FROM THE FIVE APPLICATION ERROR MESSAGES THAT ARE THE TECHNIQUE'S EVIDENCE, so a reader cannot tell the quoted strings from the author's narration. An injected commercial link also breaks the term User-Agent apart mid-word. The article's five concrete example addresses have been ERASED BY EMAIL OBFUSCATION and publish five times as a placeholder link - they are the closing practical advice; they were recovered from the stored source and are targetuser@, test@, support@, admin@ and acmeadmin@ at the article's example domain. Five comments were dropped with no label, two carrying technique: a concrete instance in another product, and two defences the article's own conclusion misses. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:83`

## https://www.gnucitizen.org/blog/web-mayhem-firefoxs-jar-protocol-issues

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: AN INJECTED COMMERCIAL LINK BREAKS THE ARTICLE'S CENTRAL EXAMPLE. The defining jar URL publishes with an affiliate link spliced into the hostname, INSIDE a code element, so the example URL the whole post is about is malformed. A fence also swallows the explanatory paragraph and the second example, so prose renders as code and its bold markers print literally, and a blockquote is broken. Twenty-three comments were dropped, six by the author, and that thread is the vulnerability's full technical record: a contradicting test result, the pointer to the vendor bug that is separately archived in this same collection, the author twice WIDENING the technique beyond what the article states, the mitigation as shipped, and the exchange that directly caused the sibling post archived beside it - the causal chain between two archived references exists only in this dropped thread. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:7`

## https://www.gnucitizen.org/projects/0day-quicktime-pwns-firefox/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE CHOOSER TOOK A CANDIDATE THREE TIMES THE ARTICLE'S SIZE AND STILL LOST THE COMMENTS. It preferred the raw candidate at 10,772 characters and 57 links over the precision candidate at 3,048 and 4, buying 7,700 characters of trackbacks and a comment form - and the 78 comments were dropped anyway, leaving an EMPTY comments heading above 53 published trackbacks. 7,461 characters of that is removable furniture. The article's demonstration files were dropped too, leaving "here is a list of a few files you have to click on" followed by nothing; recording them: four media files on the author's project path, plus a fifth from the author's own first comment. The dropped thread carries the IE variant of the exploit with its distinctive path form, the mechanism behind a contemporaneous third-party zero-day, a working refinement to the payload, the AUTHOR'S OWN CORRECTION that the flaw is not Firefox-specific as the article claims, a mitigation, and a platform matrix. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the capture states September 12th, 2007. The one figure failed against a dead host (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:89`

## https://www.gnucitizen.org/projects/hscan-redux/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THE PRECISION CANDIDATE CUT THE FOOTER WHERE THE BYLINE LIVES, and with it the post's own title heading, its date, the proof-of-concept launcher and thirteen comments. The published file therefore prints "Author not stated" and has no heading inside its content region at all, and THIS IS THE ONLY ENTRY OF ITS GROUP WITH NO ATTRIBUTION STEP - the curation pass that credited its twelve siblings did not reach it. The source names the author plainly in that cut footer. WORSE, THE MECHANISM IS ONLY IN THE COMMENTS: the article never says how the leak works, and the dropped thread names the browser internal it depends on, proposes reading it wholesale to convert the leak into full history theft, records the author's reply on whether that is possible, and carries a version matrix showing where it does and does not work. Publisher drift, affecting every gnucitizen entry: the capture is the modern static rebuild, not a 2007 page. Its footer replaces the CC BY-NC-ND 2.5 licence the 2007 site carried with a 2025 all-rights-reserved line, so licence records unknown when the research is CC-licensed. published is empty although the source states February 23rd, 2007 (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:19`

## https://www.gnucitizen.org/projects/total-surveillance-made-easy-with-voip-phones/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL FIVE IMAGES ARE DEAD AND FOUR OF THEM ARE THE EVIDENCE. The images step records 0 of 5 preserved, every one failing with UnidentifiedImageError because the dead host answered with an error page, and the four screenshots that demonstrate the attack are lost, leaving the line "and some supporting screenshots follow here:" as a lead-in to nothing; the Wayback replay under the recorded snapshot is the obvious source and was not used. The byline was dropped although the source states "posted by .mario" - a handle, which counts - and published is empty although the source states February 11th, 2008. The precision candidate also cut the title heading, so the document opens on the image. A blockquote is broken, leaving the vendor's own quoted reply reading as the author's prose. One alt attribute is MOJIBAKE - it publishes we\u00e2\u20ac\u2122ve where the same apostrophe is correct everywhere else in the file, so the alt text alone was double-decoded (reported 2026-08-15) BLOCKQUOTE QUOTING REPAIRED 2026-08-17: every line of each quoted passage now carries its marker; the other faults named above stand.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:66`

## https://www.ieee-security.org/TC/SP2011/PAPERS/2011/paper015.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 5 of the 16 pages carry no text at all. Pages 9, 10, 11, 13 and 14 publish as bare page numbers because in the stored PDF each has an empty content stream and a single 2550x3300 image - they are RASTER SCANS, so the loss is source-side and needs OCR or an author-hosted text PDF. What is missing is the Last.fm and LibraryThing case studies, the third Amazon study, ALL of the Netflix-Prize simulation carrying the headline result, and most of the mitigation section. The body jumps straight into dangling continuations - "For a setting at which 5 users had a minimum of 100 correct inferences" - with no indication anything is absent (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:68`

## https://www.ieee-security.org/TC/SP2014/papers/AllYourScreensareBelongtoUs_c_AttacksExploitingtheHTML5ScreenSharingAPI.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: five figure-label lines extracted in a shifted font encoding and publish as gibberish (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:67`

## https://www.ieee-security.org/TC/SP2017/program-papers.html#sok-exploiting-network-printers

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: WRONG PAGE - the archived body is the IEEE Symposium on Security and Privacy 2017 PROGRAMME page listing every accepted paper, standing in for one cited talk (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:109`

## https://www.ieee-security.org/TC/W2SP/2009/papers/s3p1.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THREE OF FOUR PAGES ARE MISSING AND THE PAPER ENDS MID-SENTENCE. pypdf reports four pages and pdftotext -layout returns 26,224 characters across them; the published body carries page 1 only, about 4,600 characters, with a single page marker and no page 2, 3 or 4. What is gone includes Section 3, the new XSS attacks that ARE the cited return-to-JavaScript technique, and Section 4, the mitigation - roughly 80 percent of the paper. The surviving page is damaged too, and unlike the Zscaler deck this damage IS extractor-introduced: ligatures deleted rather than substituted (signicant, buer, overow, best-eort), words split (Mar/katos, Cre/te, JavaScrip/t, clien/t-), and LaTeX braces published as letters, so the authors' address reads f elathan, vpappas, markatos g @ics.forth.gr. pdftotext reads all of it cleanly on the same stored PDF, so a repair path exists offline (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:106`

## https://www.ieee-security.org/TC/W2SP/2012/papers/w2sp12-final10.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 3 of the 4 pages are missing. The whole body is ONE 4,899-character line ending mid-sentence on a dangling lead-in. Every fi ligature is dropped - benets, signicant, le sharing, difcult, rst, specied, re an intent, eld - and the ASCII controls read zero for first, file, files, difficult, significant, specified, benefit and field, while the ff class survives, so it is specifically the fi glyph with no mapping. Braces became letters in the emails (fjkallahe...gandrew.cmu.edu) and inter-word spaces collapsed throughout (Web IntentsJenna Kallaher, forimage/*les) (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:90`

## https://www.ieee-security.org/TC/W2SP/2012/papers/w2sp12-final11.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 87 percent of the paper is missing. The file holds only page 1 and stops mid-sentence at "...the adversary simply wishes to obtain access to the sensitive", 5,245 characters against 40,008 that pdftotext returns cleanly from the same stored bytes, ending properly at reference [22]. Every ligature is DROPPED, not substituted - exltration x18, ow for flow, rst for first, nally, nancial, awed - and the ASCII controls confirm total loss: exfiltration 0, flow 0, first 0, financial 0. The LaTeX braces also came out as letters, so the author emails read "Universityferic.chen, ... collin.jacksong@sv.cmu.edu" for {eric.chen, ... collin.jackson}. REPAIRABLE OFFLINE: store bytes are present and poppler reads them correctly (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:77`

## https://www.ieee-security.org/TC/W2SP/2013/papers/s3p1.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 1,118 of 1,436 lines are a SINGLE CHARACTER - every embedded figure text box was emitted one character per line, so the label Attacker occupies eight consecutive lines and one block spells out a JSONP payload across 377 lines. The page count is fabricated: the PDF has 10 pages, the Markdown asserts 50, and 40 of those pseudo-pages are figure text boxes mislabelled as pages. Text is also dropped at nearly every apostrophe and most quoted strings (39 apostrophes in the source, 2 published), swallowing neighbouring words - "executed in the vithe attacker" for "executed in the victim's browser and the victim's data ... is sent to the attacker" - and reference [32] lost its authors and title outright (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:63`

## https://www.infoworld.com/article/2322705/governator-hack-investigated.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: SEVEN OF THE EIGHT STORED IMAGES ARE 2026 RECOMMENDATION-WIDGET THUMBNAILS harvested onto a 2006 news brief - stock photography and video thumbnails from the publisher's present-day "show me more" module - and the images step reports seven of eight preserved, so the archive is holding modern promotional artwork as this document's figures. The eighth is the author's headshot. The 2006 item had no figure at all. 3,264 characters - 42 percent of the file - are furniture at the end: an eight-item author back-catalogue and that same 2026 widget. The recorded author is the publisher's CMS USERNAME, taken from the page's structured data, where the document's own visible byline gives the name properly - the header line currently prints the username (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:60`

## https://www.kapravelos.com/publications/mystique-CCS18.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the Markdown extraction stops mid-sentence after page 1 of a 14-page CCS paper, so everything from section 2 onward is absent, and the surviving page has pervasive dropped-ligature damage (congure, rst, ow, agged, conrmed, trac, eorts) plus inter-word spaces lost at every wrap point; the PDF sibling is 744,909 bytes and predates the Markdown, so re-extract from it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:85`

## https://www.kb.cert.org/vuls/id/120541/

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: THE 111-VENDOR STATUS TABLE IS MISSING AND THE CVSS SCORE IS PLACEHOLDER DATA. The Vendor Information section publishes only the filter widget - the note number, Filter by status, Filter by content, Sort by, Expand all, View all 111 vendors - with ZERO vendor rows, because the JavaScript-loaded table did not survive the browser capture; an equivalent capture in the store proves the substance exists, carrying 3com, Cryptlib, Debian, GnuTLS and IBM rows with their statuses and notification dates. The CVSS table publishes Base 0 with AV:--/AC:--/Au:--/C:--/I:--/A:-- in every field. The recorded title, in frontmatter and body H1, is the note's type label and number with its subject dropped: the page's own title is "VU#120541 - SSL and TLS protocols renegotiation vulnerability". 288 characters of site furniture remain at the end, a PGP key download and an SEI research link, and the second body H1 is the Software Engineering Institute masthead rather than a heading. The manifest raw_sha256 is absent from the store (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:99`

## https://www.linkedin.com/pulse/how-i-used-google-dorks-find-0-days-suraj-khetani/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the cited URL now redirects to https://www.linkedin.com/top-content/?trk=article_not_found, so the archived document is that page rather than "How I used google dorks to find 0-days"; recapture from a Wayback snapshot of the original URL (reported 2026-08-14)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2016-17.md:48`

## https://www.microsoft.com/en-us/research/?p=153771

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: this is a landing page carrying an abstract, and THE PAPER IT FRONTS IS ARCHIVED SEPARATELY YET NEITHER ENTRY KNOWS ABOUT THE OTHER - also_at is empty and linked_document_url is unset on both, so a reader of either cannot reach the other. published is empty although the body states the month and the source carries an explicit machine-readable publication date. Cleared for the record: the redirect flag on this entry is a permalink resolution to the same document, not a wrong page (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:105`

## https://www.microsoft.com/en-us/research/publication/pretty-bad-proxy-an-overlooked-adversary-in-browsers-https-deployments/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: A LANDING PAGE STANDS IN FOR AN IEEE S&P 2009 PAPER, AND THE POINTER TO THE PAPER WAS LOST IN EXTRACTION. The stored page carries the paper's own download button linking pbp-final-with-update.pdf; the extractor dropped it and kept only the two secondary links, both LABELLED PDF in the source but pointing at a PowerPoint deck and a text file, so the published body offers two dead ends and no route to the research. also_at is empty and linked_document_url is unset, leaving a 2,694-character abstract in place of the paper. published is empty although the page states May 2009. The grading is wrong for an abstract stub: rule:default recorded "carries technique" where this is a records-grade landing page (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:100`

## https://www.microsoft.com/en-us/research/publication/residue-objects-a-challenge-to-web-browser-security/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Microsoft Research publication landing page stands in for the paper - title, three author bullets, a venue line, a BibTex chrome link, the abstract and ACM boilerplate, 431 words. The paper PDF is linked inside the captured page and was not resolved: also_at is empty and there is no linked_document_url, while the entry is graded class research with the reason 'carries technique'. It carries the abstract, not the technique (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:96`

## https://www.microsoft.com/en-us/research/publication/unauthorized-origin-crossing-on-mobile-platforms-threats-and-mitigation/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the Microsoft Research publication landing page stands in for the CCS 2013 paper - 5,305 characters holding title, authors, venue, a Download BibTex link, the abstract and the ACM notice, and nothing else. also_at is empty, there is no linked_document_url, and retrieved_from is the same landing page; the archived PDF was rendered FROM this Markdown, so no copy of the paper exists in the pair. The remedy is on the page itself: the stored raw object carries citation_pdf_url pointing at ccs127-wang.pdf (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2013.md:50`

## https://www.microsoft.com/en-us/research/wp-content/uploads/2016/02/ScriptAccenting.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: NINETEEN PAGE MARKERS OVER A TEN-PAGE PDF, AND TEXT IS DROPPED AT THE SEAMS. Nine markers are invented and none aligns with a real page boundary - the PDF's first page alone is spread across eight of them - and at each seam a run of text is missing: the abstract loses the clause "end-to-end browsing time did not show any noticeable slowdown", and another seam loses "ation mechanisms: 1) Script-". TWO FONT-STREAM BLOBS ARE PUBLISHED AS PROSE under two of the invented markers; neither pdftotext nor pypdf emits any of it, so this is the same class the repository already fixed once and it survived here. EIGHT XOR OPERATORS WERE DELETED that pypdf recovers, so the section arguing the impossibility of XOR probing attacks renders its central expression with the operator simply missing. Words are also broken mid-token throughout - Achievi/ng, Isolati/on, web appli/cations - which pdftotext reads cleanly. THE RECORDED TITLE IS THE URL BASENAME in frontmatter, banner and body H1; the paper's own cover names the analysis and the defence, and a SIBLING LANDING PAGE holds that same title, so this one takes the (Paper) suffix. The results table collapses into prose and eight figures are absent. publisher and published are empty although the paper states its conference and copyright line (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:105`

## https://www.nds.rub.de/media/emma/veroeffentlichungen/2012/08/16/scriptlessAttacks-ccs2012.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 11 of the 12 pages are missing - the file carries one page marker and stops at "disables the aforementioned protection mechanism", so every case study, the attack fonts, the mitigations and the references are absent. Every ligature on the one surviving page is dropped: {rstname.lastname}@rub.de, eective, eects, font les, exltrate, prot, rst page, specic, Specically, dierent. The stored PDF carries NO ToUnicode map on any of its subset fonts, which is the cause. About 20 mid-word line breaks with no hyphen as well (Thorste/n Holz, attac/ks, informati/on). Store bytes present, so a re-extract is safe (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2012.md:81`

## https://www.nds.rub.de/media/nds/veroeffentlichungen/2011/10/22/HowToBreakXMLenc.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: TRUNCATED TO PAGE 1 OF 10. One page marker and 6,084 characters, where pypdf extracts 51,959 across ten pages from the very PDF the archive publishes - about 88 percent missing, including the CBC construction, both algorithms, the Axis2 timing evaluation and the whole reference list. It ends mid-argument on "alternate XML frameworks and possibly even other systems". The ligatures were DELETED rather than transliterated - nancial, specication, condential, dierent, prot, rst - and there are no precomposed ligature codepoints anywhere in the file, so ASCII different, first, efficient and specific all return zero for real. A TeX font-encoding table also substituted a pipe for the em dash and a backslash for the opening double quote, giving \"an \\oracle\" telling us\". REPAIRABLE OFFLINE: the stored raw is present and the published PDF is byte-identical to it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:73`

## https://www.nds.ruhr-uni-bochum.de/media/hgi/veroeffentlichungen/2011/10/19/svgSecurity-ccs11.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 92 PERCENT OF THE PAPER IS MISSING. The published body is 4,546 characters - page 1 only - ending mid-sentence on "port provided by major browsers was not consistent and only", where the stored PDF is 12 pages and 70,523 characters. Every attack technique, the browser results, the filter-circumvention analysis, the mitigation and all 43 references are gone, and the file carries a single page marker for a 12-page paper. The ligature damage is ARCHIVE-INTRODUCED, not source-inherent: pdftotext on the same stored bytes returns correct ASCII with specific x28, files x100 and filtering x14, while the published text has ZERO of each and 21 damaged tokens in 4,546 characters (les, specic, rst, ltering, dierent, prot, aws). The glyphs were deleted, not preserved as ligature codepoints - the entire non-ASCII census of the body is two en-dashes. REPAIRABLE OFFLINE (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2011.md:80`

## https://www.ndss-symposium.org/ndss2014/ndss-2014-programme/breaking-and-fixing-origin-based-access-control-hybrid-webmobile-application-frameworks/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the ti ligature is mapped to a wrong digit in four figure-diagram labels - applica6on, adver6ser, adver6sing, applica5on. Scope confirmed narrow: running prose is clean, with application x41, advertising x19 and attack x44 all correctly spelled (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:65`

## https://www.ndss-symposium.org/ndss2014/ndss-2014-programme/toward-black-box-detection-logic-flaws-web-applications/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the LaTeX Type1 fonts were read under the wrong encoding, so 124 occurrences of Þ stand where fi belongs and 52 of ß where fl belongs - speciÞc, Þrst, ßaws, insufÞcient, reßects - plus Ñ/Ð for the dashes and ÒÓÕ for the quotes. About 2,712 characters of decoded Type3 figure-glyph bytes are published as prose across 14 interleaved pseudo-pages, and 400-plus inter-word spaces are lost at line joins (WebApplicationsGiancarlo, basedon, toperform). The source PDF supplies NO usable ToUnicode table, so a plain re-render will reproduce the mapping - the repair needs an encoding map or the sibling slides. Check raw_sha256 is present in the store before any re-render (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:74`

## https://www.ndss-symposium.org/wp-content/uploads/2017/09/09_1_2.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: SEVEN OF THIRTEEN PAGES ARE MISSING - the capture holds only pages 1, 6, 8, 10, 12 and 13, and sections II, III and IV do not exist in the file at all, while the surviving text refers back to them (We avoided discussing the stack-pivoting gadget in Section IV). Listing 1 is referenced but never printed, and Figures 2, 3 and 5 never appear. Everything present is faithful, so this is lost pages rather than damaged text (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:70`

## https://www.ndss-symposium.org/wp-content/uploads/2017/09/09_3_2.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: six of fifteen pages are absent - the page-marker sequence breaks 4 to 6 and 6 to 12, prose is discontinuous across both breaks, and sections IV-B, IV-C (gadget chaining, the paper named contribution) and all of section V are gone. CAUSE VERIFIED against the stored source: pages 5, 7, 8, 9, 10 and 11 are SCANNED RASTER IMAGES, each with a 31-byte content stream, no /Font resource and a single /Im0 XObject, so pdftotext returns zero characters for them. This one needs OCR or a full-text source; a --force re-render would reproduce the loss exactly (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:78`

## https://www.ndss-symposium.org/wp-content/uploads/2018/02/ndss2018_04B-3_Yang_paper.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: 34 lines of figure-label text are decoded through a broken font map and publish as a whole-alphabet substitution - ,dD>ZenderTJSngine for HTML Render and JS Engine - so Figure 2 label set is unreadable; the prose is unaffected, which is why length and quality checks pass (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:86`

## https://www.sambuz.com/doc/p-r-e-p-a-r-e-i-n-t-r-o-d-u-c-i-n-g-n-o-v-e-l-e-x-p-l-o-i-presentation-910240

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the document is a sambuz.com SCRAPER MIRROR of the original deck rather than the deck itself - every slide title is shredded into spaced single characters (## P r e p a r e ( ) : I n t r), words carry wrong-character ligature damage (fmexible, modifjed, fjx), prose stutters (do doubl ble pr prepa paring), and all 36 slide images are the same pre.svg placeholder; re-acquire from the original RIPS/Black Hat deck (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: the deck is Robin Peraglie (RIPS Technologies), AppSec EU 2018; the conference schedule page links no slides PDF and no authoritative open copy (SpeakerDeck/SlideShare/OWASP) was found live. The Wayback route was unavailable (archive.org returning 498/503 this session). Retry Wayback for the original deck, or import a maintainer-obtained copy.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:12`

## https://www.securityevaluators.com/sl/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THIS IS THE WRONG PAGE - THE ARTICLE IS 100 PERCENT ABSENT. The published body is the publisher's present-day corporate marketing homepage: a press-logo wall, "Join our Team" and "Ask an Expert" calls to action, fifteen headings for 395 words, and zero words about the subject. health records status soft-404 with the evidence "200 with not-found wording" and a redirect to the new corporate domain, and the browser check reported ok on it, which is how it got published. THE CORRECT 2007 BYTES ARE ALREADY IN THE STORE, under the hash THE MANIFEST ITSELF RECORDS - the frontmatter names a different hash, which is the one that is missing, so acquire --force would go to the network: the documented trap exactly. The real page carries the title "Exploiting Second Life", names both researchers in its opening paragraph, and embeds a demo video (id RaCo4USXd5Y) plus a captioned figure. The published file also has three unclosed bold runs and a line truncated mid-word with a stray pipe (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:90`

## https://www.sensepost.com/blog/1303.html

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: SEVENTY-ONE PERCENT OF THE FILE IS SITE FURNITURE AND THE ARTICLE IS TWELVE PERCENT OF IT. 15,942 characters of spacer-GIF layout tables, a 90-entry category list, a 60-entry month archive and a blogroll follow the article, and 1,819 characters of chrome precede it; NO precision candidate existed, so the raw candidate won by default with 174 links and 104 images. THE RECORDED TITLE IS THE BLOG MASTHEAD - the document's own entry title appears NOWHERE in the published file, because the extractor dropped the entry header along with it; the source records it in a bookmark link. Two image links are exploded by a div nested inside the anchor plus a stray quote in the source, so the reader sees a bare bracket and a bare quote-bracket-URL run in place of each figure. ALL 28 IMAGES FAILED because they were resolved against the dead live host rather than the Wayback replay that supplied the page, and TWO OF THEM ARE THE ESSENTIAL FIGURES - the diagrams of the timing attack and of the brute force against a time-leaky login page - both recoverable from the replay. The byline is empty although the source names the author in an entry byline element, and published is empty although the source states the date and time (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:43`

## https://www.sensepost.com/research/reDuh/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: ALL THREE EXPLANATORY DIAGRAMS ARE LOST, and the prose introduces them - "we had to have prettier pics for the .ppt so here you go:" is followed by nothing. The source uses RELATIVE image paths and the converter resolved them against the live host, which is dead and now Orange Cyberdefense, rather than the Wayback replay the text came from, so the network diagram and both tool screenshots failed with UnidentifiedImageError. 461 characters of furniture remain at the end, and the ** that opens that block leaves an unbalanced emphasis run so the PDF prints a literal ** before Disclaimer; a second line leaves an unclosed bracket. The first body heading is an H1 whose entire content is a masthead image that no longer resolves, so the PDF shows an empty heading. The download-links line just above the furniture is part of the document and must not be cut with it (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:52`

## https://www.slideshare.net/fransrosen/attacking-modern-web-technologies?from_action=save

- Outcome: `broken-capture` (no bytes stored)
- Kind: slides
- Reason: faulty capture: the deck is 142 slides and only 100 were captured - the last image is slide 100 and the file stops mid-demo, so slides 101 to 142 are absent - and SlideShare lorem ipsum stands in as alt text on two slides (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2018.md:9`

## https://www.usenix.org/conference/hotsec-07/transaction-generators-root-kits-web

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: THREE OF FOUR PAGES ARE MISSING AND THE BODY ENDS MID-SENTENCE, breaking off inside a hyphenated word. 14,725 characters are absent: the section that builds the tool, the countermeasures section including the transaction-confirmation design the abstract advertises, the conclusion and the references. EVERY fi LIGATURE IS DELETED - the text reads conrmation six times, plus identication and modication - and pdftotext reads the same stored bytes cleanly, so this is extractor-introduced and repairable offline without fetching. publisher is empty although both recorded URLs are the publisher's (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:106`

## https://www.usenix.org/conference/usenixsecurity14/technical-sessions/presentation/dahse

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: every dollar sigil and every listing apostrophe is absent - ZERO occurrences of $ in 103,211 characters of a PHP paper - so the code is unusable: mysql_fetch_row($res) reads as mysql_fetch_row( res), and the RCE payload {${system(id)}} reads as { {system(id)}}. Inline monospace also loses underscores (file put contents, move uploaded file). VERIFIED AGAINST THE STORE: pdftotext -layout on the stored object gives identical text with the same zero dollar count, so this is the publisher own PDF text layer and A RE-RENDER CANNOT REPAIR IT. Needs OCR or a different source copy (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:66`

## https://www.usenix.org/conference/usenixsecurity14/technical-sessions/presentation/li_zhiwei

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the USENIX programme page stands in for the paper - 7,001 characters of abstract, session time, BibTeX and download links, where the paper itself is 15 pages (pages 465-479 per the page own BibTeX). Unlike its NDSS sibling in this same collection the tooling never followed through: linked_document_url is null and also_at is empty, although the body links sec14-paper-li-zhiwei.pdf directly. The abstract is also published TWICE, verbatim, 1,116 characters each. The archived PDF is only a render of this page, so no copy of the paper exists in the pair. Remedy: acquire the linked paper PDF (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2014.md:79`

## https://www.usenix.org/event/woot08/tech/full_papers/shue/shue.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: SIX OF SEVEN PAGES ARE MISSING AND THE PAPER STOPS MID-SENTENCE. pypdf reads all seven pages cleanly, about 31,800 characters; the published body carries "--- page 1 ---" and nothing else, ending at "on testing if any of these were open. To our surprise, 79%". Gone: the heuristics section that identifies open redirects, the data-collection methodology, the prevalence results, both mitigation sections, related work, the conclusion and the references - roughly 86 percent of the paper and its entire contribution. Because pypdf reads all seven, this is a render-path fault and is repairable offline. Separately a normalisation step DELETES ligatures rather than expanding them, so the paper publishes Identication, signicant, nd themselves and ght against; pdftotext returns all of them correctly. Note this is the only entry in its batch with NO health block and no check step at all, so nothing in the record establishes the document's identity (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:94`

## https://www.usenix.org/events/woot10/tech/full_papers/Rizzo.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: fi/ff/fl ligatures are missing inside words ("aected", "conguration", "dierent"); re-read with poppler on 2026-08-14, which recovered text but not the ligatures because the PDF's embedded font carries no usable ToUnicode map for them; needs a different extractor or OCR (reported 2026-08-14) WIDENING AN ALREADY-FILED GAP: the same broken font map also deletes EN DASHES, which silently corrupts the reference page ranges - 'pp. 583599' for 583-599, 'pages 305323', '534545' - and the multiplication sign, so the paper's '128 x b oracle calls' publishes as '128 ? b oracle calls' with a literal question mark. pdftotext drops that glyph entirely, so the visible question mark is specific to this extractor. Footnote superscripts are flattened into the text as well. (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:5`

## https://www.usenix.org/legacy/event/hotbots07/tech/full_papers/provos/provos.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: AN UNCLOSED SCRIPT TAG SWALLOWS 47 PERCENT OF THE PAPER. A script opening and an HTML comment opening sit at column 0 with no closing tag anywhere after them, so 45,090 of 95,755 characters - sections 5 onward, the discussion, the conclusion, the acknowledgments and the entire reference list - are emitted as raw HTML inside a script element and are deleted outright by a sanitiser. Three live iframe sources and two live script sources pointing at 2007 malware hosts are also published unfenced. The whole paper is additionally TWO-COLUMN INTERLEAVED, so the obfuscated-JavaScript figure that is the paper's evidence reads as one line glued to unrelated right-column prose; plain pdftotext and pypdf both read it in correct order, so this is repairable offline. THE RECORDED TITLE IS THE PDF FILE STEM and the body H1 is a synthesised label; the paper's own cover reads "The Ghost In The Browser: Analysis of Web-based Malware", which the year list already spells out. Note a SIBLING ENTRY HOLDS THE SAME PDF BYTES - the landing page for this paper - and neither cross-links the other (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:103`

## https://www.usenix.org/legacy/event/hotsec07/tech/full_papers/jackson/jackson.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE RECORDED TITLE IS THE PDF FILE STEM, WHICH ALSO HAPPENS TO BE THE FIRST AUTHOR'S SURNAME - frontmatter, body H1, byline line and slug all read it. The paper's own cover page reads "Transaction Generators: Root Kits for Web", which is exactly what the year list says. A SIBLING ENTRY ALREADY HOLDS THAT TITLE - the conference landing page on the same citation line - so the repository's format-word rule applies and this one takes the (Paper) suffix, which is also its cited_title. THREE OF FOUR PAGES ARE MISSING: the body is 4,455 characters against 20,046 that pdftotext returns, and what is gone includes the section that builds the tool, the countermeasures section, the conclusion, the entire reference list, and the XUL overlay listing that is the paper's only payload. EVERY fi LIGATURE IS DELETED rather than substituted, so the text reads conrmation, identication, modication and rst - pypdf reproduces this but pdftotext reads the same stored bytes cleanly, so it is extractor-introduced and fixable offline without fetching. The file also has no blank lines and no indentation at all, so all 91 body lines render as ONE paragraph, running the author block into the abstract and the introduction (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:106`

## https://www.usenix.org/legacy/event/sec08/tech/full_papers/martin/martin_html/index.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: authors is empty although the paper's own cover block names Michael Martin and Monica S. Lam of Stanford, both under their own headings - a paper cover page, which counts unambiguously. Six blockquotes are broken: a bare marker alone, then a blank line, then a rule with the quoted figure at column 0, so each renders as an empty quote wrapped in horizontal rules. 103 characters of converter credit remain at the end. The author e-mail addresses were lost to Cloudflare obfuscation, an artefact of fetching the page live in 2026 rather than a capture fault. Note the manifest raw_sha256 is ABSENT from the store while the frontmatter hash resolves and is the better render - the manifest's copy is full of empty links - so this must not be re-rendered with --force. Two apparent text faults were checked and are SOURCE-INHERENT, carried by the publisher's own HEVEA output: the en-dashes in the commented-out markup, and the dropped word in "programmable systems like , PQL, and QED" (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2008.md:92`

## https://www.usenix.org/legacy/event/woot07/tech/full_papers/futoransky/futoransky.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: the paper is TWO-COLUMN INTERLEAVED, so the pseudo-code is shredded into the prose column and the results table rows are spliced with the closing remarks; plain pdftotext and pypdf both read it in correct order, so this is repairable offline. The paper's central illustration, its B+-tree diagram, is absent with only its caption remaining. published is empty although the cover states July 31, 2007. DO NOT REPAIR THE MATH GLYPHS BY RE-EXTRACTING WITH pdftotext: the archive's ceiling and not-equal renderings are imperfect, but pdftotext DELETES those glyphs outright, which in one place REVERSES THE MEANING of a condition. Reflow the columns; leave the mathematics alone (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2007.md:100`

## https://www.whitehatsec.com/blog/magic-hashes/

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: the file contains ZERO backslash characters, so escape sequences inside quoted code are destroyed - print "Matched. " publishes as print "Matched.n" and the regex character class [!-~] publishes as [x21-x7e]; the article is about hash comparison payloads, so the listings no longer say what they demonstrate (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2015.md:13`

## https://www.whitehatsec.com/home/resources/presentations/files/javascript_malware.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE PORT-SCAN PAYLOAD RENDERS AS NOTHING AND FIRES A REQUEST. Three payloads were published as live markup: the script-source probe the citation is named for - which renders invisible AND makes a reader's browser fetch a script from the internal address in the example - an image with a live error handler, and an applet block. All three show the reader nothing. THE RECORDED TITLE IS THE CITING LIST'S LABEL, not the deck's: slide one reads "Hacking Intranet Websites from the Outside" with the subtitle about JavaScript malware. Note this entry is CITED TWICE from the year list under two different technique names, so a rename needs the repository's sibling convention rather than a second slug. Four slides publish EMPTY because their entire content is a screenshot and no images step ran, and 128 lines at four-space indent become accidental code blocks - 18 percent of the body. publisher is empty although all 36 slides carry the company's copyright footer, and published is empty although slide one states the date. CLEARED: there is no font damage - both reference tools read this PDF perfectly (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2006.md:5`, `2006.md:23`

## https://www.wiz.io/blog/ingress-nginx-kubernetes-vulnerabilities

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: every figure is missing while the prose keeps handing off to one ("the process would look like this:" and "as you see from the above screenshot" with nothing after them); the argument is carried by diagrams and screenshots that did not come through, so re-acquire with an extraction that keeps them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:43`

## https://www.wokb.cz/Hacking%20technique/Abusing%20Internet%20Explorer%208%27s%20XSS%20Filters.pdf

- Outcome: `broken-capture`
- Kind: whitepaper
- Reason: faulty capture: THE BODY H1 IS THE ARCHIVE'S OWN TYPE LABEL. Line 64 reads '# Whitepaper', which is the entry's type field rather than anything in the document; the real title then follows as plain text on the next line, and the PDF's own first line is the title. The frontmatter title is correct, so only the heading is wrong. The render step for this entry is also anomalous - it records a file and result ok with no chars, depth or utc, unlike every sibling - which corroborates a hand-patched render. Separately three inner backticks sit inside the IE8 filter regexes with no fence, so the first opens an inline code span that closes at the second and SWALLOWS EIGHT LINES of regex, leaving the third unpaired; and three escaped payloads publish as live HTML because their block is indented three spaces rather than four (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:10`

## https://www.yeswehack.com/learn-bug-bounty/server-side-template-injection-exploitation

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all 20 code listings have the site's line-number gutter merged into the code, so every published payload is incorrectly prefixed with "1"; re-extract with the gutter excluded (reported 2026-09-02)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:160`

## https://www.yeswehack.com/learn-bug-bounty/syntax-confusion-ambiguous-parsing-exploits

- Outcome: `broken-capture`
- Kind: article
- Reason: faulty capture: all 17 code listings have the site's line-number gutter merged into the code, so the payloads as published are wrong ("1file://127.0.0.1/<pathToFile>", "2%:include <stdio.h>", "1anyBodyParam=@/tmp/../etc/passwd"); re-extract with the gutter excluded (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2025.md:73`

## https://www.youtube.com/watch?v=gVrdE6g_fa8

- Outcome: `broken-capture`
- Kind: video
- Reason: faulty capture: an unclosed inline <noscript> tag at line 75 makes a browser treat the remaining 6,209 characters - over half the file - as raw text rather than content, and <noscript> parsing is the subject of the talk; escape the tag or wrap it in a code span (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2019.md:9`

## https://www.zerodayinitiative.com/blog/2024/12/11/solarwinds-access-rights-manager-one-vulnerability-to-lpe-them-all

- Outcome: `broken-capture` (no bytes stored)
- Kind: advisory
- Reason: faulty capture: every figure is missing while the prose keeps introducing one ("consider the following code:", "This is what the patch looks like:", captions Figure 1 through Figure 10 with nothing above them), so the screenshots carrying the evidence are absent; re-acquire with an extraction that keeps them (reported 2026-08-15) RECOVERY ATTEMPTED 2026-08-17: refetched live; the extraction is byte-identical and still keeps only 3 of the ~13 Squarespace figures (the rest are lazy-loaded and never enter the markup), so the missing screenshots are an extractor/image-discovery limitation on Squarespace decks, not a fetch fault. Fix belongs in image discovery, tested corpus-wide.
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:122`

## https://www.zerodayinitiative.com/blog/2024/9/4/exploiting-exchange-powershell-after-proxynotshell-part-1-multivaluedproperty

- Outcome: `broken-capture` (no bytes stored)
- Kind: advisory
- Reason: faulty capture: every figure is missing while the prose keeps introducing one ("consider the following code:", "This is what the patch looks like:", captions Figure 1 through Figure 10 with nothing above them), so the screenshots carrying the evidence are absent; re-acquire with an extraction that keeps them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2024.md:127`

## https://zeroknock.blogspot.com/2009/12/google-chrome-webkit-msword-scripting.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: A PAYLOAD URI WAS REWRITTEN TO # INSIDE A QUOTED VENDOR REPLY. The source reads iframe src="javascript:alert(document.domain)" and the published file src="#", and the line sits inside the Chrome team's own argument that the two constructs behave similarly - which now reads as nonsense. A closing param tag was deleted as well. The byline and date were dropped although the stored source carries a post-author vcard naming Aditya K Sood and a published timestamp of 2009-12-27. The image policy also inverted the two figures: browser_check.jpg, the comparison table the prose introduces with "is presented below", was rejected as furniture on its 320x94 size while the decorative Chrome logo was the one preserved. Bookkeeping: the manifest raw_sha256 is absent from the store while the file's is present, so the store-gaps row for this entry is chasing a stale hash (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:78`

## https://zeroknock.blogspot.com/2009/12/google-translate-google-user-content.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the byline was dropped although the source carries a post-author vcard naming Aditya K Sood. The recorded title also drops the post own suffix - the heading ends "- A Talk", which the manifest cited_title keeps but the frontmatter and body H1 do not. Twelve lines of nslookup output publish unfenced at column 0, so the aligned address rows collapse onto one line. THREE GENUINE SCREENSHOTS were rejected by the size heuristic as furniture, including the opening figure and the Bing notice, so no local copy exists for them (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2009.md:75`

## https://zeroknock.blogspot.com/2010/08/google-chrome-http-auth-dialog-through.html

- Outcome: `broken-capture` (no bytes stored)
- Kind: article
- Reason: faulty capture: the body ends on a dangling lead-in - the last line is "The video is embedded below" with nothing after it - and the stored source shows the YouTube embed that followed, whose id r1KuE2th_EY is recoverable. The byline was dropped although the source carries "Posted by Aditya K Sood". Note also a metadata inconsistency worth reconciling before its store-gaps row is trusted: the manifest raw_sha256 is absent from the store while the hash the PUBLISHED FILE names is present, so the reference is listed as a store gap on the strength of the wrong value (reported 2026-08-15)
- What would fix it: The recorded fault names its own remedy - follow it, then `refs.py acquire --faulty-captures` (or `wayback --faulty-captures`) and clear `content_gap`.
- Cited at: `2010.md:61`

## https://zenodo.org/records/20762298

- Outcome: `needs-browser`
- Kind: article
- Reason: extraction produced 34 characters, below the floor
- What would fix it: The page is built by JavaScript and the browser ladder could not settle it. Try saving the page by hand and importing it.
- Cited at: `2026-ai.md:72`

## https://www.usenix.org/conference/hotbots-07/ghost-browser-analysis-web-based-malware

- Outcome: `stored`
- Kind: article
- Reason: WIDENING: this entry and its sibling paper entry hold THE SAME PDF BYTES and neither cross-links the other. The same unclosed script tag swallows 45,121 of 95,755 characters here, and the same two-column interleaving destroys the obfuscated-JavaScript figure. One further caution: do NOT re-extract this with the pypdf path - it deletes the fi ligature from this PDF entirely, where pdftotext reads all 89 instances cleanly. (reported 2026-08-15)
- What would fix it: Unknown cause. Read the reason and decide.
- Cited at: `2007.md:103`
